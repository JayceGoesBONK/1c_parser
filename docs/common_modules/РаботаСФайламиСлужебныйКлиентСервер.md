# Модуль РаботаСФайламиСлужебныйКлиентСервер

**Описание подсистемы:** Работа с файлами служебный клиент сервер

Всего найдено экспортных методов: **17**

## Список методов

- [УникальноеИмяСПутем](#уникальноеимяспутем) (Функция)
- [РасширениеФайлаВСписке](#расширениефайлавсписке) (Функция)
- [СообщениеОНедопустимостиПодписанияЗанятогоФайла](#сообщениеонедопустимостиподписаниязанятогофайла) (Функция)
- [СообщениеОНедопустимостиПодписанияЗашифрованногоФайла](#сообщениеонедопустимостиподписаниязашифрованногофайла) (Функция)
- [ПредставлениеРазмераФайла](#представлениеразмерафайла) (Функция)
- [ИндексПиктограммыФайла](#индекспиктограммыфайла) (Функция)
- [ЗаполнитьСтатусПодписи](#заполнитьстатусподписи) (Процедура)
- [АдресВОблачномСервисе](#адресвоблачномсервисе) (Функция)
- [ПараметрыЗанятияФайла](#параметрызанятияфайла) (Функция)
- [ОписаниеКомпоненты](#описаниекомпоненты) (Функция)
- [ИзвлечьТекстИзТекстовогоФайла](#извлечьтекстизтекстовогофайла) (Функция)
- [ИзвлечьТекстOpenDocument](#извлечьтекстopendocument) (Функция)
- [ИмяСканированногоФайла](#имясканированногофайла) (Функция)
- [ПараметрыДобавленияФайла](#параметрыдобавленияфайла) (Функция)
- [ОпределитьКодировкуДвоичныхДанных](#определитькодировкудвоичныхданных) (Функция)
- [Кодировки](#кодировки) (Функция)
- [ПараметрыСканирования](#параметрысканирования) (Функция)

---

### <a name="уникальноеимяспутем"></a>УникальноеИмяСПутем

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УникальноеИмяСПутем(Знач ИмяКаталога, Знач ИмяФайла) Экспорт
  ```
- **Описание:**

  Получить уникальное имя файла в рабочем каталоге.
  Если есть совпадения, будет имя подобное "A1\Приказ.doc".

---

### <a name="расширениефайлавсписке"></a>РасширениеФайлаВСписке

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РасширениеФайлаВСписке(СписокРасширений, РасширениеФайла) Экспорт
  ```
- **Описание:**

  Возвращает Истина, если файл с таким расширением находится в списке расширений.

---

### <a name="сообщениеонедопустимостиподписаниязанятогофайла"></a>СообщениеОНедопустимостиПодписанияЗанятогоФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СообщениеОНедопустимостиПодписанияЗанятогоФайла(ФайлСсылка = Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает сообщение о недопустимости подписания занятого файла.

---

### <a name="сообщениеонедопустимостиподписаниязашифрованногофайла"></a>СообщениеОНедопустимостиПодписанияЗашифрованногоФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СообщениеОНедопустимостиПодписанияЗашифрованногоФайла(ФайлСсылка = Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает сообщение о недопустимости подписания зашифрованного файла.

---

### <a name="представлениеразмерафайла"></a>ПредставлениеРазмераФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредставлениеРазмераФайла(Знач РазмерВМб) Экспорт
  ```
- **Описание:**

  Получить строку с представлением размера файла - например для отображения состояния при передаче файла.

---

### <a name="индекспиктограммыфайла"></a>ИндексПиктограммыФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИндексПиктограммыФайла(Знач РасширениеФайла) Экспорт
  ```
- **Описание:**

  Получить индекс пиктограммы файла - индекс в картинке КоллекцияПиктограммФайлов.

---

### <a name="заполнитьстатусподписи"></a>ЗаполнитьСтатусПодписи

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьСтатусПодписи(СтрокаПодписи, ТекущаяДата) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="адресвоблачномсервисе"></a>АдресВОблачномСервисе

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция АдресВОблачномСервисе(Сервис, Href) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="параметрызанятияфайла"></a>ПараметрыЗанятияФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыЗанятияФайла() Экспорт
  ```
- **Описание:**

  Параметры занятия файла на редактирование.

  Возвращаемое значение:
    Структура:
      * УникальныйИдентификатор - уникальный идентификатор формы.
      * Пользователь - СправочникСсылка.Пользователи
      * ДополнительныеСвойства - Структура - дополнительные свойства для записи файла.

---

### <a name="описаниекомпоненты"></a>ОписаниеКомпоненты

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОписаниеКомпоненты() Экспорт
  ```
- **Описание:**

  Описание подключения внешней компоненты сканирования.

  Возвращаемое значение:
   Структура:
    * ПолноеИмяМакета - Строка
    * ИмяОбъекта      - Строка

---

### <a name="извлечьтекстизтекстовогофайла"></a>ИзвлечьТекстИзТекстовогоФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИзвлечьТекстИзТекстовогоФайла(ПолноеИмяФайла, Кодировка, Отказ) Экспорт
  ```
- **Описание:**

  Извлекает текст в соответствии с кодировкой.
  Если кодировка не задана - сама вычисляет кодировку.

---

### <a name="извлечьтекстopendocument"></a>ИзвлечьТекстOpenDocument

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИзвлечьТекстOpenDocument(ПутьКФайлу, Отказ) Экспорт
  ```
- **Описание:**

  Извлечь текст из файла OpenDocument и возвратить его в виде строки.

---

### <a name="имясканированногофайла"></a>ИмяСканированногоФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяСканированногоФайла(НомерФайла, ПрефиксБазы) Экспорт
  ```
- **Описание:**

  Получить имя сканированного файла, вида ДМ-00000012, где ДМ - префикс базы.

  Параметры:
   НомерФайла  - Число - целое число, например, 12.
   ПрефиксБазы - Строка - префикс базы, например, "ДМ".

  Возвращаемое значение:
   Строка - имя сканированного файла, например, "ДМ-00000012".

---

### <a name="параметрыдобавленияфайла"></a>ПараметрыДобавленияФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыДобавленияФайла(ДополнительныеРеквизиты = Неопределено) Экспорт
  ```
- **Описание:**

  Инициализирует структуру параметров для добавления файла.
  Для использования в РаботаСФайлами.ДобавитьФайл и РаботаСФайламиСлужебныйВызовСервера.ДобавитьФайл.

---

### <a name="определитькодировкудвоичныхданных"></a>ОпределитьКодировкуДвоичныхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОпределитьКодировкуДвоичныхДанных(ДанныеДляАнализа, Расширение) Экспорт
  ```
- **Описание:**

  Автоматически определяет и возвращает кодировку текстового файла.

  Параметры:
   ДанныеДляАнализа - ДвоичныеДанные, Строка - данные для определения кодировки или адрес данных.
   Расширение         - Строка - расширение файла.

  Возвращаемое значение:
   Строка

---

### <a name="кодировки"></a>Кодировки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция Кодировки() Экспорт
  ```
- **Описание:**

  Возвращает таблицу имен кодировок.

  Возвращаемое значение:
    СписокЗначений:
      * Значение - Строка - например "ibm852".
      * Представление - Строка - например "ibm852 (Центральноевропейская DOS)".

---

### <a name="параметрысканирования"></a>ПараметрыСканирования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыСканирования() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

