# Модуль РаботаСФайламиКлиентСервер

**Описание подсистемы:** Работа с файлами клиент сервер

Всего найдено экспортных методов: **19**

## Список методов

- [ПараметрыДанныхФайла](#параметрыданныхфайла) (Функция)
- [ОпределитьФормуПрисоединенногоФайла](#определитьформуприсоединенногофайла) (Процедура)
- [НастройкиСканированияПользователя](#настройкисканированияпользователя) (Функция)
- [СведенияОФайле](#сведенияофайле) (Функция)
- [МожноЛиЗанятьФайл](#можнолизанятьфайл) (Функция)
- [ТолькоОдинФайлТекст](#толькоодинфайлтекст) (Функция)
- [ПрефиксКоманд](#префикскоманд) (Функция)
- [ИмяКомандыЗагрузитьФайл](#имякомандызагрузитьфайл) (Функция)
- [ИмяКомандыСоздатьПоШаблону](#имякомандысоздатьпошаблону) (Функция)
- [ИмяКомандыСканировать](#имякомандысканировать) (Функция)
- [ИмяКомандыОткрытьСписок](#имякомандыоткрытьсписок) (Функция)
- [ИмяКомандыВыбратьФайл](#имякомандывыбратьфайл) (Функция)
- [ИмяКомандыПросмотретьФайл](#имякомандыпросмотретьфайл) (Функция)
- [ИмяКомандыОчистить](#имякомандыочистить) (Функция)
- [ИмяКомандыОткрытьФорму](#имякомандыоткрытьформу) (Функция)
- [ИмяКомандыРедактироватьФайл](#имякомандыредактироватьфайл) (Функция)
- [ИмяКомандыПоместитьФайл](#имякомандыпоместитьфайл) (Функция)
- [ИмяКомандыОтменитьРедактирование](#имякомандыотменитьредактирование) (Функция)
- [ИмяДополнительнойКомандыИзКонтекстногоМеню](#имядополнительнойкомандыизконтекстногоменю) (Функция)

---

### <a name="параметрыданныхфайла"></a>ПараметрыДанныхФайла

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыДанныхФайла() Экспорт
  ```
- **Описание:**

  Инициализирует структуру параметров для получения данных файла. См. РаботаСФайлами.ДанныеФайла.

  Возвращаемое значение:
   Структура:
     * ИдентификаторФормы             - УникальныйИдентификатор - уникальный идентификатор формы, во временное хранилище
                                      которой надо поместить файл и вернуть адрес в свойстве СсылкаНаДвоичныеДанныеФайла.
                                      Значение по умолчанию - Неопределено.
     * ПолучатьСсылкуНаДвоичныеДанные - Булево - если передать Ложь, то ссылка на двоичные данные в СсылкаНаДвоичныеДанныеФайла
                                      не будет получена, что существенно ускорит выполнение для больших двоичных данных.
                                      Значение по умолчанию - Истина.
     * ДляРедактирования              - Булево - если указать Истина, то файл будет захвачен для редактирования.
                                      Значение по умолчанию - Ложь.
     * ВызыватьИсключение             - Булево - если указать Ложь, то функция в исключительных ситуациях не будет вызывать
                                      исключений и вернет Неопределено. Значение по умолчанию - Истина.

---

### <a name="определитьформуприсоединенногофайла"></a>ОпределитьФормуПрисоединенногоФайла

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОпределитьФормуПрисоединенногоФайла(Источник, ВидФормы, Параметры,
  	ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка) Экспорт
  ```
- **Описание:**

  Обработчик подписки на событие ОбработкаПолученияФормы для переопределения формы файла.

  Параметры:
   Источник                 - СправочникМенеджер - менеджер справочника с именем "*ПрисоединенныеФайлы".
   ВидФормы                 - Строка - имя стандартной формы.
   Параметры                - Структура - параметры формы.
   ВыбраннаяФорма           - Строка - имя или объект метаданных открываемой формы.
   ДополнительнаяИнформация - Структура - дополнительная информация открытия формы.
   СтандартнаяОбработка     - Булево - признак выполнения стандартной (системной) обработки события.

---

### <a name="настройкисканированияпользователя"></a>НастройкиСканированияПользователя

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НастройкиСканированияПользователя() Экспорт
  ```
- **Описание:**

  Возвращает настройки сканирования пользователя.

  Возвращаемое значение:
   Структура:
    * ПоказыватьДиалогСканера - Булево
    * ИмяУстройства - Строка - НаименованиеСканера
    * ФорматСканированногоИзображения - ПеречислениеСсылка.ФорматыСканированногоИзображения
    * СохранятьВPDF - Булево
    * ФорматХраненияМногостраничный - ПеречислениеСсылка.ФорматыХраненияМногостраничныхФайлов 
    * Разрешение - ПеречислениеСсылка.РазрешенияСканированногоИзображения
    * Цветность - ПеречислениеСсылка.ЦветностиИзображения
    * Поворот - ПеречислениеСсылка.СпособыПоворотаИзображения
    * РазмерБумаги - ПеречислениеСсылка.РазмерыБумаги
    * ДвустороннееСканирование - Булево
    * АвтоматическаяПодачаДокументов - Булево
    * ИспользоватьImageMagickДляПреобразованияВPDF - Булево
    * КачествоJPG - Число
    * СжатиеTIFF - ПеречислениеСсылка.ВариантыСжатияTIFF
    * ПутьКПрограммеКонвертации - Строка

---

### <a name="сведенияофайле"></a>СведенияОФайле

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СведенияОФайле(Знач Режим, Знач ИсходныйФайл = Неопределено) Экспорт
  ```
- **Описание:**

  Инициализирует структуру со сведениями о файле.

  Параметры:
    Режим        - Строка - "Файл" или "ФайлСВерсией".
    ИсходныйФайл - Файл   - файл, на основании которого заполняются свойства структуры.

  Возвращаемое значение:
    Структура:
     * ИмяБезРасширения             - Строка - имя файла без расширения.
     * РасширениеБезТочки           - Строка - расширение файла.
     * ВремяИзменения               - Дата   - дата и время изменения файла.
     * ВремяИзмененияУниверсальное  - Дата   - UTC дата и время изменения файла.
     * Размер                       - Число  - размер файла в байтах.
     * АдресВременногоХранилищаФайла  - Строка
                                      - ХранилищеЗначения - адрес во временном хранилище с двоичными данными
                                        файла или непосредственно двоичные данные файла.
     * АдресВременногоХранилищаТекста - Строка
                                      - ХранилищеЗначения - адрес во временном хранилище с извлеченным текстов
                                        для ППД или непосредственно сами данные с текстом.
     * ЭтоВебКлиент                 - Булево - Истина, если вызов идет из веб клиента.
     * Автор                        - СправочникСсылка.Пользователи - автор файла. Если Неопределено, то текущий
                                                                      пользователь.
     * Комментарий                  - Строка - комментарий к файлу.
     * ЗаписатьВИсторию             - Булево - записать в историю работы пользователя.
     * ХранитьВерсии                - Булево - разрешить хранение версий у файла в ИБ;
                                               при создании новой версии - создавать новую версию, или изменять
                                               существующую (Ложь).
     * Зашифрован                   - Булево - файл зашифрован.

---

### <a name="можнолизанятьфайл"></a>МожноЛиЗанятьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция МожноЛиЗанятьФайл(ДанныеФайла, ТекстСообщения = "") Экспорт
  ```
- **Описание:**

  Определить, можно ли занять файл и, если нет, то сформировать сообщение.

  Параметры:
   ДанныеФайла  - Структура - структура с данными файла.
   ТекстСообщения - Строка - возвращаемое значение. Если файл занять нельзя,
                             то содержит описание причины.

  Возвращаемое значение:
   Булево - если Истина, тогда текущий пользователь может занять файл или
            файл уже занят текущим пользователем.

---

### <a name="толькоодинфайлтекст"></a>ТолькоОдинФайлТекст

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТолькоОдинФайлТекст() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="префикскоманд"></a>ПрефиксКоманд

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПрефиксКоманд() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандызагрузитьфайл"></a>ИмяКомандыЗагрузитьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыЗагрузитьФайл() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандысоздатьпошаблону"></a>ИмяКомандыСоздатьПоШаблону

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыСоздатьПоШаблону() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандысканировать"></a>ИмяКомандыСканировать

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыСканировать() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыоткрытьсписок"></a>ИмяКомандыОткрытьСписок

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыОткрытьСписок() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандывыбратьфайл"></a>ИмяКомандыВыбратьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыВыбратьФайл() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыпросмотретьфайл"></a>ИмяКомандыПросмотретьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыПросмотретьФайл() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыочистить"></a>ИмяКомандыОчистить

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыОчистить() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыоткрытьформу"></a>ИмяКомандыОткрытьФорму

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыОткрытьФорму() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыредактироватьфайл"></a>ИмяКомандыРедактироватьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыРедактироватьФайл() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыпоместитьфайл"></a>ИмяКомандыПоместитьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыПоместитьФайл() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имякомандыотменитьредактирование"></a>ИмяКомандыОтменитьРедактирование

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяКомандыОтменитьРедактирование() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имядополнительнойкомандыизконтекстногоменю"></a>ИмяДополнительнойКомандыИзКонтекстногоМеню

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяДополнительнойКомандыИзКонтекстногоМеню() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

