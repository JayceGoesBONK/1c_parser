# Модуль УправлениеПечатьюOOWriterКлиент

**Описание подсистемы:** Управление печатью OpenOffice.org Writer клиент

Всего найдено экспортных методов: **16**

## Список методов

- [ИнициализироватьПечатнуюФормуOOWriter](#инициализироватьпечатнуюформуoowriter) (Функция)
- [ПолучитьМакетOOWriter](#получитьмакетoowriter) (Функция)
- [ЗакрытьСоединение](#закрытьсоединение) (Процедура)
- [ПоказатьДокументOOWriter](#показатьдокументoowriter) (Процедура)
- [ПолучитьОбластьМакета](#получитьобластьмакета) (Функция)
- [ПолучитьОбластьВерхнегоКолонтитула](#получитьобластьверхнегоколонтитула) (Функция)
- [ПолучитьОбластьНижнегоКолонтитула](#получитьобластьнижнегоколонтитула) (Функция)
- [ВставитьРазрывНаНовуюСтроку](#вставитьразрывнановуюстроку) (Процедура)
- [ДобавитьВерхнийКолонтитул](#добавитьверхнийколонтитул) (Процедура)
- [ДобавитьНижнийКолонтитул](#добавитьнижнийколонтитул) (Процедура)
- [ПрисоединитьОбласть](#присоединитьобласть) (Процедура)
- [ЗаполнитьПараметры](#заполнитьпараметры) (Процедура)
- [ПрисоединитьИЗаполнитьКоллекцию](#присоединитьизаполнитьколлекцию) (Процедура)
- [УстановитьОсновнойКурсорНаТелоДокумента](#установитьосновнойкурсорнателодокумента) (Процедура)
- [УстановитьОсновнойКурсорНаВерхнийКолонтитул](#установитьосновнойкурсорнаверхнийколонтитул) (Функция)
- [УстановитьОсновнойКурсорНаНижнийКолонтитул](#установитьосновнойкурсорнанижнийколонтитул) (Функция)

---

### <a name="инициализироватьпечатнуюформуoowriter"></a>ИнициализироватьПечатнуюФормуOOWriter

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИнициализироватьПечатнуюФормуOOWriter(Знач Шаблон = Неопределено) Экспорт
  ```
- **Описание:**

  Инициализация печатной формы: создается COM - объект,
  ему устанавливаются свойства.

---

### <a name="получитьмакетoowriter"></a>ПолучитьМакетOOWriter

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьМакетOOWriter(Знач ДвоичныеДанныеШаблона, ИмяВременногоФайла) Экспорт
  ```
- **Описание:**

  Возвращает структуру с макетом печатной формы.

  Параметры:
    ДвоичныеДанныеШаблона - ДвоичныеДанные - двоичные данные макета.
  Возвращаемое значение:
    Структура - ссылка макет.

---

### <a name="закрытьсоединение"></a>ЗакрытьСоединение

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗакрытьСоединение(Handler, Знач ЗакрытьПриложение) Экспорт
  ```
- **Описание:**

  Закрывает макет печатной формы и затирает ссылки на COM объект.

---

### <a name="показатьдокументoowriter"></a>ПоказатьДокументOOWriter

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьДокументOOWriter(Знач Handler) Экспорт
  ```
- **Описание:**

  Устанавливает свойство видимости у приложения OO Writer.

  Параметры:
   Handler - Структура - ссылка на печатную форму.

---

### <a name="получитьобластьмакета"></a>ПолучитьОбластьМакета

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбластьМакета(Знач Handler, Знач ИмяОбласти) Экспорт
  ```
- **Описание:**

  Получает область из макета.
  Параметры:
    Handler - ссылка на макет
    ИмяОбласти - имя области в макете.
    СмещениеНачало - смещение относительно начала области
  				смещение по умолчанию: 1 - означает что область берется без символа
  				перевода строки, за операторной скобкой открытия области.
    СмещениеОкончание - смещение относительно окончания области,
  				смещение по умолчанию: -11 - означает что область берется без символа
  				перевода строки, перед операторной скобкой закрытия области.

---

### <a name="получитьобластьверхнегоколонтитула"></a>ПолучитьОбластьВерхнегоКолонтитула

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбластьВерхнегоКолонтитула(Знач МакетСсылка) Экспорт
  ```
- **Описание:**

  Получает область верхнего колонтитула.

---

### <a name="получитьобластьнижнегоколонтитула"></a>ПолучитьОбластьНижнегоКолонтитула

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбластьНижнегоКолонтитула(МакетСсылка) Экспорт
  ```
- **Описание:**

  Получает область нижнего колонтитула.

---

### <a name="вставитьразрывнановуюстроку"></a>ВставитьРазрывНаНовуюСтроку

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВставитьРазрывНаНовуюСтроку(Знач Handler) Экспорт
  ```
- **Описание:**

  Вставляет разрыв на следующую строку.
  Параметры:
    Handler - ссылка на документ MS Word в который требуется вставить разрыв.

---

### <a name="добавитьверхнийколонтитул"></a>ДобавитьВерхнийКолонтитул

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьВерхнийКолонтитул(Знач ПечатнаяФорма,
  									Знач Область) Экспорт
  ```
- **Описание:**

  Добавляет верхний колонтитул к печатной форме.

---

### <a name="добавитьнижнийколонтитул"></a>ДобавитьНижнийКолонтитул

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьНижнийКолонтитул(Знач ПечатнаяФорма,
  									Знач Область) Экспорт
  ```
- **Описание:**

  Добавляет нижний колонтитул к печатной форме.

---

### <a name="присоединитьобласть"></a>ПрисоединитьОбласть

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПрисоединитьОбласть(Знач ПечатнаяФормаHandler,
  							Знач ОбластьHandler,
  							Знач ПереходНаСледСтроку = Истина,
  							Знач ПрисоединитьСтрокуТаблицы = Ложь) Экспорт
  ```
- **Описание:**

  Добавляет область в печатную форму из макета, при этом заменяя
  параметры в области значениями из данных объекта.
  Применяется при одиночном выводе области.

  Параметры:
    ПечатнаяФорма - ссылка на печатную форму.
    ОбластьHandler - ссылка на область в макете.
    ПереходНаСледСтроку - Булево - требуется ли вставлять разрыв после вывода области.

---

### <a name="заполнитьпараметры"></a>ЗаполнитьПараметры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьПараметры(ПечатнаяФорма, Данные) Экспорт
  ```
- **Описание:**

  Заполняет параметры в табличном поле печатной формы.

---

### <a name="присоединитьизаполнитьколлекцию"></a>ПрисоединитьИЗаполнитьКоллекцию

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПрисоединитьИЗаполнитьКоллекцию(Знач ПечатнаяФормаHandler,
  										  Знач ОбластьHandler,
  										  Знач Данные,
  										  Знач ЭтоСтрокаТаблицы = Ложь,
  										  Знач ПереходНаСледСтроку = Истина) Экспорт
  ```
- **Описание:**

  Добавляет область коллекции к печатной форме.

---

### <a name="установитьосновнойкурсорнателодокумента"></a>УстановитьОсновнойКурсорНаТелоДокумента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьОсновнойКурсорНаТелоДокумента(Знач ДокументСсылка) Экспорт
  ```
- **Описание:**

  Устанавливает курсор в конец документа ДокументСсылка.

---

### <a name="установитьосновнойкурсорнаверхнийколонтитул"></a>УстановитьОсновнойКурсорНаВерхнийКолонтитул

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УстановитьОсновнойКурсорНаВерхнийКолонтитул(Знач ДокументСсылка) Экспорт
  ```
- **Описание:**

  Устанавливает курсор на верхний колонтитул.

---

### <a name="установитьосновнойкурсорнанижнийколонтитул"></a>УстановитьОсновнойКурсорНаНижнийКолонтитул

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УстановитьОсновнойКурсорНаНижнийКолонтитул(Знач ДокументСсылка) Экспорт
  ```
- **Описание:**

  Устанавливает курсор на нижний колонтитул.

---

