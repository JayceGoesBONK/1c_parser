# Модуль ОбменДаннымиВнешнееСоединение

**Описание подсистемы:** Обмен данными внешнее соединение

Всего найдено экспортных методов: **18**

## Список методов

- [ПолучитьОбъектыТаблицы](#получитьобъектытаблицы) (Функция)
- [ПолучитьОбъектыТаблицы_2_0_1_6](#получитьобъектытаблицы_2_0_1_6) (Функция)
- [СвойстваОбъектаМетаданных](#свойстваобъектаметаданных) (Функция)
- [ВыполнитьВыгрузкуДляУзлаИнформационнойБазы](#выполнитьвыгрузкудляузлаинформационнойбазы) (Процедура)
- [ЗаписьЖурналаРегистрацииНачалаОбменаДанными](#записьжурналарегистрацииначалаобменаданными) (Процедура)
- [ЗафиксироватьЗавершениеОбмена](#зафиксироватьзавершениеобмена) (Процедура)
- [ПолучитьПравилаКонвертацииОбъектов](#получитьправилаконвертацииобъектов) (Функция)
- [СтруктураНастроекОбмена](#структуранастроекобмена) (Функция)
- [ПланОбменаСуществует](#планобменасуществует) (Функция)
- [ПрефиксИнформационнойБазыПоУмолчанию](#префиксинформационнойбазыпоумолчанию) (Функция)
- [ПредупреждатьОНесоответствииВерсийПравилОбмена](#предупреждатьонесоответствииверсийправилобмена) (Функция)
- [РольДоступнаПолныеПрава](#рольдоступнаполныеправа) (Функция)
- [НаименованиеПредопределенногоУзлаПланаОбмена](#наименованиепредопределенногоузлапланаобмена) (Функция)
- [КодПредопределенногоУзлаПланаОбмена](#кодпредопределенногоузлапланаобмена) (Функция)
- [ПолучитьОбщиеДанныеУзлов](#получитьобщиеданныеузлов) (Функция)
- [ПолучитьОбщиеДанныеУзлов_2_0_1_6](#получитьобщиеданныеузлов_2_0_1_6) (Функция)
- [ПолучитьПараметрыИнформационнойБазы](#получитьпараметрыинформационнойбазы) (Функция)
- [ПолучитьПараметрыИнформационнойБазы_2_0_1_6](#получитьпараметрыинформационнойбазы_2_0_1_6) (Функция)

---

### <a name="получитьобъектытаблицы"></a>ПолучитьОбъектыТаблицы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбъектыТаблицы(ПолноеИмяТаблицы) Экспорт
  ```
- **Описание:**

  Возвращает данные списка объектов заданного объекта метаданных в виде системного представления.

  Параметры:
   ПолноеИмяТаблицы - Строка - имя таблицы, соответствующей объекту метаданных.

  Возвращаемое значение:
   Строка - системное представление таблицы значений, содержащей данные заданного объекта метаданных.

---

### <a name="получитьобъектытаблицы_2_0_1_6"></a>ПолучитьОбъектыТаблицы_2_0_1_6

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбъектыТаблицы_2_0_1_6(ПолноеИмяТаблицы) Экспорт
  ```
- **Описание:**

  Возвращает данные списка объектов заданного объекта метаданных в виде XML-строки.

  Параметры:
   ПолноеИмяТаблицы - Строка - имя таблицы, соответствующей объекту метаданных.

  Возвращаемое значение:
   Строка - XML-строка сериализованного представления таблицы значений, содержащей данные заданного объекта метаданных.

---

### <a name="свойстваобъектаметаданных"></a>СвойстваОбъектаМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СвойстваОбъектаМетаданных(ПолноеИмяТаблицы) Экспорт
  ```
- **Описание:**

  Возвращает заданные свойства (Синоним, Иерархический) объекта метаданных.

  Параметры:
   ПолноеИмяТаблицы - Строка - имя таблицы, соответствующей объекту метаданных.

  Возвращаемое значение:
   СтруктураНастроек - Структура - свойства объекта метаданных:
     * Синоним - Строка - синоним.
     * Иерархический - Строка - признак Иерархический.

---

### <a name="выполнитьвыгрузкудляузлаинформационнойбазы"></a>ВыполнитьВыгрузкуДляУзлаИнформационнойБазы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыполнитьВыгрузкуДляУзлаИнформационнойБазы(Отказ,
  												ИмяПланаОбмена,
  												КодУзлаИнформационнойБазы,
  												ПолноеИмяФайлаСообщенияОбмена,
  												СтрокаСообщенияОбОшибке = "") Экспорт
  ```
- **Описание:**

  Выполняет выгрузку данных для узла информационной базы во временный файл.
  (Только для внутреннего использования).

---

### <a name="записьжурналарегистрацииначалаобменаданными"></a>ЗаписьЖурналаРегистрацииНачалаОбменаДанными

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписьЖурналаРегистрацииНачалаОбменаДанными(СтруктураНастроекОбмена) Экспорт
  ```
- **Описание:**

  Помещает в журнал регистрации запись о начале обмена данными.
  (Только для внутреннего использования).

---

### <a name="зафиксироватьзавершениеобмена"></a>ЗафиксироватьЗавершениеОбмена

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗафиксироватьЗавершениеОбмена(СтруктураНастроекОбменаВнешнееСоединение) Экспорт
  ```
- **Описание:**

  Фиксирует завершение обмена данными через внешнее соединение.
  (Только для внутреннего использования).

---

### <a name="получитьправилаконвертацииобъектов"></a>ПолучитьПравилаКонвертацииОбъектов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьПравилаКонвертацииОбъектов(ИмяПланаОбмена, ПолучатьПравилаКорреспондента = Ложь) Экспорт
  ```
- **Описание:**

  Получает зачитанные правила конвертации объектов по имени плана обмена.
  (Только для внутреннего использования).

   Возвращаемое значение:
     Зачитанные правила конвертации объектов.

---

### <a name="структуранастроекобмена"></a>СтруктураНастроекОбмена

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СтруктураНастроекОбмена(Структура) Экспорт
  ```
- **Описание:**

  Получает структуру настроек обмена.
  (Только для внутреннего использования).

---

### <a name="планобменасуществует"></a>ПланОбменаСуществует

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПланОбменаСуществует(ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Проверяет существование плана обмена с заданным именем.
  (Только для внутреннего использования).

---

### <a name="префиксинформационнойбазыпоумолчанию"></a>ПрефиксИнформационнойБазыПоУмолчанию

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПрефиксИнформационнойБазыПоУмолчанию() Экспорт
  ```
- **Описание:**

  Получает префикс информационной базы по умолчанию через внешнее соединение.
  Обертка одноименной функции в переопределяемом модуле.
  (Только для внутреннего использования).

---

### <a name="предупреждатьонесоответствииверсийправилобмена"></a>ПредупреждатьОНесоответствииВерсийПравилОбмена

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредупреждатьОНесоответствииВерсийПравилОбмена(ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Проверяет необходимость проверки расхождения версий в правилах конвертации.

---

### <a name="рольдоступнаполныеправа"></a>РольДоступнаПолныеПрава

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РольДоступнаПолныеПрава() Экспорт
  ```
- **Описание:**

  Возвращает признак доступности роли ПолныеПрава.
  (Только для внутреннего использования).

---

### <a name="наименованиепредопределенногоузлапланаобмена"></a>НаименованиеПредопределенногоУзлаПланаОбмена

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НаименованиеПредопределенногоУзлаПланаОбмена(ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Возвращает название предопределенного узла плана обмена.
  (Только для внутреннего использования).

---

### <a name="кодпредопределенногоузлапланаобмена"></a>КодПредопределенногоУзлаПланаОбмена

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КодПредопределенногоУзлаПланаОбмена(ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Возвращает код предопределенного узла плана обмена.
  (Только для внутреннего использования).

---

### <a name="получитьобщиеданныеузлов"></a>ПолучитьОбщиеДанныеУзлов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбщиеДанныеУзлов(Знач ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Для внутреннего использования.

---

### <a name="получитьобщиеданныеузлов_2_0_1_6"></a>ПолучитьОбщиеДанныеУзлов_2_0_1_6

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьОбщиеДанныеУзлов_2_0_1_6(Знач ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Для внутреннего использования.

---

### <a name="получитьпараметрыинформационнойбазы"></a>ПолучитьПараметрыИнформационнойБазы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьПараметрыИнформационнойБазы(Знач ИмяПланаОбмена, Знач КодУзла, СообщениеОбОшибке) Экспорт
  ```
- **Описание:**

  Для внутреннего использования.

---

### <a name="получитьпараметрыинформационнойбазы_2_0_1_6"></a>ПолучитьПараметрыИнформационнойБазы_2_0_1_6

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьПараметрыИнформационнойБазы_2_0_1_6(Знач ИмяПланаОбмена, Знач КодУзла, СообщениеОбОшибке) Экспорт
  ```
- **Описание:**

  Для внутреннего использования.

---

