# Модуль ВариантыОтчетовКлиентСервер

**Описание подсистемы:** Варианты отчетов клиент сервер

Всего найдено экспортных методов: **6**

## Список методов

- [ИдентификаторНачальнойСтраницы](#идентификаторначальнойстраницы) (Функция)
- [ДополнитьСтруктуруКлючом](#дополнитьструктуруключом) (Процедура)
- [ПолноеИмяПодсистемы](#полноеимяподсистемы) (Функция)
- [РазложитьСтрокуПоискаВМассивСлов](#разложитьстрокупоискавмассивслов) (Функция)
- [ТипОтчетаСтрокой](#типотчетастрокой) (Функция)
- [ИмяДействияПрименитьПереданныеНастройки](#имядействияприменитьпереданныенастройки) (Функция)

---

### <a name="идентификаторначальнойстраницы"></a>ИдентификаторНачальнойСтраницы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИдентификаторНачальнойСтраницы() Экспорт
  ```
- **Описание:**

  Идентификатор, который используется для начальной страницы в модуле ВариантыОтчетовПереопределяемый.

  Возвращаемое значение:
    Строка - идентификатор, который используется для начальной страницы в модуле ВариантыОтчетовПереопределяемый.

---

### <a name="дополнитьструктуруключом"></a>ДополнитьСтруктуруКлючом

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДополнитьСтруктуруКлючом(Структура, Ключ, Значение = Неопределено) Экспорт
  ```
- **Описание:**

  Добавляет Ключ в Структуру если его нет.

  Параметры:
    Структура - Структура    - дополняемая структура.
    Ключ      - Строка       - имя свойства.
    Значение  - Произвольный - значение свойства если оно отсутствует в структуре.

---

### <a name="полноеимяподсистемы"></a>ПолноеИмяПодсистемы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолноеИмяПодсистемы() Экспорт
  ```
- **Описание:**

  Полное имя подсистемы.

---

### <a name="разложитьстрокупоискавмассивслов"></a>РазложитьСтрокуПоискаВМассивСлов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РазложитьСтрокуПоискаВМассивСлов(СтрокаПоиска) Экспорт
  ```
- **Описание:**

  Превращает строку поиска в массив слов с уникальными значениями, отсортированный по убыванию длины.

---

### <a name="типотчетастрокой"></a>ТипОтчетаСтрокой

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТипОтчетаСтрокой(Знач ТипОтчета, Знач Отчет = Неопределено) Экспорт
  ```
- **Описание:**

  Превращает тип отчета в строковый идентификатор.

---

### <a name="имядействияприменитьпереданныенастройки"></a>ИмяДействияПрименитьПереданныеНастройки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяДействияПрименитьПереданныеНастройки() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

