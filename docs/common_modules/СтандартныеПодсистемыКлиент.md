# Модуль СтандартныеПодсистемыКлиент

**Описание подсистемы:** Стандартные подсистемы клиент

Всего найдено экспортных методов: **83**

## Список методов

- [УстановитьРасширенныйЗаголовокПриложения](#установитьрасширенныйзаголовокприложения) (Процедура)
- [ПоказатьВопросПользователю](#показатьвопроспользователю) (Процедура)
- [ПараметрыВопросаПользователю](#параметрывопросапользователю) (Функция)
- [ОткрытьСписокАктивныхПользователей](#открытьсписокактивныхпользователей) (Процедура)
- [ЭтоБазоваяВерсияКонфигурации](#этобазоваяверсияконфигурации) (Функция)
- [ЭтоУчебнаяПлатформа](#этоучебнаяплатформа) (Функция)
- [ВывестиИнформациюОбОшибке](#вывестиинформациюобошибке) (Процедура)
- [НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке](#настроитьвидимостьизаголовокссылкиотправкиотчетаобошибке) (Процедура)
- [ПоказатьОтчетОбОшибке](#показатьотчетобошибке) (Процедура)
- [ОтправитьОтчетОбОшибке](#отправитьотчетобошибке) (Процедура)
- [ПропуститьПредупреждениеПередЗавершениемРаботыСистемы](#пропуститьпредупреждениепередзавершениемработысистемы) (Процедура)
- [ПередНачаломРаботыСистемы](#передначаломработысистемы) (Процедура)
- [ПриНачалеРаботыСистемы](#приначалеработысистемы) (Процедура)
- [ПередЗавершениемРаботыСистемы](#передзавершениемработысистемы) (Процедура)
- [ОбработкаПолученияФормыВыбораПользователейСистемыВзаимодействия](#обработкаполученияформывыборапользователейсистемывзаимодействия) (Процедура)
- [ПредупреждениеПриЗавершенииРаботы](#предупреждениепризавершенииработы) (Функция)
- [ПараметрыРаботыКлиентаПриЗапуске](#параметрыработыклиентапризапуске) (Функция)
- [ПараметрыРаботыКлиента](#параметрыработыклиента) (Функция)
- [ПриПолученииСерверногоОповещения](#приполучениисерверногооповещения) (Процедура)
- [ЗапускПрограммыЗавершен](#запускпрограммызавершен) (Функция)
- [ПараметрКлиента](#параметрклиента) (Функция)
- [УстановитьПараметрКлиента](#установитьпараметрклиента) (Процедура)
- [ЗаполнитьПараметрыКлиента](#заполнитьпараметрыклиента) (Процедура)
- [ПоказатьПредупреждениеИПродолжить](#показатьпредупреждениеипродолжить) (Процедура)
- [ИмяИсполняемогоФайлаПриложения](#имяисполняемогофайлаприложения) (Функция)
- [УстановитьХранениеФормы](#установитьхранениеформы) (Процедура)
- [ЭтоЭлементДинамическогоСписка](#этоэлементдинамическогосписка) (Функция)
- [ОтключенаЛогикаНачалаРаботыСистемы](#отключеналогиканачалаработысистемы) (Функция)
- [ЭлементыСтиля](#элементыстиля) (Функция)
- [ОповещениеБезРезультата](#оповещениебезрезультата) (Функция)
- [ПередПериодическойОтправкойДанныхКлиентаНаСервер](#передпериодическойотправкойданныхклиентанасервер) (Процедура)
- [ПослеПериодическогоПолученияДанныхКлиентаНаСервере](#послепериодическогополученияданныхклиентанасервере) (Процедура)
- [РазвернутьУзлыДерева](#развернутьузлыдерева) (Процедура)
- [ОповеститьФормыОбИзменении](#оповеститьформыобизменении) (Процедура)
- [ПоказатьВСписке](#показатьвсписке) (Процедура)
- [ПоказатьПодробнуюИнформацию](#показатьподробнуюинформацию) (Процедура)
- [ИнформацияДляПоддержки](#информациядляподдержки) (Функция)
- [КаталогСистемныхПриложений](#каталогсистемныхприложений) (Функция)
- [НачатьВыполнениеОбработкиОповещения](#начатьвыполнениеобработкиоповещения) (Процедура)
- [ВыбратьОбъектыМетаданных](#выбратьобъектыметаданных) (Процедура)
- [ПоказатьРедакторТабличногоДокумента](#показатьредактортабличногодокумента) (Процедура)
- [ПараметрыРедактораТабличногоДокумента](#параметрыредакторатабличногодокумента) (Функция)
- [ПоказатьСравнениеТабличныхДокументов](#показатьсравнениетабличныхдокументов) (Процедура)
- [ПараметрыСравненияТабличныхДокументов](#параметрысравнениятабличныхдокументов) (Функция)
- [ДействияПередНачаломРаботыСистемыВИнтеграционнойПроцедуре](#действияпередначаломработысистемывинтеграционнойпроцедуре) (Процедура)
- [ДействияПередНачаломРаботыСистемыВМодуляхИнтеграционнойПроцедуры](#действияпередначаломработысистемывмодуляхинтеграционнойпроцедуры) (Процедура)
- [ДействияПередНачаломРаботыСистемыВМодуляхПереопределяемойПроцедуры](#действияпередначаломработысистемывмодуляхпереопределяемойпроцедуры) (Процедура)
- [ДействияПередНачаломРаботыСистемыОбработкаЗавершения](#действияпередначаломработысистемыобработказавершения) (Процедура)
- [ДействияПриНачалеРаботыСистемыВИнтеграционнойПроцедуре](#действияприначалеработысистемывинтеграционнойпроцедуре) (Процедура)
- [ДействияПриНачалеРаботыСистемыВМодуляхИнтеграционнойПроцедуры](#действияприначалеработысистемывмодуляхинтеграционнойпроцедуры) (Процедура)
- [ДействияПриНачалеРаботыСистемыВМодуляхПереопределяемойПроцедуры](#действияприначалеработысистемывмодуляхпереопределяемойпроцедуры) (Процедура)
- [ДействияПриНачалеРаботыСистемыОбработкаЗавершения](#действияприначалеработысистемыобработказавершения) (Процедура)
- [ПараметрыДействийПередЗавершениемРаботыСистемы](#параметрыдействийпередзавершениемработысистемы) (Функция)
- [ДействияПередЗавершениемРаботыСистемы](#действияпередзавершениемработысистемы) (Процедура)
- [ДействияПередЗавершениемРаботыСистемыОбработкаЗавершения](#действияпередзавершениемработысистемыобработказавершения) (Процедура)
- [ДействияПередЗавершениемРаботыСистемыПослеОбработкиОшибки](#действияпередзавершениемработысистемыпослеобработкиошибки) (Процедура)
- [ПередНачаломРаботыСистемы2](#передначаломработысистемы2) (Процедура)
- [ПроверитьВерсиюПлатформыПриЗапуске](#проверитьверсиюплатформыпризапуске) (Процедура)
- [ПослеЗакрытияФормыНерекомендуемойВерсииПлатформы](#послезакрытияформынерекомендуемойверсииплатформы) (Процедура)
- [ПредупредитьОНедопустимойВерсииПлатформы](#предупредитьонедопустимойверсииплатформы) (Процедура)
- [ПослеЗакрытияФормыНедопустимойВерсииПлатформы](#послезакрытияформынедопустимойверсииплатформы) (Процедура)
- [ПередНачаломРаботыСистемы3](#передначаломработысистемы3) (Процедура)
- [ПередНачаломРаботыСистемы4](#передначаломработысистемы4) (Процедура)
- [ИнтерактивнаяОбработкаВосстановленияСвязиСГлавнымУзлом](#интерактивнаяобработкавосстановлениясвязисглавнымузлом) (Процедура)
- [ИнтерактивнаяОбработкаНачальныхРегиональныхНастроекИБ](#интерактивнаяобработканачальныхрегиональныхнастроекиб) (Процедура)
- [ПослеЗакрытияФормыВосстановленияСвязиСГлавнымУзлом](#послезакрытияформывосстановлениясвязисглавнымузлом) (Процедура)
- [ПослеЗакрытияФормыВыбораНачальныхРегиональныхНастроекИБ](#послезакрытияформывыбораначальныхрегиональныхнастроекиб) (Процедура)
- [СкрытьРабочийСтолПриНачалеРаботыСистемы](#скрытьрабочийстолприначалеработысистемы) (Процедура)
- [ВыполнитьОповещениеСПустымРезультатом](#выполнитьоповещениеспустымрезультатом) (Процедура)
- [НачатьИнтерактивнуюОбработкуПередЗавершениемРаботыСистемы](#начатьинтерактивнуюобработкупередзавершениемработысистемы) (Процедура)
- [ПослеЗакрытияФормыПредупрежденийПриЗавершенииРаботы](#послезакрытияформыпредупрежденийпризавершенииработы) (Процедура)
- [ПослеНачалаРаботыСистемы](#посленачалаработысистемы) (Процедура)
- [ИдентификаторыОбъектовМетаданныхФормаСпискаСписокВыборЗначения](#идентификаторыобъектовметаданныхформаспискасписоквыборзначения) (Процедура)
- [ЗаполнитьПараметрыРаботыКлиентаНаСервере](#заполнитьпараметрыработыклиентанасервере) (Процедура)
- [НачатьВыполнениеОбработкиОповещенияЗавершение](#начатьвыполнениеобработкиоповещениязавершение) (Процедура)
- [ПолноеИмяПоследнейПроцедурыПередНачаломРаботыСистемы](#полноеимяпоследнейпроцедурыпередначаломработысистемы) (Функция)
- [НачатьИнтерактивнуюОбработкуПередНачаломРаботыСистемы](#начатьинтерактивнуюобработкупередначаломработысистемы) (Процедура)
- [ИнтерактивнаяОбработкаПредупрежденийПриЗавершенииРаботы](#интерактивнаяобработкапредупрежденийпризавершенииработы) (Процедура)
- [ПоказатьПредупреждениеИПродолжитьЗавершение](#показатьпредупреждениеипродолжитьзавершение) (Процедура)
- [ОповеститьОНехваткеПамяти](#оповеститьонехваткепамяти) (Процедура)
- [ОповещениеТекущегоПользователяОПредстоящемПерезапуске](#оповещениетекущегопользователяопредстоящемперезапуске) (Процедура)
- [ПодключениеОбработчиковОжиданияПоказаОповещенийИПерезапуска](#подключениеобработчиковожиданияпоказаоповещенийиперезапуска) (Процедура)
- [ОтключитьЗапланированныйПерезапуск](#отключитьзапланированныйперезапуск) (Процедура)

---

### <a name="установитьрасширенныйзаголовокприложения"></a>УстановитьРасширенныйЗаголовокПриложения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьРасширенныйЗаголовокПриложения(ПриЗапуске = Ложь) Экспорт
  ```
- **Описание:**

  Устанавливает заголовок главного окна приложения, используя значение константы
  ЗаголовокПриложения и заголовок приложения по умолчанию.

  Параметры:
    ПриЗапуске - Булево - Истина, если вызывается при начале работы программы.

---

### <a name="показатьвопроспользователю"></a>ПоказатьВопросПользователю

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьВопросПользователю(ОписаниеОповещенияОЗавершении, ТекстВопроса, Кнопки, ДополнительныеПараметры = Неопределено) Экспорт
  ```
- **Описание:**

  Показать форму вопроса.

  Параметры:
    ОписаниеОповещенияОЗавершении - ОписаниеОповещения - описание процедуры, которая будет вызвана после закрытия окна
                                                         вопроса со следующими параметрами:
                                                           РезультатВопроса - Структура:
                                                             Значение - результат выбора пользователя: значение
                                                                        системного перечисления или значение,
                                                                        связанное с нажатой кнопкой. В случае закрытия
                                                                        диалога по истечении времени - значение
                                                                        Таймаут.
                                                             БольшеНеЗадаватьЭтотВопрос - Булево - результат выбора
                                                                                                   пользователя в
                                                                                                   одноименном флажке.
                                                           ДополнительныеПараметры - Структура 
    ТекстВопроса                  - Строка, ФорматированнаяСтрока - текст задаваемого вопроса. 
    Кнопки                        - РежимДиалогаВопрос
                                  - СписокЗначений     - может быть задан список значений, в котором:
                                        Значение - содержит значение, связанное с кнопкой и возвращаемое при 
                                                   ее нажатии. В качестве значения может использоваться значение
                                                   перечисления КодВозвратаДиалога, а также другие значения,
                                                   поддерживающее XDTO-сериализацию.
                                        Представление - текст кнопки.

    ДополнительныеПараметры       - см. СтандартныеПодсистемыКлиент.ПараметрыВопросаПользователю.

---

### <a name="параметрывопросапользователю"></a>ПараметрыВопросаПользователю

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыВопросаПользователю() Экспорт
  ```
- **Описание:**

  Возвращает новую структуру дополнительных параметров для процедуры ПоказатьВопросПользователю.

  Возвращаемое значение:
   Структура:
     * КнопкаПоУмолчанию             - Произвольный - определяет кнопку по умолчанию по типу кнопки или по связанному
                                                      с ней значению.
     * Таймаут                       - Число        - интервал времени в секундах до автоматического закрытия окна
                                                      вопроса.
     * КнопкаТаймаута                - Произвольный - кнопка (по типу кнопки или по связанному с ней значению), 
                                                      на которой отображается количество секунд, оставшихся до
                                                      истечения таймаута.
     * Заголовок                     - Строка       - заголовок вопроса. 
     * ПредлагатьБольшеНеЗадаватьЭтотВопрос - Булево - если Истина, то в окне вопроса будет доступен одноименный флажок.
     * БольшеНеЗадаватьЭтотВопрос    - Булево       - принимает значение, выбранное пользователем в соответствующем
                                                      флажке.
     * БлокироватьВесьИнтерфейс      - Булево       - если Истина, форма вопроса открывается, блокируя работу всех
                                                      остальных открытых окон, включая главное окно.
     * Картинка                      - Картинка     - картинка, выводимая в окне вопроса.
     * ТекстФлажка                   - Строка       - текст флажка "Больше не спрашивать".

---

### <a name="открытьсписокактивныхпользователей"></a>ОткрытьСписокАктивныхПользователей

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОткрытьСписокАктивныхПользователей(ПараметрыФормы = Неопределено, ВладелецФормы = Неопределено) Экспорт
  ```
- **Описание:**

  Вызывается при необходимости открыть форму списка активных пользователей,
  которые в данный момент времени работают с системой.

  Параметры:
     ПараметрыФормы - Структура        - см. описание параметра Параметры метода ОткрытьФорму в синтакс-помощнике.
     ВладелецФормы  - ФормаКлиентскогоПриложения - см. описание параметра Владелец метода ОткрытьФорму в синтакс-помощнике.

---

### <a name="этобазоваяверсияконфигурации"></a>ЭтоБазоваяВерсияКонфигурации

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоБазоваяВерсияКонфигурации() Экспорт
  ```
- **Описание:**

  См. СтандартныеПодсистемыСервер.ЭтоБазоваяВерсияКонфигурации

---

### <a name="этоучебнаяплатформа"></a>ЭтоУчебнаяПлатформа

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоУчебнаяПлатформа() Экспорт
  ```
- **Описание:**

  См. СтандартныеПодсистемыСервер.ЭтоУчебнаяПлатформа

---

### <a name="вывестиинформациюобошибке"></a>ВывестиИнформациюОбОшибке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВывестиИнформациюОбОшибке(ИнформацияОбОшибке) Экспорт
  ```
- **Описание:**

  Вызывает метод ПоказатьИнформациюОбОшибке объекта ОбработкаОшибок.
  Требуется использовать для возможности автоматического тестирования,
  чтобы перехватывать ошибки с помощью расширения. 

  Параметры:
   ИнформацияОбОшибке - ИнформацияОбОшибке

  Пример:
  // Параметры:
  //  Результат - см. ДлительныеОперацииКлиент.НовыйРезультатДлительнойОперации
  //  ДополнительныеПараметры - Неопределено
  //
  &НаКлиенте
  Процедура ОбновитьТекущийСписокЗавершение(Результат, ДополнительныеПараметры) Экспорт
  	Если Результат = Неопределено Тогда
  		Возврат;
  	КонецЕсли;
  	Если Результат.Статус = "Ошибка" Тогда
  		СтандартныеПодсистемыКлиент.ВывестиИнформациюОбОшибке(Результат.ИнформацияОбОшибке);
  		Возврат;
  	КонецЕсли;
  	... // Статус = "Выполнено"
  КонецПроцедуры

---

### <a name="настроитьвидимостьизаголовокссылкиотправкиотчетаобошибке"></a>НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке(Элемент, ИнформацияОбОшибке, ОшибкаТребуетЗавершениеРаботы = Ложь) Экспорт
  ```
- **Описание:**

  Отключает видимость указанному элементу, если ошибка
  не подходит по категории с учетом параметра ОшибкаТребуетЗавершениеРаботы.
  Устанавливает в заголовок текст обязательной отправки,
  если администратор настроил Отправлять в объекте
  РежимОтправкиИнформацииОбОшибке, в противном случае
  (СпрашиватьПользователя или Авто) текст необязательной отправки.
  Рекомендуется вызывать ПриОткрытии своей формы ошибки.

  Параметры:
   Элемент - ПолеФормы, КнопкаФормы - элемент которому будет установлена видимость и заголовок.
   ИнформацияОбОшибке  - ИнформацияОбОшибке - ошибка по категории которой уточняется необходимость отправки отчета.
   ОшибкаТребуетЗавершениеРаботы - Булево - учитывается при настройке видимости ссылки. По ошибкам,
     из-за которых требуется завершение работы или перезапуск, как правило обязательно отправляется отчет.

  Пример:
  #Область ОписаниеПеременных
  &НаКлиенте
  Перем ОтчетОбОшибке, ОтчетИнформацияОбОшибке;
  #КонецОбласти
  #Область ОбработчикиСобытийФормы
  &НаКлиенте
  Процедура ПриОткрытии(Отказ)
  	Если Параметры.ИнформацияОбОшибке <> Неопределено Тогда
  		ОтчетИнформацияОбОшибке = Параметры.ИнформацияОбОшибке;
  		ОтчетОбОшибке = Новый ОтчетОбОшибке(ОтчетИнформацияОбОшибке);
  		СтандартныеПодсистемыКлиент.НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке(
  			Элементы.СформироватьОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  &НаКлиенте
  Процедура ПриЗакрытии(ЗавершениеРаботы)
  	Если ОтчетОбОшибке <> Неопределено Тогда
  		СтандартныеПодсистемыКлиент.ОтправитьОтчетОбОшибке(ОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  #КонецОбласти
  #Область ОбработчикиСобытийЭлементовШапкиФормы
  &НаКлиенте
  Процедура СформироватьОтчетОбОшибкеНажатие(Элемент)
  	СтандартныеПодсистемыКлиент.ПоказатьОтчетОбОшибке(ОтчетОбОшибке);
  КонецПроцедуры
  #КонецОбласти

---

### <a name="показатьотчетобошибке"></a>ПоказатьОтчетОбОшибке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьОтчетОбОшибке(ОтчетДляОтправки) Экспорт
  ```
- **Описание:**

  Открывает отчет об ошибке для проверки и настройки состава отчета,
  либо в режиме отправки (если указан адрес сервиса для отправки и администратор
  не настроил НеОтправлять в объекте РежимОтправкиИнформацииОбОшибке)
  либо в режиме записи в файл в противном случае.

  Параметры:
   ОтчетДляОтправки - ОтчетОбОшибке

  Пример:
  #Область ОписаниеПеременных
  &НаКлиенте
  Перем ОтчетОбОшибке, ОтчетИнформацияОбОшибке;
  #КонецОбласти
  #Область ОбработчикиСобытийФормы
  &НаКлиенте
  Процедура ПриОткрытии(Отказ)
  	Если Параметры.ИнформацияОбОшибке <> Неопределено Тогда
  		ОтчетИнформацияОбОшибке = Параметры.ИнформацияОбОшибке;
  		ОтчетОбОшибке = Новый ОтчетОбОшибке(ОтчетИнформацияОбОшибке);
  		СтандартныеПодсистемыКлиент.НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке(
  			Элементы.СформироватьОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  &НаКлиенте
  Процедура ПриЗакрытии(ЗавершениеРаботы)
  	Если ОтчетОбОшибке <> Неопределено Тогда
  		СтандартныеПодсистемыКлиент.ОтправитьОтчетОбОшибке(ОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  #КонецОбласти
  #Область ОбработчикиСобытийЭлементовШапкиФормы
  &НаКлиенте
  Процедура СформироватьОтчетОбОшибкеНажатие(Элемент)
  	СтандартныеПодсистемыКлиент.ПоказатьОтчетОбОшибке(ОтчетОбОшибке);
  КонецПроцедуры
  #КонецОбласти

---

### <a name="отправитьотчетобошибке"></a>ОтправитьОтчетОбОшибке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОтправитьОтчетОбОшибке(ОтчетДляОтправки, ИнформацияОбОшибке, ОшибкаТребуетЗавершениеРаботы = Ложь) Экспорт
  ```
- **Описание:**

  Неинтерактивно отправляет отчет об ошибке, если ошибка подходит
  по категории для отправки с учетом параметра ЗавершениеРаботы и
  администратор настроил Отправлять в объекте РежимОтправкиИнформацииОбОшибке.
  Рекомендуется вызывать ПриЗакрытии своей формы ошибки.

  Параметры:
   ОтчетДляОтправки - ОтчетОбОшибке
   ИнформацияОбОшибке  - ИнформацияОбОшибке - ошибка по категории которой уточняется необходимость отправки отчета.
   ОшибкаТребуетЗавершениеРаботы - Булево - учитывается при отправке отчета. По ошибкам,
     из-за которых требуется завершение работы или перезапуск, как правило обязательно отправляется отчет.
     Нужно передать то же значение, что и в процедуру НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке.

  Пример:
  #Область ОписаниеПеременных
  &НаКлиенте
  Перем ОтчетОбОшибке, ОтчетИнформацияОбОшибке;
  #КонецОбласти
  #Область ОбработчикиСобытийФормы
  &НаКлиенте
  Процедура ПриОткрытии(Отказ)
  	Если Параметры.ИнформацияОбОшибке <> Неопределено Тогда
  		ОтчетИнформацияОбОшибке = Параметры.ИнформацияОбОшибке;
  		ОтчетОбОшибке = Новый ОтчетОбОшибке(ОтчетИнформацияОбОшибке);
  		СтандартныеПодсистемыКлиент.НастроитьВидимостьИЗаголовокСсылкиОтправкиОтчетаОбОшибке(
  			Элементы.СформироватьОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  &НаКлиенте
  Процедура ПриЗакрытии(ЗавершениеРаботы)
  	Если ОтчетОбОшибке <> Неопределено Тогда
  		СтандартныеПодсистемыКлиент.ОтправитьОтчетОбОшибке(ОтчетОбОшибке, ОтчетИнформацияОбОшибке);
  	КонецЕсли;
  КонецПроцедуры
  #КонецОбласти
  #Область ОбработчикиСобытийЭлементовШапкиФормы
  &НаКлиенте
  Процедура СформироватьОтчетОбОшибкеНажатие(Элемент)
  	СтандартныеПодсистемыКлиент.ПоказатьОтчетОбОшибке(ОтчетОбОшибке);
  КонецПроцедуры
  #КонецОбласти

---

### <a name="пропуститьпредупреждениепередзавершениемработысистемы"></a>ПропуститьПредупреждениеПередЗавершениемРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПропуститьПредупреждениеПередЗавершениемРаботыСистемы() Экспорт
  ```
- **Описание:**

  Отключает выдачу предупреждения пользователю при завершении работы программы.

---

### <a name="передначаломработысистемы"></a>ПередНачаломРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередНачаломРаботыСистемы(Знач ОповещениеЗавершения = Неопределено) Экспорт
  ```
- **Описание:**

  Выполнить стандартные действия перед началом работы пользователя
  с областью данных, либо в локальном режиме работы.

  Предназначена для вызова из обработчика ПередНачаломРаботыСистемы модулей управляемого и обычного приложения.

  Параметры:
   ОповещениеЗавершения - ОписаниеОповещения - не задается при вызове из обработчика ПередНачаломРаботыСистемы 
                          модулей управляемого и обычного приложения. В других случаях, после запуска будет вызвано
                          оповещение с параметром типа Структура со свойствами:
                          > Отказ - Булево - Ложь, если запуск выполнен успешно, или Истина, если вход в программу не
                          выполнен;
                          > Перезапустить - Булево - если требуется перезапуск программы;
                          > ДополнительныеПараметрыКоманднойСтроки - Строка - для перезапуска.

---

### <a name="приначалеработысистемы"></a>ПриНачалеРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриНачалеРаботыСистемы(Знач ОповещениеЗавершения = Неопределено, НепрерывноеВыполнение = Истина) Экспорт
  ```
- **Описание:**

  Выполнить стандартные действия при начале работы пользователя
  с областью данных, либо в локальном режиме работы.

  Предназначена для вызова из обработчика ПриНачалеРаботыСистемы модулей управляемого и обычного приложения.

  Параметры:
   ОповещениеЗавершения - ОписаниеОповещения - не задается при вызове из обработчика ПриНачалеРаботыСистемы 
                          модулей управляемого и обычного приложения. В других случаях, после запуска будет вызвано
                          оповещение с параметром типа Структура со свойствами:
                          > Отказ - Булево - Ложь, если запуск выполнен успешно, или Истина, если вход в программу не
                          выполнен;
                          > Перезапустить - Булево - если требуется перезапуск программы;
                          > ДополнительныеПараметрыКоманднойСтроки - Строка - для перезапуска.

   НепрерывноеВыполнение - Булево - только для внутреннего использования.
                           Для перехода из обработчика ПередНачаломРаботыСистемы
                           выполненного в режиме интерактивной обработки.

---

### <a name="передзавершениемработысистемы"></a>ПередЗавершениемРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередЗавершениемРаботыСистемы(Отказ = Ложь, ТекстПредупреждения = "") Экспорт
  ```
- **Описание:**

  Выполнить стандартные действия перед завершением работы пользователя
  с областью данных, либо в локальном режиме работы.

  Предназначена для вызова из обработчика ПередЗавершениемРаботыСистемы модулей управляемого и обычного приложения.

  Параметры:
   Отказ                - Булево - возвращаемое значение. Признак отказа от завершения работы 
                          для обработчика события ПередЗавершениемРаботыСистемы, либо программного отказа,
                          либо того, что потребовалась интерактивная обработка. В случае успешного взаимодействия
                          с пользователем завершение работы будет продолжено.
   ТекстПредупреждения  - Строка - см. ПередЗавершениемРаботыСистемы() в синтакс-помощнике.

---

### <a name="обработкаполученияформывыборапользователейсистемывзаимодействия"></a>ОбработкаПолученияФормыВыбораПользователейСистемыВзаимодействия

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбработкаПолученияФормыВыбораПользователейСистемыВзаимодействия(НазначениеВыбора,
  			Форма, ИдентификаторОбсуждения, Параметры, ВыбраннаяФорма, СтандартнаяОбработка) Экспорт
  ```
- **Описание:**

  Выполнить стандартные действия при обработке получения формы выбора пользователей системы взаимодействия.

  Параметры:
   НазначениеВыбора - НазначениеВыбораПользователейСистемыВзаимодействия
   Форма - ФормаКлиентскогоПриложения
   ИдентификаторОбсуждения - ИдентификаторОбсужденияСистемыВзаимодействия
   Параметры - Структура
   ВыбраннаяФорма - Строка
   СтандартнаяОбработка - Булево

---

### <a name="предупреждениепризавершенииработы"></a>ПредупреждениеПриЗавершенииРаботы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредупреждениеПриЗавершенииРаботы() Экспорт
  ```
- **Описание:**

  Возвращает новую структуру параметров для вывода предупреждения перед завершением работы программы.
  Для использования в ОбщегоНазначенияКлиентПереопределяемый.ПередЗавершениемРаботыСистемы.

  Возвращаемое значение:
   Структура:
     ТекстПредупреждения  - Строка - текст, выводимый в окне веб-браузера (или тонкого клиента) при завершении работы.
                                     Например, "Имеются редактируемые файлы, которые не помещены в приложение".
                                     Остальные параметры определяют внешний вид формы предупреждений,
                                     открываемой после подтверждения в окне веб-браузера (или тонкого клиента).
     ТекстФлажка          - Строка - если задан, то в форме предупреждений выводится флажок с указанным текстом. 
                                     Например, "Завершить редактирование файлов (5)".
     ПоясняющийТекст      - Строка - текст, выводимый в форме сверху управляющего элемента (флажок или гиперссылка).
                                     Например, "Редактируемые файлы, не помещенные в приложение".
     ТекстГиперссылки     - Строка - если задан, то выводится гиперссылка с указанным текстом.
                                     Например, "Редактируемые файлы (5)".
     РасширеннаяПодсказка - Строка - текст подсказки, выводимый по кнопке справа от управляющего элемента (флажок или
                                     гиперссылка). Например, "Нажмите для перехода к списку файлов, 
                                     открытых для редактирования".
     Приоритет            - Число  - относительный порядок в списке предупреждений на форме (чем больше, тем выше).
     ВывестиОдноПредупреждение - Булево - если Истина, то в списке предупреждений выводится только одно это
                                          предупреждение. Т.е. такое предупреждение не совместимо с любыми другими.
     ДействиеПриУстановленномФлажке - Структура:
       * Форма          - Строка    - если пользователь установил флажок, то следует открыть указанную форму.
                                      Например, "Обработка.Файлы.РедактируемыеФайлы".
       * ПараметрыФормы - Структура - произвольная структура параметров для открытия формы. 
     ДействиеПриНажатииГиперссылки - Структура:
       * Форма          - Строка    - путь к форме, которая должна открываться по нажатию на гиперссылку.
                                      Например, "Обработка.Файлы.РедактируемыеФайлы".
       * ПараметрыФормы - Структура - произвольная структура параметров для открываемой формы.
       * ПрикладнаяФормаПредупреждения - Строка - путь к форме, которая должна открываться сразу
                                         вместо универсальной формы в случае, когда в списке 
                                         предупреждений оказывается только одно данное предупреждение.
                                         Например, "Обработка.Файлы.РедактируемыеФайлы".
       * ПараметрыПрикладнойФормыПредупреждения - Структура - произвольная структура
                                                  параметров для вышеописанной формы.
       * РежимОткрытияОкна - РежимОткрытияОкнаФормы - режим открытия форм Форма или ПрикладнаяФормаПредупреждения.

---

### <a name="параметрыработыклиентапризапуске"></a>ПараметрыРаботыКлиентаПриЗапуске

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыРаботыКлиентаПриЗапуске() Экспорт
  ```
- **Описание:**

  Возвращает значения параметров, необходимых для работы клиентского кода
  при запуске конфигурации за один серверный вызов (для минимизации клиент-серверного взаимодействия
  и снижения времени запуска). 
  С помощью этой функции можно обращаться к параметрам в клиентском коде, вызываемым из обработчиков событий:
  - ПередНачаломРаботыСистемы,
  - ПриНачалеРаботыСистемы.

  В этих обработчиках при запуске недопустимо использовать команды сброса кэша
  повторно используемых модулей, иначе запуск может привести
  к непредсказуемым ошибкам и лишним серверным вызовам.

  Возвращаемое значение:
    ФиксированнаяСтруктура - параметры работы клиента при запуске. 
                             См. в ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиентаПриЗапуске.

---

### <a name="параметрыработыклиента"></a>ПараметрыРаботыКлиента

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыРаботыКлиента() Экспорт
  ```
- **Описание:**

  Возвращает значения параметров, необходимых для работы клиентского кода конфигурации
  без дополнительных серверных вызовов.

  Возвращаемое значение:
    ФиксированнаяСтруктура - параметры работы клиента.
                             Состав свойств см. в ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиента.

---

### <a name="приполучениисерверногооповещения"></a>ПриПолученииСерверногоОповещения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриПолученииСерверногоОповещения(ИмяОповещения, Результат) Экспорт
  ```
- **Описание:**

  Вызывается при получении оповещений от сервера.
  Оповещения отправляются на сервере в процедурах ПриОтправкеСерверногоОповещения.
  Список оповещений смотри в ОбщегоНазначенияПереопределяемый.ПриДобавленииСерверныхОповещений.

  Параметры:
   ИмяОповещения - см. СерверныеОповещения.ОтправитьСерверноеОповещение.ИмяОповещения
   Результат     - см. СерверныеОповещения.ОтправитьСерверноеОповещение.Результат

  Пример:
  Если ИмяОповещения <> "СтандартныеПодсистемы.ЗавершениеРаботыПользователей.БлокировкаСеансов" Тогда
  	Возврат;
  КонецЕсли;
  Если РаботаПользователейЗавершается() Тогда
  	ЗавершитьРаботуПользователей(Результат);
  ИначеЕсли ВключенКонтрольЗавершенияРаботыПользователей() Тогда
  	КонтрольРежимаЗавершенияРаботыПользователей(Результат);
  КонецЕсли;

---

### <a name="запускпрограммызавершен"></a>ЗапускПрограммыЗавершен

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЗапускПрограммыЗавершен() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="параметрклиента"></a>ПараметрКлиента

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрКлиента(ИмяПараметра = Неопределено) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="установитьпараметрклиента"></a>УстановитьПараметрКлиента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьПараметрКлиента(ИмяПараметра, Значение) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="заполнитьпараметрыклиента"></a>ЗаполнитьПараметрыКлиента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьПараметрыКлиента(ПараметрыКлиента) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="показатьпредупреждениеипродолжить"></a>ПоказатьПредупреждениеИПродолжить

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьПредупреждениеИПродолжить(Параметры, ОписаниеПредупреждения) Экспорт
  ```
- **Описание:**

  После предупреждения вызывает процедуру с параметрами Результат, ДополнительныеПараметры.

  Параметры:
   Параметры           - Структура - которая содержит свойство:
                           ОбработкаПродолжения - ОписаниеОповещения - которое
                           содержит процедуру с двумя параметрами:
                             Результат, ДополнительныеПараметры.

   ОписаниеПредупреждения - Неопределено - предупреждение не требуется.
   ОписаниеПредупреждения - Строка - текст предупреждения, который нужно показать.
   ОписаниеПредупреждения - Структура:
        * ТекстПредупреждения - Строка - текст предупреждения, который нужно показать.
        * Кнопки              - СписокЗначений - для процедуры ПоказатьВопросПользователю.
        * ПараметрыВопроса    - Структура - содержит подмножество свойств,
                                  которые нужно переопределить, из числа
                                  возвращаемых функцией ПараметрыВопросаПользователю.

---

### <a name="имяисполняемогофайлаприложения"></a>ИмяИсполняемогоФайлаПриложения

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяИсполняемогоФайлаПриложения(ПолучитьИмяФайлаКонфигуратора = Ложь) Экспорт
  ```
- **Описание:**

  Возвращает имя исполняемого файла в зависимости от вида клиента.

  Возвращаемое значение:
   Строка

---

### <a name="установитьхранениеформы"></a>УстановитьХранениеФормы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьХранениеФормы(Форма, Хранение) Экспорт
  ```
- **Описание:**

  Устанавливает / отменяет хранение ссылки на управляемую форму в глобальной переменной.
  Требуется для случаев, когда ссылка на форму передается через ДополнительныеПараметры
  в объекте ОписаниеОповещения, который не блокирует освобождение закрытой формы.

---

### <a name="этоэлементдинамическогосписка"></a>ЭтоЭлементДинамическогоСписка

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоЭлементДинамическогоСписка(ТаблицаИлиТекущиеДанные) Экспорт
  ```
- **Описание:**

  Проверяет, что текущие данные определены и не являются группировкой.
  Предназначена для обработчиков таблиц формы динамических списков.

  Параметры:
   ТаблицаИлиТекущиеДанные - ТаблицаФормы - таблица формы динамического списка для проверки текущих данных.
                           - Неопределено
                           - ДанныеФормыСтруктура
                           - Структура - текущие данные для проверки.

  Возвращаемое значение:
   Булево

---

### <a name="отключеналогиканачалаработысистемы"></a>ОтключенаЛогикаНачалаРаботыСистемы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОтключенаЛогикаНачалаРаботыСистемы() Экспорт
  ```
- **Описание:**

  Проверяет, выполнено ли опасное отключение процедур запуска для целей автоматического тестирования.

  Возвращаемое значение:
   Булево

---

### <a name="элементыстиля"></a>ЭлементыСтиля

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭлементыСтиля() Экспорт
  ```
- **Описание:**

  Возвращает элементы стиля конфигурации.

  Возвращаемое значение:
   Структура:
    * Ключ - Строка - имя элемента стиля, например, "ГиперссылкаЦвет".
    * Значение - ОбъектМетаданныхЭлементСтиля.

---

### <a name="оповещениебезрезультата"></a>ОповещениеБезРезультата

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОповещениеБезРезультата(ОповещениеСРезультатом) Экспорт
  ```
- **Описание:**

  Переадресует оповещение без результата на оповещение с результатом.

  Возвращаемое значение:
   ОписаниеОповещения

---

### <a name="передпериодическойотправкойданныхклиентанасервер"></a>ПередПериодическойОтправкойДанныхКлиентаНаСервер

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередПериодическойОтправкойДанныхКлиентаНаСервер(Параметры) Экспорт
  ```
- **Описание:**

  См. ИнтеграцияПодсистемБСПКлиент.ПередПериодическойОтправкойДанныхКлиентаНаСервер

---

### <a name="послепериодическогополученияданныхклиентанасервере"></a>ПослеПериодическогоПолученияДанныхКлиентаНаСервере

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеПериодическогоПолученияДанныхКлиентаНаСервере(Результаты) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияКлиентПереопределяемый.ПослеПериодическогоПолученияДанныхКлиентаНаСервере

---

### <a name="развернутьузлыдерева"></a>РазвернутьУзлыДерева

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура РазвернутьУзлыДерева(Форма, ИмяЭлементаФормы, ИдентификаторСтрокиДерева = "*", РазвернутьСПодчиненными = Ложь) Экспорт
  ```
- **Описание:**

  Разворачивает узлы указанного дерева на форме.

  Параметры:
    Форма                     - ФормаКлиентскогоПриложения - форма, на которой размещен элемент управления с деревом значений.
    ИмяЭлементаФормы          - Строка           - имя элемента с таблицей формы (деревом значений) и связанного с ней
                                                   реквизита формы (должны совпадать).
    ИдентификаторСтрокиДерева - Произвольный     - идентификатор строки дерева, которую требуется развернуть.
                                                   Если указано "*", то будут развернуты все узлы верхнего уровня.
                                                   Если указано Неопределено, то строки дерева развернуты не будут.
                                                   Значение по умолчанию: "*".
    РазвернутьСПодчиненными   - Булево           - если Истина, то следует раскрыть также и все подчиненные узлы.
                                                   По умолчанию Ложь.

---

### <a name="оповеститьформыобизменении"></a>ОповеститьФормыОбИзменении

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОповеститьФормыОбИзменении(ТипыИзмененныхОбъектов, ПараметрОповещенияФорм = Неопределено) Экспорт
  ```
- **Описание:**

  Оповещает открытые форм и динамические списки о массовых изменениях объектов различных типов
  с помощью методов глобального контекста Оповестить и ОповеститьОбИзменении.

  Параметры:
   ТипыИзмененныхОбъектов - см. СтандартныеПодсистемыСервер.ПодготовитьОповещениеФормОбИзменении
   ПараметрОповещенияФорм - Произвольный - параметр сообщения для метода Оповестить.

---

### <a name="показатьвсписке"></a>ПоказатьВСписке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьВСписке(Ссылка, ИмяФормыСписка, ПараметрыФормы = Неопределено) Экспорт
  ```
- **Описание:**

  Открывает форму списка объекта с позиционированием на объекте.

  Параметры:
    Ссылка - ЛюбаяСсылка - объект, который необходимо показать в списке.
    ИмяФормыСписка - Строка - имя формы списка.
        Если передать Неопределено то будет определена автоматически (потребуется вызов сервера).
    ПараметрыФормы - Структура - дополнительные параметры открытия формы списка.

---

### <a name="показатьподробнуюинформацию"></a>ПоказатьПодробнуюИнформацию

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьПодробнуюИнформацию(Обработчик, Текст, Заголовок = Неопределено) Экспорт
  ```
- **Описание:**

  Выводит текст, который пользователь может скопировать.

  Параметры:
    Обработчик - ОписаниеОповещения - описание процедуры, которая будет вызвана после завершения показа.
        Возвращаемое значение аналогично ПоказатьВопросПользователю().
    Текст     - Строка - текст информации.
    Заголовок - Строка - заголовок окна. По умолчанию "Подробнее".

---

### <a name="информациядляподдержки"></a>ИнформацияДляПоддержки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИнформацияДляПоддержки() Экспорт
  ```
- **Описание:**

  Шапка файла для технической поддержки.

  Возвращаемое значение:
   Строка

---

### <a name="каталогсистемныхприложений"></a>КаталогСистемныхПриложений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогСистемныхПриложений() Экспорт
  ```
- **Описание:**

  Каталог системных приложений, например, "C:\Windows\System32".
  Используется только в ОС Windows.

  Возвращаемое значение:
   Строка

---

### <a name="начатьвыполнениеобработкиоповещения"></a>НачатьВыполнениеОбработкиОповещения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура НачатьВыполнениеОбработкиОповещения(ОписаниеОповещения, Результат = Неопределено) Экспорт
  ```
- **Описание:**

  Асинхронный аналог метода платформы ВыполнитьОбработкуОповещения.
  Выполняет обработку оповещения асинхронно, подобно методам платформы, например НачатьПолучениеФайла.
  Используется, когда обработку оповещения нужно выполнить после синхронного завершения вызова,
  то есть с некоторой минимальной задержкой.

  Параметры:
   ОписаниеОповещения - ОписаниеОповещения - оповещение, которое нужно выполнить.
   Результат  - Произвольный - значение, которое передается в параметр Результат
                метода платформы ВыполнитьОбработкуОповещения.

---

### <a name="выбратьобъектыметаданных"></a>ВыбратьОбъектыМетаданных

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыбратьОбъектыМетаданных(ПараметрыФормы, ОписаниеОповещенияОЗакрытии = Неопределено) Экспорт
  ```
- **Описание:**

  Открыть выбор объектов метаданных.

  Параметры:
   ПараметрыФормы - см. СтандартныеПодсистемыКлиентСервер.ПараметрыВыбораОбъектовМетаданных
   ОписаниеОповещенияОЗакрытии - ОписаниеОповещения - оповещение, вызываемое при закрытии формы с параметрами: 
  		# ВыбранныеОбъектыМетаданных - СписокЗначений - полные имена выбранных объектов метаданных,
  			или ссылки на идентификаторы объектов метаданных, если параметр ВыбиратьСсылки установлен в Истина. 
  		# ДополнительныеПараметры - Произвольный - дополнительные параметры, переданные при создании оповещения. 
  	Если ОписаниеОповещенияОЗакрытии не указано, то будет вызвано оповещение, которое можно получить 
  	с помощью обработчика ОбработкаОповещения:
  		# ИмяСобытия - Строка - "ВыборОбъектовМетаданных"
  		# Параметр - СписокЗначений - выбранные объекты метаданных.
  		# Источник - см. СтандартныеПодсистемыКлиентСервер.ПараметрыВыбораОбъектовМетаданных.УникальныйИдентификаторИсточник

---

### <a name="показатьредактортабличногодокумента"></a>ПоказатьРедакторТабличногоДокумента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьРедакторТабличногоДокумента(Знач ТабличныйДокумент, Знач ПараметрыФормы = Неопределено, 
  	Знач ОписаниеОповещенияОЗакрытии = Неопределено, Владелец = Неопределено) Экспорт
  ```
- **Описание:**

  Открыть табличный документ на просмотр или редактирование.
  При записи отредактированного табличного документа вызывается оповещение, которое можно получить 
  с помощью обработчика ОбработкаОповещения:
  # ИмяСобытия - Строка - "Запись_ТабличныйДокумент" или "ОтменаРедактированияТабличногоДокумента"
  # Параметр - Структура:
    ## ПутьКФайлу - Строка - полный путь к файлу отредактированного табличного документа.
    ## Представление - Строка - имя документа, заданное в ПараметрыРедактораТабличногоДокумента.ИмяДокумента
  # Источник - ФормаКлиентскогоПриложения - форма редактора табличного документа.

  Параметры:
   ТабличныйДокумент - ТабличныйДокумент - отображаемый или редактируемый табличный документ.
   ПараметрыФормы - см. СтандартныеПодсистемыКлиент.ПараметрыРедактораТабличногоДокумента

---

### <a name="параметрыредакторатабличногодокумента"></a>ПараметрыРедактораТабличногоДокумента

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыРедактораТабличногоДокумента() Экспорт
  ```
- **Описание:**

  Параметры редактора табличного документа для СтандартныеПодсистемыКлиент.ПоказатьРедакторТабличногоДокумента.

  Возвращаемое значение:
   Структура:
    * ИмяДокумента - Строка - наименование табличного документа, выводимое в заголовке редактора. 
    * ТабличныйДокумент - ТабличныйДокумент, Строка - отображаемый или редактируемый табличный документ.
                          Также может быть указан адрес табличного документа во временном хранилище.
    * ПутьКФайлу - Строка - полный путь к файлу табличного документа, опционально.
    * Редактирование - Булево - если Истина, то табличный документ можно отредактировать. По умолчанию Ложь.

---

### <a name="показатьсравнениетабличныхдокументов"></a>ПоказатьСравнениеТабличныхДокументов

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьСравнениеТабличныхДокументов(ТабличныйДокументЛевый, ТабличныйДокументПравый, Параметры) Экспорт
  ```
- **Описание:**

  Отрыть форму сравнения табличных документов, в которой можно посмотреть их отличия.

  Параметры:
   ТабличныйДокументЛевый - ТабличныйДокумент - первый сравниваемый табличный документ.
   ТабличныйДокументПравый - ТабличныйДокумент - второй сравниваемый табличный документ.
   Параметры - см. ПараметрыСравненияТабличныхДокументов

---

### <a name="параметрысравнениятабличныхдокументов"></a>ПараметрыСравненияТабличныхДокументов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыСравненияТабличныхДокументов() Экспорт
  ```
- **Описание:**

  Параметры сравнения табличных документов для процедуры ПоказатьСравнениеТабличныхДокументов.

  Возвращаемое значение:
   Структура:
     * АдресТабличныхДокументов - Строка - адрес сравниваемых табличных документов во временном хранилище.
     * Заголовок - Строка - заголовок формы. Если не задано, то "Сравнение табличных документов".
     * ЗаголовокЛевый - Строка - заголовок первого табличного документа (слева).
     * ЗаголовокПравый - Строка - заголовок правого табличного документа (справа).

---

### <a name="действияпередначаломработысистемывинтеграционнойпроцедуре"></a>ДействияПередНачаломРаботыСистемыВИнтеграционнойПроцедуре

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередНачаломРаботыСистемыВИнтеграционнойПроцедуре(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы.

---

### <a name="действияпередначаломработысистемывмодуляхинтеграционнойпроцедуры"></a>ДействияПередНачаломРаботыСистемыВМодуляхИнтеграционнойПроцедуры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередНачаломРаботыСистемыВМодуляхИнтеграционнойПроцедуры(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы.

---

### <a name="действияпередначаломработысистемывмодуляхпереопределяемойпроцедуры"></a>ДействияПередНачаломРаботыСистемыВМодуляхПереопределяемойПроцедуры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередНачаломРаботыСистемыВМодуляхПереопределяемойПроцедуры(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы.

---

### <a name="действияпередначаломработысистемыобработказавершения"></a>ДействияПередНачаломРаботыСистемыОбработкаЗавершения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередНачаломРаботыСистемыОбработкаЗавершения(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Завершение процедуры ПередНачаломРаботыСистемы.

---

### <a name="действияприначалеработысистемывинтеграционнойпроцедуре"></a>ДействияПриНачалеРаботыСистемыВИнтеграционнойПроцедуре

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПриНачалеРаботыСистемыВИнтеграционнойПроцедуре(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПриНачалеРаботыСистемы.

---

### <a name="действияприначалеработысистемывмодуляхинтеграционнойпроцедуры"></a>ДействияПриНачалеРаботыСистемыВМодуляхИнтеграционнойПроцедуры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПриНачалеРаботыСистемыВМодуляхИнтеграционнойПроцедуры(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПриНачалеРаботыСистемы.

---

### <a name="действияприначалеработысистемывмодуляхпереопределяемойпроцедуры"></a>ДействияПриНачалеРаботыСистемыВМодуляхПереопределяемойПроцедуры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПриНачалеРаботыСистемыВМодуляхПереопределяемойПроцедуры(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПриНачалеРаботыСистемы.

---

### <a name="действияприначалеработысистемыобработказавершения"></a>ДействияПриНачалеРаботыСистемыОбработкаЗавершения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПриНачалеРаботыСистемыОбработкаЗавершения(Неопределен, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Завершение процедуры ПриНачалеРаботыСистемы.

---

### <a name="параметрыдействийпередзавершениемработысистемы"></a>ПараметрыДействийПередЗавершениемРаботыСистемы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыДействийПередЗавершениемРаботыСистемы(Пересоздать = Ложь) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. 

  Параметры:
   Пересоздать - Булево

  Возвращаемое значение:
    Структура:
      Отказ - Булево
      Предупреждения - Массив из см. СтандартныеПодсистемыКлиент.ПредупреждениеПриЗавершенииРаботы.
      ИнтерактивнаяОбработка - ОписаниеОповещения, Неопределено
      ОбработкаПродолжения - ОписаниеОповещения, Неопределено
      НепрерывноеВыполнение - Булево
      ОбработкаЗавершения - ОписаниеОповещения

---

### <a name="действияпередзавершениемработысистемы"></a>ДействияПередЗавершениемРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередЗавершениемРаботыСистемы(Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередЗавершениемРаботыСистемы.

  Параметры:
    Параметры - см. СтандартныеПодсистемыКлиент.ПараметрыДействийПередЗавершениемРаботыСистемы

---

### <a name="действияпередзавершениемработысистемыобработказавершения"></a>ДействияПередЗавершениемРаботыСистемыОбработкаЗавершения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередЗавершениемРаботыСистемыОбработкаЗавершения(Неопределен, Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Завершение процедуры ПередЗавершениемРаботыСистемы.

  Параметры:
    Неопределен - Неопределено
    Параметры - см. СтандартныеПодсистемыКлиент.ПараметрыДействийПередЗавершениемРаботыСистемы

---

### <a name="действияпередзавершениемработысистемыпослеобработкиошибки"></a>ДействияПередЗавершениемРаботыСистемыПослеОбработкиОшибки

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДействияПередЗавершениемРаботыСистемыПослеОбработкиОшибки(Неопределен, ОбработкаПродолжения) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Завершение процедуры ПередЗавершениемРаботыСистемы.

  Параметры:
   Неопределен - Неопределено
   ОбработкаПродолжения - ОписаниеОповещения

---

### <a name="передначаломработысистемы2"></a>ПередНачаломРаботыСистемы2

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередНачаломРаботыСистемы2(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияКлиентПереопределяемый.ПередНачаломРаботыСистемы.

---

### <a name="проверитьверсиюплатформыпризапуске"></a>ПроверитьВерсиюПлатформыПриЗапуске

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверитьВерсиюПлатформыПриЗапуске(Параметры, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы2.

---

### <a name="послезакрытияформынерекомендуемойверсииплатформы"></a>ПослеЗакрытияФормыНерекомендуемойВерсииПлатформы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеЗакрытияФормыНерекомендуемойВерсииПлатформы(Результат, Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПроверитьВерсиюПлатформыПриЗапуске.

---

### <a name="предупредитьонедопустимойверсииплатформы"></a>ПредупредитьОНедопустимойВерсииПлатформы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПредупредитьОНедопустимойВерсииПлатформы(Параметры, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы2.

---

### <a name="послезакрытияформынедопустимойверсииплатформы"></a>ПослеЗакрытияФормыНедопустимойВерсииПлатформы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеЗакрытияФормыНедопустимойВерсииПлатформы(Результат, Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПроверитьВерсиюПлатформыПриЗапуске.

---

### <a name="передначаломработысистемы3"></a>ПередНачаломРаботыСистемы3

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередНачаломРаботыСистемы3(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияКлиентПереопределяемый.ПередНачаломРаботыСистемы.

---

### <a name="передначаломработысистемы4"></a>ПередНачаломРаботыСистемы4

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПередНачаломРаботыСистемы4(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияКлиентПереопределяемый.ПередНачаломРаботыСистемы.

---

### <a name="интерактивнаяобработкавосстановлениясвязисглавнымузлом"></a>ИнтерактивнаяОбработкаВосстановленияСвязиСГлавнымУзлом

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ИнтерактивнаяОбработкаВосстановленияСвязиСГлавнымУзлом(Параметры, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПроверитьНеобходимостьВосстановленияСвязиСГлавнымУзлом.

---

### <a name="интерактивнаяобработканачальныхрегиональныхнастроекиб"></a>ИнтерактивнаяОбработкаНачальныхРегиональныхНастроекИБ

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ИнтерактивнаяОбработкаНачальныхРегиональныхНастроекИБ(Параметры, Контекст) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы4.

---

### <a name="послезакрытияформывосстановлениясвязисглавнымузлом"></a>ПослеЗакрытияФормыВосстановленияСвязиСГлавнымУзлом

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеЗакрытияФормыВосстановленияСвязиСГлавнымУзлом(Результат, Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПроверитьНеобходимостьВосстановленияСвязиСГлавнымУзлом.

---

### <a name="послезакрытияформывыбораначальныхрегиональныхнастроекиб"></a>ПослеЗакрытияФормыВыбораНачальныхРегиональныхНастроекИБ

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеЗакрытияФормыВыбораНачальныхРегиональныхНастроекИБ(Результат, Параметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования. Продолжение процедуры ПередНачаломРаботыСистемы4.

---

### <a name="скрытьрабочийстолприначалеработысистемы"></a>СкрытьРабочийСтолПриНачалеРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СкрытьРабочийСтолПриНачалеРаботыСистемы(Скрыть = Истина, УжеВыполненоНаСервере = Ложь) Экспорт
  ```
- **Описание:**

  Устанавливает флаг скрытия рабочего стола при начале работы системы,
  который блокирует создание форм на рабочем столе.
  Снимает флаг скрытия и обновляет рабочий стол, когда это станет возможным,
  если скрытие выполнялось.

  Параметры:
   Скрыть - Булево - если передать Ложь, тогда при условии скрытия рабочего
            стола он будет вновь показан.

   УжеВыполненоНаСервере - Булево - если передать Истина, тогда уже был вызван
            метод в модуле СтандартныеПодсистемыВызовСервера, и его не требуется
            вызвать, а требуется только установить на клиенте, что рабочий стол
            был скрыт и позднее его требуется показать.

---

### <a name="выполнитьоповещениеспустымрезультатом"></a>ВыполнитьОповещениеСПустымРезультатом

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыполнитьОповещениеСПустымРезультатом(ОповещениеСРезультатом) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="начатьинтерактивнуюобработкупередзавершениемработысистемы"></a>НачатьИнтерактивнуюОбработкуПередЗавершениемРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура НачатьИнтерактивнуюОбработкуПередЗавершениемРаботыСистемы() Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="послезакрытияформыпредупрежденийпризавершенииработы"></a>ПослеЗакрытияФормыПредупрежденийПриЗавершенииРаботы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеЗакрытияФормыПредупрежденийПриЗавершенииРаботы(Результат, ДополнительныеПараметры) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

  Параметры:
   Результат - КодВозвратаДиалога 
             - Неопределено
   ДополнительныеПараметры - Структура

---

### <a name="посленачалаработысистемы"></a>ПослеНачалаРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеНачалаРаботыСистемы() Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияКлиентПереопределяемый.ПослеНачалаРаботыСистемы.

---

### <a name="идентификаторыобъектовметаданныхформаспискасписоквыборзначения"></a>ИдентификаторыОбъектовМетаданныхФормаСпискаСписокВыборЗначения

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ИдентификаторыОбъектовМетаданныхФормаСпискаСписокВыборЗначения(Форма, Элемент, Значение, СтандартнаяОбработка) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="заполнитьпараметрыработыклиентанасервере"></a>ЗаполнитьПараметрыРаботыКлиентаНаСервере

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьПараметрыРаботыКлиентаНаСервере(Параметры) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="начатьвыполнениеобработкиоповещениязавершение"></a>НачатьВыполнениеОбработкиОповещенияЗавершение

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура НачатьВыполнениеОбработкиОповещенияЗавершение(Размер, Контекст) Экспорт
  ```
- **Описание:**

  Продолжение процедуры НачатьВыполнениеОбработкиОповещения.

  Параметры:
   Размер - Число
   Контекст - Структура:
    * Оповещение - ОписаниеОповещения
    * Результат  - Произвольный

---

### <a name="полноеимяпоследнейпроцедурыпередначаломработысистемы"></a>ПолноеИмяПоследнейПроцедурыПередНачаломРаботыСистемы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолноеИмяПоследнейПроцедурыПередНачаломРаботыСистемы() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="начатьинтерактивнуюобработкупередначаломработысистемы"></a>НачатьИнтерактивнуюОбработкуПередНачаломРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура НачатьИнтерактивнуюОбработкуПередНачаломРаботыСистемы() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="интерактивнаяобработкапредупрежденийпризавершенииработы"></a>ИнтерактивнаяОбработкаПредупрежденийПриЗавершенииРаботы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ИнтерактивнаяОбработкаПредупрежденийПриЗавершенииРаботы(Параметры, ПараметрыОткрытияФормы) Экспорт
  ```
- **Описание:**

  Продолжение процедуры ОткрытьФормуПредупрежденийПриЗавершенииРаботы.

---

### <a name="показатьпредупреждениеипродолжитьзавершение"></a>ПоказатьПредупреждениеИПродолжитьЗавершение

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьПредупреждениеИПродолжитьЗавершение(Результат, Параметры) Экспорт
  ```
- **Описание:**

  Продолжение процедуры ПоказатьПредупреждениеИПродолжить.

---

### <a name="оповеститьонехваткепамяти"></a>ОповеститьОНехваткеПамяти

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОповеститьОНехваткеПамяти() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="оповещениетекущегопользователяопредстоящемперезапуске"></a>ОповещениеТекущегоПользователяОПредстоящемПерезапуске

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОповещениеТекущегоПользователяОПредстоящемПерезапуске(СекундДоПерезапуска) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="подключениеобработчиковожиданияпоказаоповещенийиперезапуска"></a>ПодключениеОбработчиковОжиданияПоказаОповещенийИПерезапуска

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПодключениеОбработчиковОжиданияПоказаОповещенийИПерезапуска(СекундДоПерезапуска) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="отключитьзапланированныйперезапуск"></a>ОтключитьЗапланированныйПерезапуск

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОтключитьЗапланированныйПерезапуск() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

