# Модуль ЗадачиПроцессовКлиентСерверПовтИсп

**Описание подсистемы:** Задачи процессов клиент сервер повт исп

Всего найдено экспортных методов: **10**

## Список методов

- [СоответствиеСпецСимволов](#соответствиеспецсимволов) (Функция)
- [ОбратноеСоответствиеСпецСимволов](#обратноесоответствиеспецсимволов) (Функция)
- [ДопустимыеВURIСпецСимволы](#допустимыевuriспецсимволы) (Функция)
- [ЗначимыеВURIСпецСимволы](#значимыевuriспецсимволы) (Функция)
- [СписокОграничителейURI](#списокограничителейuri) (Функция)
- [СписокДопустимыхВСсылкеТегов](#списокдопустимыхвссылкетегов) (Функция)
- [СписокТомовДисков](#списоктомовдисков) (Функция)
- [СоответствиеОткрывающихСкобок](#соответствиеоткрывающихскобок) (Функция)
- [СписокЗакрывающихСкобок](#списокзакрывающихскобок) (Функция)
- [СписокОткрывающихСкобок](#списокоткрывающихскобок) (Функция)

---

### <a name="соответствиеспецсимволов"></a>СоответствиеСпецСимволов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СоответствиеСпецСимволов() Экспорт
  ```
- **Описание:**

  Возвращает соответствие для замены спецсимволов

  Возвращаемое значение:
    Соответствие   - содержит специальные символы

---

### <a name="обратноесоответствиеспецсимволов"></a>ОбратноеСоответствиеСпецСимволов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОбратноеСоответствиеСпецСимволов() Экспорт
  ```
- **Описание:**

  Возвращает соответствие для обратной замены спецсимволов

  Возвращаемое значение:
    Соответствие   - содержит обратное соответствие специальных символов

---

### <a name="допустимыевuriспецсимволы"></a>ДопустимыеВURIСпецСимволы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДопустимыеВURIСпецСимволы() Экспорт
  ```
- **Описание:**

  Возвращает список допустимых в URI спецсимволов

  Возвращаемое значение:
    СписокЗначений   - содержит список допустимых в ссылке специальных символов.

---

### <a name="значимыевuriспецсимволы"></a>ЗначимыеВURIСпецСимволы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЗначимыеВURIСпецСимволы() Экспорт
  ```
- **Описание:**

  Возвращает список значимых в URI спецсимволов

  Возвращаемое значение:
    СписокЗначений   - содержит список значимых в ссылке специальных символов.

---

### <a name="списокограничителейuri"></a>СписокОграничителейURI

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокОграничителейURI() Экспорт
  ```
- **Описание:**

  Возвращает список ограничителей, используемых в URI

  Возвращаемое значение:
    СписокЗначений   - содержит список символов-ограничителей в ссылке.

---

### <a name="списокдопустимыхвссылкетегов"></a>СписокДопустимыхВСсылкеТегов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокДопустимыхВСсылкеТегов() Экспорт
  ```
- **Описание:**

  Список ограничителей HTML, которые могут стоять перед ссылкой
  > - закрытие какого-либо тега перед ссылкой

  Возвращаемое значение:
    СписокЗначений   - содержит список допустимых в ссылке тегов.

---

### <a name="списоктомовдисков"></a>СписокТомовДисков

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокТомовДисков() Экспорт
  ```
- **Описание:**

  Список томов дисков

  Возвращаемое значение:
    СписокЗначений   - содержит список допустимых томов диска.

---

### <a name="соответствиеоткрывающихскобок"></a>СоответствиеОткрывающихСкобок

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СоответствиеОткрывающихСкобок() Экспорт
  ```
- **Описание:**

  Возвращает соответствие открывающих скобок

  Возвращаемое значение:
    Соответствие   - содержит соответствие открывающихся и закрывающихся скобок.

---

### <a name="списокзакрывающихскобок"></a>СписокЗакрывающихСкобок

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокЗакрывающихСкобок() Экспорт
  ```
- **Описание:**

  Возвращает список закрывающих скобок

  Возвращаемое значение:
    Список   - содержит список открывающихся скобок.

---

### <a name="списокоткрывающихскобок"></a>СписокОткрывающихСкобок

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокОткрывающихСкобок() Экспорт
  ```
- **Описание:**

  Возвращает список открывающих скобок
  Возвращает список закрывающих скобок

  Возвращаемое значение:
    Список   - содержит список закрывающихся скобок.

---

