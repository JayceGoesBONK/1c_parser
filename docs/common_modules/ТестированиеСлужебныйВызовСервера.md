# Модуль ТестированиеСлужебныйВызовСервера

**Описание подсистемы:** Тестирование служебный вызов сервера

Всего найдено экспортных методов: **24**

## Список методов

- [ИмяПрофиляTestClient](#имяпрофиляtestclient) (Функция)
- [ПодключитьВнешнююОбработкуСервер](#подключитьвнешнююобработкусервер) (Функция)
- [ДанныеСценарияИзРепозитория](#данныесценарияизрепозитория) (Функция)
- [ДанныеИерархииПоДаннымGit](#данныеиерархииподаннымgit) (Функция)
- [ДанныеНастройкиСценарияИзРепозитория](#данныенастройкисценарияизрепозитория) (Функция)
- [ИмяВеткиСтрокой](#имяветкистрокой) (Функция)
- [ИмяВеткиВРепозитории](#имяветкиврепозитории) (Функция)
- [ИмяПроектаСтрокой](#имяпроектастрокой) (Функция)
- [ПараметрыДляПолученияСценариев](#параметрыдляполучениясценариев) (Функция)
- [ФайлыСценарияИзРепозитория](#файлысценарияизрепозитория) (Функция)
- [СлужебныеДанныеСценариев](#служебныеданныесценариев) (Функция)
- [ДанныеНастроекСценарияИзРепозитория](#данныенастроексценарияизрепозитория) (Функция)
- [ДанныеДляВыгрузки](#данныедлявыгрузки) (Функция)
- [ДанныеЭталонныхБД](#данныеэталонныхбд) (Функция)
- [ТокенПроекта](#токенпроекта) (Функция)
- [ПараметрыСценарияИзТекстовыхДанных](#параметрысценарияизтекстовыхданных) (Функция)
- [ТегиСценарияИзТекстовыхДанных](#тегисценарияизтекстовыхданных) (Функция)
- [ВложенныеСценарииТекстовыхДанных](#вложенныесценариитекстовыхданных) (Функция)
- [ВызывающиеСценарииТекстовыхДанных](#вызывающиесценариитекстовыхданных) (Функция)
- [КаталогДляДанныхТестирования](#каталогдляданныхтестирования) (Функция)
- [ПроверкаИсправленияОшибокВВетке](#проверкаисправленияошибоквветке) (Функция)
- [ДанныеРасшифровкиОтчетовТестирования](#данныерасшифровкиотчетовтестирования) (Функция)
- [РасшифроватьОшибки](#расшифроватьошибки) (Функция)
- [ОбъединитьЗаголовкиВТабличномДокументе](#объединитьзаголовкивтабличномдокументе) (Процедура)

---

### <a name="имяпрофиляtestclient"></a>ИмяПрофиляTestClient

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяПрофиляTestClient(Роль,НастройкиБДДляЗапускаТестовИзСценария,ЭкранироватьСпецСимволы) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="подключитьвнешнююобработкусервер"></a>ПодключитьВнешнююОбработкуСервер

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПодключитьВнешнююОбработкуСервер(АдресХранилища) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="данныесценарияизрепозитория"></a>ДанныеСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеСценарияИзРепозитория(Проект, ДанныеСценария, ТокенПользователя,  Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария работы пользователя из репозитория
  Параметры:
   Проект - Справочники.Проекты
   ДанныеСценария - Структура
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   Структура

---

### <a name="данныеиерархииподаннымgit"></a>ДанныеИерархииПоДаннымGit

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеИерархииПоДаннымGit(Проект, ТокенПользователя, Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария работы пользователя из репозитория
  Параметры:
   Проект - Справочники.Проекты
   ДанныеСценария - Структура
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   Структура

---

### <a name="данныенастройкисценарияизрепозитория"></a>ДанныеНастройкиСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастройкиСценарияИзРепозитория(Проект, ДанныеНастройки, ТокенПользователя,  Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные настройки сценария работы пользователя из репозитория
  Параметры:
   Проект - Справочники.Проекты
   ДанныеНастройки - Структура
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   Структура

---

### <a name="имяветкистрокой"></a>ИмяВеткиСтрокой

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяВеткиСтрокой(Ветка) Экспорт
  ```
- **Описание:**

  Возвращает представление ветки
  Параметры:
   Ветка - Справочник.Ветки

  Возвращаемое значение:
   Строка

---

### <a name="имяветкиврепозитории"></a>ИмяВеткиВРепозитории

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяВеткиВРепозитории(Ветка) Экспорт
  ```
- **Описание:**

  Возвращает имя ветки в репозитории
  Параметры:
   Ветка - Справочник.Ветки

  Возвращаемое значение:
   Строка

---

### <a name="имяпроектастрокой"></a>ИмяПроектаСтрокой

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяПроектаСтрокой(Проект) Экспорт
  ```
- **Описание:**

  Возвращает представление проекта
  Параметры:
   Проект - Справочник.Проекты

  Возвращаемое значение:
   Строка

---

### <a name="параметрыдляполучениясценариев"></a>ПараметрыДляПолученияСценариев

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыДляПолученияСценариев(Проект, Ветка=Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает основную ветку проекта
  Параметры:
   Проект - Справочник.Проекты

  Возвращаемое значение:
   Структура

---

### <a name="файлысценарияизрепозитория"></a>ФайлыСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ФайлыСценарияИзРепозитория(ДанныеДляПолученияФайлов) Экспорт
  ```
- **Описание:**

  Возвращает двоичные данные файлов сценария работы пользователя из репозитория
  Параметры:
   ДанныеДляПолученияФайлов - Структура

  Возвращаемое значение:
   Массив

---

### <a name="служебныеданныесценариев"></a>СлужебныеДанныеСценариев

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СлужебныеДанныеСценариев(Параметры) Экспорт
  ```
- **Описание:**

  Возвращает считанные служебные данные сценариев из репозитория
  Параметры:
   Параметры - Структура

  Возвращаемое значение:
   Структура

---

### <a name="данныенастроексценарияизрепозитория"></a>ДанныеНастроекСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастроекСценарияИзРепозитория(ДанныеДляПолученияНастроек) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария работы пользователя из репозитория
  Параметры:
   ДанныеДляПолученияНастроек - Структура

  Возвращаемое значение:
   Структура

---

### <a name="данныедлявыгрузки"></a>ДанныеДляВыгрузки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеДляВыгрузки(МассивТестов) Экспорт
  ```
- **Описание:**

  Возвращает данные для выгрузки сценариев
  Параметры:
   МассивТестов - Массив

---

### <a name="данныеэталонныхбд"></a>ДанныеЭталонныхБД

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеЭталонныхБД(СписокТестов) Экспорт
  ```
- **Описание:**

  Возвращает данные эталонных БД
  Параметры:
   СписокТестов - СписокЗнаечений

  Возвращаемое значение:
   Массив

---

### <a name="токенпроекта"></a>ТокенПроекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТокенПроекта(Проект) Экспорт
  ```
- **Описание:**

  Возвращает токен проекта
  Параметры:
   Проект - Справочник.Проекты

  Возвращаемое значение:
   Строка

---

### <a name="параметрысценарияизтекстовыхданных"></a>ПараметрыСценарияИзТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыСценарияИзТекстовыхДанных(Данные) Экспорт
  ```
- **Описание:**

  Возвращает параметры сценария
  Параметры:
   Данные - Массив

  Возвращаемое значение:
   Массив

---

### <a name="тегисценарияизтекстовыхданных"></a>ТегиСценарияИзТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТегиСценарияИзТекстовыхДанных(Данные) Экспорт
  ```
- **Описание:**

  Возвращает теги сценария
  Параметры:
   Данные - Массив

  Возвращаемое значение:
   Массив

---

### <a name="вложенныесценариитекстовыхданных"></a>ВложенныеСценарииТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВложенныеСценарииТекстовыхДанных(Данные) Экспорт
  ```
- **Описание:**

  Возвращает вложеныне сценарии
  Параметры:
   Данные - Массив

  Возвращаемое значение:
   Массив

---

### <a name="вызывающиесценариитекстовыхданных"></a>ВызывающиеСценарииТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВызывающиеСценарииТекстовыхДанных(Данные) Экспорт
  ```
- **Описание:**

  Возвращает вызывающие сценарии
  Параметры:
   Данные - Массив

  Возвращаемое значение:
   Массив

---

### <a name="каталогдляданныхтестирования"></a>КаталогДляДанныхТестирования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогДляДанныхТестирования() Экспорт
  ```
- **Описание:**

  Возвращает значение каталога данных, в который выгружаются данные для запуска сценария.

  Возвращаемое значение:
    Строка

---

### <a name="проверкаисправленияошибоквветке"></a>ПроверкаИсправленияОшибокВВетке

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПроверкаИсправленияОшибокВВетке(Ветка) Экспорт
  ```
- **Описание:**

  Возвращает результат выполнения метода ПроверкаИсправленияОшибокВВетке.
  Параметры:
   Ветка - Справочник.Ветки

  Возвращаемое значение:
   Структура

---

### <a name="данныерасшифровкиотчетовтестирования"></a>ДанныеРасшифровкиОтчетовТестирования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеРасшифровкиОтчетовТестирования(Расшифровка, АдресДанныхРасшифровки) Экспорт
  ```
- **Описание:**

  Получает данные расшифровки для отчетов тестирования "СтатусПрохожденияТестовВВетке" и "ТестыЗаблокированныеОшибками".

  Параметры:
  Расшифровка - ИдентификаторРасшифровкиКомпоновкиДанных - Идентификатор расшифровываемого поля
  АдресДанныхРасшифровки - Строка - Адрес временного хранилища данных расшифровки

  Возвращаемое значение:
  Структура - Параметры расшифровки.

---

### <a name="расшифроватьошибки"></a>РасшифроватьОшибки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РасшифроватьОшибки(ОтчетДанныеРасшифровки, Отчет, Расшифровка) Экспорт
  ```
- **Описание:**

  Получает описание обработки расшифровки для поля Ошибка для отчетов тестирования "СтатусПрохожденияТестовВВетке" и "ТестыЗаблокированныеОшибками".

  Параметры:
  	ОтчетДанныеРасшифровки - Строка - Адрес временного хранилища данных расшифровки
  	Отчет - ДанныеФормыСтруктура - Источник доступных настроек компоновки данных
  	Расшифровка - ИдентификаторРасшифровкиКомпоновкиДанных - Идентификатор расшифровываемого поля

  Возвращаемое значение:
  ОписаниеОбработкиРасшифровкиКомпоновкиДанных - Параметры для отчета с расшфировкой по ошибкам.

---

### <a name="объединитьзаголовкивтабличномдокументе"></a>ОбъединитьЗаголовкиВТабличномДокументе

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбъединитьЗаголовкиВТабличномДокументе(ТабличныйДокумент, ПодстрокаПоиска) Экспорт
  ```
- **Описание:**

  Находит заголовки в табличном документе, начинающиеся с подстроки и выносит подстроку в общую ячейку над заголовками

  Параметры:
  	ТабличныйДокумент - ТабличныйДокумент - Табличный документ, в котором выполняется поиск подстроки
  	ПодстрокаПоиска   - Строка - Подстрока, которую нужно найти в заголовках

---

