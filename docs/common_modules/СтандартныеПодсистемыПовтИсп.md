# Модуль СтандартныеПодсистемыПовтИсп

**Описание подсистемы:** Стандартные подсистемы повт исп

Всего найдено экспортных методов: **29**

## Список методов

- [ОписанияПодсистем](#описанияподсистем) (Функция)
- [ПривилегированныйРежимУстановленПриЗапуске](#привилегированныйрежимустановленпризапуске) (Функция)
- [ОтключитьИдентификаторыОбъектовМетаданных](#отключитьидентификаторыобъектовметаданных) (Функция)
- [ИдентификаторыОбъектовМетаданныхПроверкаИспользования](#идентификаторыобъектовметаданныхпроверкаиспользования) (Функция)
- [ИспользуетсяРИБ](#используетсяриб) (Функция)
- [УзлыРИБ](#узлыриб) (Функция)
- [ПланыОбменаРИБ](#планыобменариб) (Функция)
- [РежимРегистрацииДанныхДляПланаОбмена](#режимрегистрацииданныхдляпланаобмена) (Функция)
- [ДоступностьОбъектовПоОпциям](#доступностьобъектовпоопциям) (Функция)
- [ПоследняяВерсияКомпонентыИзМакета](#последняяверсиякомпонентыизмакета) (Функция)
- [СвойстваКоллекцийОбъектовМетаданных](#свойстваколлекцийобъектовметаданных) (Функция)
- [ЭтоРазделенныйРежимБезРасширенийОбластиДанных](#эторазделенныйрежимбезрасширенийобластиданных) (Функция)
- [ОбщиеПараметрыБазовойФункциональности](#общиепараметрыбазовойфункциональности) (Функция)
- [ОпцииИнтерфейса](#опцииинтерфейса) (Функция)
- [ИменаПодсистем](#именаподсистем) (Функция)
- [ОписаниеТипаВсеСсылки](#описаниетипавсессылки) (Функция)
- [ЭтоСеансДлительнойОперации](#этосеансдлительнойоперации) (Функция)
- [ИнформационнаяБазаФайловая](#информационнаябазафайловая) (Функция)
- [РазделениеВключено](#разделениевключено) (Функция)
- [ДоступноИспользованиеРазделенныхДанных](#доступноиспользованиеразделенныхданных) (Функция)
- [ИменаКоллекцийПоИменамБазовыхТипов](#именаколлекцийпоименамбазовыхтипов) (Функция)
- [СвойстваТекущегоСеанса](#свойстватекущегосеанса) (Функция)
- [ШаблоныЗаданийОчереди](#шаблонызаданийочереди) (Функция)
- [КэшИдентификаторовОбъектовМетаданных](#кэшидентификаторовобъектовметаданных) (Функция)
- [ТаблицаПереименованияДляТекущейВерсии](#таблицапереименованиядлятекущейверсии) (Функция)
- [ПредставлениеИдентификатораОбъектаМетаданных](#представлениеидентификатораобъектаметаданных) (Функция)
- [РолиПоКлючамОбъектовМетаданных](#ролипоключамобъектовметаданных) (Функция)
- [СсылкиПоИменамПредопределенных](#ссылкипоименампредопределенных) (Функция)
- [НовоеОписаниеПодсистемы](#новоеописаниеподсистемы) (Функция)

---

### <a name="описанияподсистем"></a>ОписанияПодсистем

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОписанияПодсистем() Экспорт
  ```
- **Описание:**

  Возвращает описания всех библиотек конфигурации, включая
  описание самой конфигурации.

  Возвращаемое значение:
   ФиксированнаяСтруктура:
    * Порядок - Массив из Строка
    * ПоИменам - Соответствие из КлючИЗначение:
      ** Ключ - Строка
      ** Значение - см. НовоеОписаниеПодсистемы

---

### <a name="привилегированныйрежимустановленпризапуске"></a>ПривилегированныйРежимУстановленПриЗапуске

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПривилегированныйРежимУстановленПриЗапуске() Экспорт
  ```
- **Описание:**

  Возвращает Истина, если привилегированный режим был установлен
  при запуске с помощью параметра UsePrivilegedMode.

  Поддерживается только при запуске клиентских приложений
  (внешнее соединение не поддерживается).

  Возвращаемое значение:
   Булево

---

### <a name="отключитьидентификаторыобъектовметаданных"></a>ОтключитьИдентификаторыОбъектовМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОтключитьИдентификаторыОбъектовМетаданных() Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

  Возвращаемое значение:
   Булево

---

### <a name="идентификаторыобъектовметаданныхпроверкаиспользования"></a>ИдентификаторыОбъектовМетаданныхПроверкаИспользования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИдентификаторыОбъектовМетаданныхПроверкаИспользования(ПроверитьОбновление = Ложь, ОбъектыРасширений = Ложь) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

  Параметры:
   ПроверитьОбновление  - Булево
   ОбъектыРасширений    - Булево

  Возвращаемое значение:
   Булево

---

### <a name="используетсяриб"></a>ИспользуетсяРИБ

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИспользуетсяРИБ(ФильтрПоНазначению = "") Экспорт
  ```
- **Описание:**

  Возвращает признак использования в информационной базе полного РИБ (без фильтров).
  Проверка выполняется по более точному алгоритму, если используется подсистема "Обмен данными".

  Параметры:
   ФильтрПоНазначению - Строка - уточняет, наличие какого РИБ проверяется:
                                 Пустая строка - любого РИБ;
                                 "СФильтром" - РИБ с фильтром;
                                 "Полный" - РИБ без фильтров.

  Возвращаемое значение:
    Булево

---

### <a name="узлыриб"></a>УзлыРИБ

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УзлыРИБ(ФильтрПоНазначению = "") Экспорт
  ```
- **Описание:**

  Возвращает список используемых в информационной базе узлов РИБ (без фильтров).
  Проверка выполняется по более точному алгоритму, если используется подсистема "Обмен данными".

  Параметры:
   ФильтрПоНазначению - Строка - задает назначение узлов планов обмена РИБ, которые необходимо вернуть:
                                 Пустая строка - будут возвращены все узлы РИБ;
                                 "СФильтром" - будут возвращены узлы РИБ с фильтром;
                                 "Полный" - будут возвращены узлы РИБ без фильтров.

  Возвращаемое значение:
    СписокЗначений

---

### <a name="планыобменариб"></a>ПланыОбменаРИБ

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПланыОбменаРИБ() Экспорт
  ```
- **Описание:**

  Возвращает список планов обмена РИБ.
  Если конфигурация работает в модели сервиса,
  то возвращает список разделенных планов обмена РИБ.

  Возвращаемое значение:
   Массив из Строка

---

### <a name="режимрегистрацииданныхдляпланаобмена"></a>РежимРегистрацииДанныхДляПланаОбмена

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РежимРегистрацииДанныхДляПланаОбмена(ПолноеИмяОбъекта, ИмяПланаОбмена) Экспорт
  ```
- **Описание:**

  Определяет режим регистрации данных на узлах плана обмена.

  Параметры:
   ПолноеИмяОбъекта - Строка - полное имя проверяемого объекта метаданных.
   ИмяПланаОбмена - Строка - проверяемый план обмена.

  Возвращаемое значение:
   Неопределено - объект не включен в состав плана обмена,
   "АвторегистрацияВключена"  - объект включен в состав плана обмена, авторегистрация включена,
   "АвторегистрацияОтключена" - объект включен в состав плана обмена, авторегистрация отключена,
                                объекты обрабатываются при создания начального образа РИБ.
   "ПрограммнаяРегистрация"   - объект включен в состав плана обмена, авторегистрация отключена,
                                регистрация осуществляется программно с помощью подписок на события,
                                объекты обрабатываются при создания начального образа РИБ.

---

### <a name="доступностьобъектовпоопциям"></a>ДоступностьОбъектовПоОпциям

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДоступностьОбъектовПоОпциям() Экспорт
  ```
- **Описание:**

  Доступность объектов метаданных по функциональным опциям.

  Возвращаемое значение:
   ФиксированноеСоответствие из КлючИЗначение:
    * Ключ - Строка
    * Значение - Булево

---

### <a name="последняяверсиякомпонентыизмакета"></a>ПоследняяВерсияКомпонентыИзМакета

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПоследняяВерсияКомпонентыИзМакета(Местоположение) Экспорт
  ```
- **Описание:**

  Последняя версия компоненты из макета.

  Параметры:
   Местоположение - Строка - полное имя макета в метаданных

  Возвращаемое значение:
   ФиксированнаяСтруктура - последняя версия компоненты из макета:
    * Версия - Строка
    * Местоположение - Строка

---

### <a name="свойстваколлекцийобъектовметаданных"></a>СвойстваКоллекцийОбъектовМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СвойстваКоллекцийОбъектовМетаданных(ОбъектыРасширений = Ложь) Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.СвойстваКоллекцийОбъектовМетаданных

---

### <a name="эторазделенныйрежимбезрасширенийобластиданных"></a>ЭтоРазделенныйРежимБезРасширенийОбластиДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоРазделенныйРежимБезРасширенийОбластиДанных() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="общиепараметрыбазовойфункциональности"></a>ОбщиеПараметрыБазовойФункциональности

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОбщиеПараметрыБазовойФункциональности() Экспорт
  ```
- **Описание:**

  Кеширует общие параметры работы между вызовами сервера.

  Возвращаемое значение:
    см. ОбщегоНазначенияПереопределяемый.ПриОпределенииОбщихПараметровБазовойФункциональности.ОбщиеПараметры

---

### <a name="опцииинтерфейса"></a>ОпцииИнтерфейса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОпцииИнтерфейса() Экспорт
  ```
- **Описание:**

  Параметры, применяемых к элементам командного интерфейса, связанным с параметрическими функциональными опциями.

  Возвращаемое значение:
   ФиксированнаяСтруктура:
    * Ключ - Строка
    * Значение - Произвольный

---

### <a name="именаподсистем"></a>ИменаПодсистем

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИменаПодсистем() Экспорт
  ```
- **Описание:**

  Возвращает соответствие имен "функциональных" подсистем и значения Истина.
  У "функциональной" подсистемы снят флажок "Включать в командный интерфейс".

  Возвращаемое значение:
   ФиксированноеСоответствие из КлючИЗначение:
    * Ключ - Строка
    * Значение - Булево

---

### <a name="описаниетипавсессылки"></a>ОписаниеТипаВсеСсылки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОписаниеТипаВсеСсылки() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="этосеансдлительнойоперации"></a>ЭтоСеансДлительнойОперации

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоСеансДлительнойОперации() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="информационнаябазафайловая"></a>ИнформационнаяБазаФайловая

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИнформационнаяБазаФайловая() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="разделениевключено"></a>РазделениеВключено

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РазделениеВключено() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="доступноиспользованиеразделенныхданных"></a>ДоступноИспользованиеРазделенныхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДоступноИспользованиеРазделенныхДанных() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="именаколлекцийпоименамбазовыхтипов"></a>ИменаКоллекцийПоИменамБазовыхТипов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИменаКоллекцийПоИменамБазовыхТипов() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="свойстватекущегосеанса"></a>СвойстваТекущегоСеанса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СвойстваТекущегоСеанса() Экспорт
  ```
- **Описание:**

  Возвращаемое значение:
   Структура:
    * НомерСеанса - Число
    * НачалоСеанса - Дата
    * КлючКомпьютера - Строка

---

### <a name="шаблонызаданийочереди"></a>ШаблоныЗаданийОчереди

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ШаблоныЗаданийОчереди() Экспорт
  ```
- **Описание:**

  Возвращаемое значение:
   ФиксированноеСоответствие из КлючИЗначение:
     * Ключ - Строка - имя предопределенного неразделенного регламентного задания,
                       которое используются в качестве шаблона для задания очереди.
     * Значение - Булево - значение Истина.

---

### <a name="кэшидентификаторовобъектовметаданных"></a>КэшИдентификаторовОбъектовМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КэшИдентификаторовОбъектовМетаданных(КлючДанныхПовторногоИспользования) Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.КэшИдентификаторовОбъектовМетаданных

---

### <a name="таблицапереименованиядлятекущейверсии"></a>ТаблицаПереименованияДляТекущейВерсии

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТаблицаПереименованияДляТекущейВерсии() Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.ТаблицаПереименованияДляТекущейВерсии

---

### <a name="представлениеидентификатораобъектаметаданных"></a>ПредставлениеИдентификатораОбъектаМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредставлениеИдентификатораОбъектаМетаданных(Ссылка) Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.ПредставлениеИдентификатора

---

### <a name="ролипоключамобъектовметаданных"></a>РолиПоКлючамОбъектовМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РолиПоКлючамОбъектовМетаданных() Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.РолиПоКлючамОбъектовМетаданных

---

### <a name="ссылкипоименампредопределенных"></a>СсылкиПоИменамПредопределенных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СсылкиПоИменамПредопределенных(ПолноеИмяОбъектаМетаданных) Экспорт
  ```
- **Описание:**

  Возвращает соответствие имен предопределенных значений ссылкам на них.

  Параметры:
   ПолноеИмяОбъектаМетаданных - Строка - например, "Справочник.ВидыНоменклатуры",
                                Поддерживаются только таблицы
                                с предопределенными элементами:
                                > Справочники,
                                > Планы видов характеристик,
                                > Планы счетов,
                                > Планы видов расчета.

  Возвращаемое значение:
   ФиксированноеСоответствие из КлючИЗначение:
       * Ключ     - Строка - имя предопределенного,
       * Значение - СправочникСсылка
                  - ПланВидовХарактеристикСсылка
                  - ПланСчетовСсылка
                  - ПланВидовРасчетаСсылка
                  - Null - ссылка предопределенного или Null, если объекта нет в ИБ.

   Если ошибка в имени метаданных или неподходящий тип метаданного, то возвращается Неопределено.
   Если предопределенных у метаданного нет, то возвращается пустое фиксированное соответствие.
   Если предопределенный определен в метаданных, но не создан в ИБ, то для него в соответствии возвращается Null.

---

### <a name="новоеописаниеподсистемы"></a>НовоеОписаниеПодсистемы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НовоеОписаниеПодсистемы() Экспорт
  ```
- **Описание:**

  Возвращаемое значение:
    Структура:
    * ПараллельноеОтложенноеОбновлениеСВерсии - Строка
    * РежимВыполненияОтложенныхОбработчиков - Строка
    * ОсновнойСерверныйМодуль - Строка
    * ЭтоКонфигурация - Булево
    * ИдентификаторИнтернетПоддержки - Строка
    * ТребуемыеПодсистемы - Массив
    * Версия - Строка
    * Имя - Строка
    * ЗаполнятьДанныеНовыхПодсистемПриПереходеСДругойПрограммы - Булево

---

