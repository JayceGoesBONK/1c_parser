# Модуль ТестированиеКлиент

**Описание подсистемы:** Тестирование клиент

Всего найдено экспортных методов: **43**

## Список методов

- [СоздатьЛокальнуюБазуДанныхИзЭталона](#создатьлокальнуюбазуданныхизэталона) (Функция)
- [ПараметрыЗапускаТестов](#параметрызапускатестов) (Функция)
- [ЗаменитьСтрокуПодключенияПрофиляTestClient](#заменитьстрокуподключенияпрофиляtestclient) (Процедура)
- [ПодключитьФреймВоркТестирования](#подключитьфреймворктестирования) (Процедура)
- [ОткрытьФормуФреймворкаТестирования](#открытьформуфреймворкатестирования) (Процедура)
- [ПолученОтветОтВнешнегоTestManager](#полученответотвнешнегоtestmanager) (Процедура)
- [ОбработкаРасшифровки](#обработкарасшифровки) (Процедура)
- [ДанныеСценарияИзYaml](#данныесценарияизyaml) (Функция)
- [ДанныеНастройкиСценарияИзYaml](#данныенастройкисценарияизyaml) (Функция)
- [ЗаписатьСценарийВоВременныйФайл](#записатьсценарийвовременныйфайл) (Процедура)
- [ВыгрузитьДанныеЭталонныхБДВФайл](#выгрузитьданныеэталонныхбдвфайл) (Процедура)
- [ВыгрузитьТестыВКаталог](#выгрузитьтестывкаталог) (Процедура)
- [ЗаписатьНастройкуСценарияВоВременныйФайл](#записатьнастройкусценариявовременныйфайл) (Процедура)
- [ЗаписатьИдентификаторКоммитаВоВременныйФайл](#записатьидентификаторкоммитавовременныйфайл) (Процедура)
- [КаталогВременныхФайловВетки](#каталогвременныхфайловветки) (Функция)
- [ДанныеКоммитаВетки](#данныекоммитаветки) (Функция)
- [ДанныеРанееИзмененныхСценариев](#данныеранееизмененныхсценариев) (Функция)
- [УдалитьВременныеФайлыСценариевРаботыПользователей](#удалитьвременныефайлысценариевработыпользователей) (Процедура)
- [УдалитьВременныеФайлыСценария](#удалитьвременныефайлысценария) (Процедура)
- [ДанныеСценарияИзРепозитория](#данныесценарияизрепозитория) (Функция)
- [ОткрытьСценарийДляРедактированияВРепозитории](#открытьсценарийдляредактированияврепозитории) (Процедура)
- [ОткрытьНастройкуСценарияДляРедактированияВРепозитории](#открытьнастройкусценариядляредактированияврепозитории) (Процедура)
- [СостояниеФайловЛокальнойКопииРепозитория](#состояниефайловлокальнойкопиирепозитория) (Функция)
- [ОбновитьДанныеОСпискеЛокальноИзмененныхФайловРепозитория](#обновитьданныеоспискелокальноизмененныхфайловрепозитория) (Процедура)
- [ТокенПользователя](#токенпользователя) (Функция)
- [ДополнитьПараметрыОткрытияСценария](#дополнитьпараметрыоткрытиясценария) (Процедура)
- [ДанныеНастроекСценарияИзРепозитория](#данныенастроексценарияизрепозитория) (Функция)
- [ЗаписатьДанныеНовойГруппы](#записатьданныеновойгруппы) (Процедура)
- [ЗаписатьДанныеОбУдаленныхСценариях](#записатьданныеобудаленныхсценариях) (Процедура)
- [ЗаписатьДанныеОбУдаленныхНастройках](#записатьданныеобудаленныхнастройках) (Процедура)
- [ТекущиеДанныеСценария](#текущиеданныесценария) (Функция)
- [ВыгрузитьДанныеВКаталогВФорматеYaml](#выгрузитьданныевкаталогвформатеyaml) (Процедура)
- [ДанныYamlВФорматСтруктурыСценария](#данныyamlвформатструктурысценария) (Функция)
- [ДобавитьИнформациюВСценарийОВнешнемВызове](#добавитьинформациювсценарийовнешнемвызове) (Процедура)
- [УдалитьИнформациюИзСценарияОВнешнемВызове](#удалитьинформациюизсценарияовнешнемвызове) (Процедура)
- [ПоказатьОкноСравнениеТекста](#показатьокносравнениетекста) (Процедура)
- [КаталогРедактора](#каталогредактора) (Функция)
- [ЕстьКешФайловРедактора](#естькешфайловредактора) (Функция)
- [ПроверкаИсправленияОшибокВВетке](#проверкаисправленияошибоквветке) (Процедура)
- [СтатусТестовВВетке](#статустестоввветке) (Процедура)
- [ОбработкаПослеПомещенияФайла](#обработкапослепомещенияфайла) (Процедура)
- [КомандаОС](#командаос) (Функция)
- [ДобавитьОтбор](#добавитьотбор) (Функция)

---

### <a name="создатьлокальнуюбазуданныхизэталона"></a>СоздатьЛокальнуюБазуДанныхИзЭталона

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СоздатьЛокальнуюБазуДанныхИзЭталона(Параметры) Экспорт
  ```
- **Описание:**

  Создает локальную базу данных по переданным параметрам

  Параметры:
   Параметры - Структура

  Возвращаемое значение:
   Булево - Статус операции

---

### <a name="параметрызапускатестов"></a>ПараметрыЗапускаТестов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыЗапускаТестов() Экспорт
  ```
- **Описание:**

  Возвращает параметры запуска тестов

  Возвращаемое значение:
   Структура

---

### <a name="заменитьстрокуподключенияпрофиляtestclient"></a>ЗаменитьСтрокуПодключенияПрофиляTestClient

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаменитьСтрокуПодключенияПрофиляTestClient(ТекстСценариев,НастройкиБДДляЗапускаТестовИзСценария,Роль) Экспорт
  ```
- **Описание:**

  Подставляет в текст сценария строку подключения TestClient

  Параметры:
   ТекстСценариев - Строка
   НастройкиБДДляЗапускаТестовИзСценария - Структура
   Роль - СправочникСсылка.ПрофилиПользователей

---

### <a name="подключитьфреймворктестирования"></a>ПодключитьФреймВоркТестирования

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПодключитьФреймВоркТестирования(УникальныйИдентификатор,ИмяПроцедуры,Модуль,ДополнительныеПараметры) Экспорт
  ```
- **Описание:**

  Подключает файл фреймворка запускающего тесты на выполнение
  Параметры:
   УникальныйИдентификатор - Строка
   ИмяПроцедуры - Структура
   Модуль - Строка
   ДополнительныеПараметры - Структура

---

### <a name="открытьформуфреймворкатестирования"></a>ОткрытьФормуФреймворкаТестирования

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОткрытьФормуФреймворкаТестирования(ПараметрыЗапуска) Экспорт
  ```
- **Описание:**

  Запускает фреймворк тестирования с нужным сценарием

  Параметры:
   ПараметрыЗапуска - Структура

---

### <a name="полученответотвнешнегоtestmanager"></a>ПолученОтветОтВнешнегоTestManager

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПолученОтветОтВнешнегоTestManager() Экспорт
  ```
- **Описание:**

  Проверяет файл ответа внешнего TestManager

---

### <a name="обработкарасшифровки"></a>ОбработкаРасшифровки

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбработкаРасшифровки(ФормаОтчета, Элемент, Расшифровка, СтандартнаяОбработка) Экспорт
  ```
- **Описание:**

  Обработчик расшифровки табличного документа формы отчета.
  См. "Расширение поля формы для поля табличного документа.ОбработкаРасшифровки" в синтакс-помощнике.

  Параметры:
    ФормаОтчета - ФормаКлиентскогоПриложения - форма отчета.
    Элемент     - ПолеФормы        - табличный документ.
    Расшифровка - Произвольный     - значение расшифровки точки, серии или значения диаграммы.
    СтандартнаяОбработка - Булево  - признак выполнения стандартной (системной) обработки события.

---

### <a name="данныесценарияизyaml"></a>ДанныеСценарияИзYaml

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеСценарияИзYaml(ИсточникМетаданные, ИсточникТекстСценария, ДанныеСценария) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария из формата Yaml
  Параметры:
   ИсточникМетаданные - Строка или Поток
   ИсточникТекстСценария - Строка или Поток
   ДанныеСценария - Структура

  Возвращаемое значение:
   Соответствие

---

### <a name="данныенастройкисценарияизyaml"></a>ДанныеНастройкиСценарияИзYaml

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастройкиСценарияИзYaml(ИсточникМетаданные, ДопПараметры) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария из формата Yaml
  Параметры:
   ИсточникМетаданные - Строка или Поток
   ИсточникТекстСценария - Строка или Поток
   ДопПараметры - Структура

  Возвращаемое значение:
   Соответствие

---

### <a name="записатьсценарийвовременныйфайл"></a>ЗаписатьСценарийВоВременныйФайл

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьСценарийВоВременныйФайл(ДанныеСценария) Экспорт
  ```
- **Описание:**

  Сохраняет данные сценария во временный файл
  Параметры:
   ДанныеСценария - Структура

---

### <a name="выгрузитьданныеэталонныхбдвфайл"></a>ВыгрузитьДанныеЭталонныхБДВФайл

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыгрузитьДанныеЭталонныхБДВФайл(ДанныеДляВыгрузки, ПолноеИмяФайла) Экспорт
  ```
- **Описание:**

  Совершает выгрузку данных в каталог

  Параметры:
   ДанныеДляВыгрузки - Массив
   ПолноеИмяФайла - Строка

---

### <a name="выгрузитьтестывкаталог"></a>ВыгрузитьТестыВКаталог

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыгрузитьТестыВКаталог(СписокТестов, КаталогВыгрузкиСценариев, КаталогВыгрузкиПроцессов, КаталогДанныхЭталонныхБД, ПереданныйТипВыгрузки) Экспорт
  ```
- **Описание:**

  Совершает выгрузку данных в каталог

  Параметры:
   СписокТестов - СписокЗначений
   КаталогВыгрузкиСценариев - Строка
   КаталогВыгрузкиПроцессов - Строка
   КаталогДанныхЭталонныхБД - Строка
   ПереданныйТипВыгрузки - Строка

---

### <a name="записатьнастройкусценариявовременныйфайл"></a>ЗаписатьНастройкуСценарияВоВременныйФайл

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьНастройкуСценарияВоВременныйФайл(Данные) Экспорт
  ```
- **Описание:**

  Сохраняет данные настройки сценария во временный файл
  Параметры:
   ДанныеСценария - Структура

---

### <a name="записатьидентификаторкоммитавовременныйфайл"></a>ЗаписатьИдентификаторКоммитаВоВременныйФайл

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьИдентификаторКоммитаВоВременныйФайл(Ветка, ИмяПроекта, ИдентификаторКоммита, АвторКоммита, ДатаКоммита, СсылкаНаКоммит) Экспорт
  ```
- **Описание:**

  Сохраняет идентифиактор коммита во временный файл
  Параметры:
   Ветка - Справочник.Ветки
   ИмяПроекта - Строка
   ИдентификаторКоммита - Строка
   АвторКоммита - Строка
   ДатаКоммита - Дата
   СсылкаНаКоммит - Строка

---

### <a name="каталогвременныхфайловветки"></a>КаталогВременныхФайловВетки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогВременныхФайловВетки(Знач КаталогДляДанныхТестирования, Ветка, ИмяПроекта) Экспорт
  ```
- **Описание:**

  Возвращает каталог верменных файлов ветки
  Параметры:
   КаталогДляДанныхТестирования - Строка
   ИмяВетки - Строка

  Возвращаемое значение:
   Строка

---

### <a name="данныекоммитаветки"></a>ДанныеКоммитаВетки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеКоммитаВетки(Ветка, ИмяПроекта) Экспорт
  ```
- **Описание:**

  Считывает раннее сохраненные данные сценариев пользователя
  Параметры:
   Ветка - Справочник.Ветки
   ИмяПроекта - Строка

  Возвращаемое значение:
   Данные - Структура

---

### <a name="данныеранееизмененныхсценариев"></a>ДанныеРанееИзмененныхСценариев

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеРанееИзмененныхСценариев(Ветка, Проект) Экспорт
  ```
- **Описание:**

  Считывает раннее сохраненные данные сценариев пользователя
  Параметры:
   Ветка - Справочник.Ветки
   Проект - Справочник.Проекты

  Возвращаемое значение:
   Структура

---

### <a name="удалитьвременныефайлысценариевработыпользователей"></a>УдалитьВременныеФайлыСценариевРаботыПользователей

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УдалитьВременныеФайлыСценариевРаботыПользователей(Ветка, ИмяПроекта) Экспорт
  ```
- **Описание:**

  Удаляет файлы сценариев из временного каталога
  Параметры:
   Ветка - Справочник.Ветки
   ИмяПроекта - Строка

---

### <a name="удалитьвременныефайлысценария"></a>УдалитьВременныеФайлыСценария

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УдалитьВременныеФайлыСценария(Ветка, ИмяПроекта, КодСценария, КодНастройки) Экспорт
  ```
- **Описание:**

  Удаляет файлы сценариев из временного каталога
  Параметры:
   Ветка - Справочник.Ветки
   Код - Строка

---

### <a name="данныесценарияизрепозитория"></a>ДанныеСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеСценарияИзРепозитория(ДанныеДляПолученияСценария) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария из репозитория
  Параметры:
   ДанныеДляПолученияСценария - Структура

  Возвращаемое значение:
   Структура

---

### <a name="открытьсценарийдляредактированияврепозитории"></a>ОткрытьСценарийДляРедактированияВРепозитории

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОткрытьСценарийДляРедактированияВРепозитории(ДанныеДляПолученияСценария) Экспорт
  ```
- **Описание:**

  Открывает сценарий для редактирования в репозитории
  Параметры:
   ДанныеДляПолученияСценария - Структура

---

### <a name="открытьнастройкусценариядляредактированияврепозитории"></a>ОткрытьНастройкуСценарияДляРедактированияВРепозитории

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОткрытьНастройкуСценарияДляРедактированияВРепозитории(ДанныеДляПолучения) Экспорт
  ```
- **Описание:**

  Открывает настройку сценария для редактирования в репозитории
  Параметры:
   ДанныеДляПолучения - Структура

---

### <a name="состояниефайловлокальнойкопиирепозитория"></a>СостояниеФайловЛокальнойКопииРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СостояниеФайловЛокальнойКопииРепозитория(ДанныеДляПолученияФайлов) Экспорт
  ```
- **Описание:**

  Возвращает данные об локальных изменениях файлов репозитория
  Параметры:
   ДанныеДляПолученияСценария - Структура

  Возвращаемое значение:
   Структура

---

### <a name="обновитьданныеоспискелокальноизмененныхфайловрепозитория"></a>ОбновитьДанныеОСпискеЛокальноИзмененныхФайловРепозитория

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбновитьДанныеОСпискеЛокальноИзмененныхФайловРепозитория(ДанныеДляПолученияФайлов) Экспорт
  ```
- **Описание:**

  Обновляет данные о локально измененных файлах репозитория
  Параметры:
   ДанныеДляПолученияСценария - Структура

---

### <a name="токенпользователя"></a>ТокенПользователя

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТокенПользователя(ПутьКФайлуТокена = Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает токен пользователя для чтения информации из репозитория
  Параметры:
   ПутьКФайлуТокена - Строка

  Возвращаемое значение:
   Строка

---

### <a name="дополнитьпараметрыоткрытиясценария"></a>ДополнитьПараметрыОткрытияСценария

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДополнитьПараметрыОткрытияСценария(Параметры) Экспорт
  ```
- **Описание:**

  Обновляет параметры для чтения информации из репозитория
  Параметры:
   Параметры - Структура

---

### <a name="данныенастроексценарияизрепозитория"></a>ДанныеНастроекСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастроекСценарияИзРепозитория(ДанныеДляПолученияНастроек) Экспорт
  ```
- **Описание:**

  Возвращает данные настроек сценария из репозитория
  Параметры:
   ДанныеДляПолученияСценария - Структура

  Возвращаемое значение:
   Структура

---

### <a name="записатьданныеновойгруппы"></a>ЗаписатьДанныеНовойГруппы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьДанныеНовойГруппы(Стр, Ветка, Проект) Экспорт
  ```
- **Описание:**

  Записывает данный новой группы дерева сценариев во временный файл

  Параметры:
   Стр - Строка
   Ветка - Справочник.Ветки
   Проект - Справочник.Проекты

---

### <a name="записатьданныеобудаленныхсценариях"></a>ЗаписатьДанныеОбУдаленныхСценариях

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьДанныеОбУдаленныхСценариях(СценарииСПометкойНаУдаление, Ветка, Проект) Экспорт
  ```
- **Описание:**

  Записывает данные об удаленных сценариях

  Параметры:
   СценарииСПометкойНаУдаление - Массив
   Ветка - Справочник.Ветки
   Проект - Справочник.Проекты

---

### <a name="записатьданныеобудаленныхнастройках"></a>ЗаписатьДанныеОбУдаленныхНастройках

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьДанныеОбУдаленныхНастройках(НастройкиСПометкойНаУдаление, Ветка, Проект, КодСценария) Экспорт
  ```
- **Описание:**

  Записывает данные об удаленных настройках сценариев

  Параметры:
   НастройкиСПометкойНаУдаление - Массив
   Ветка - Справочник.Ветки
   Проект - Справочник.Проекты
   КодСценария - Строка

---

### <a name="текущиеданныесценария"></a>ТекущиеДанныеСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТекущиеДанныеСценария(СтруктураПараметров) Экспорт
  ```
- **Описание:**

  Возвращает текущие данные сценария. Если сценарий был изменен локально, то берутся локальные изменения. Иначе данные из репозитория.
  Параметры:
   СтруктураПараметров - Структура

  Возвращаемое значение:
   Структура

---

### <a name="выгрузитьданныевкаталогвформатеyaml"></a>ВыгрузитьДанныеВКаталогВФорматеYaml

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВыгрузитьДанныеВКаталогВФорматеYaml(ДанныеДляВыгрузки) Экспорт
  ```
- **Описание:**

  Делает выгрузку переданных данных в формате yaml
  Параметры:
   ДанныеДляВыгрузки - Массив

---

### <a name="данныyamlвформатструктурысценария"></a>ДанныYamlВФорматСтруктурыСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныYamlВФорматСтруктурыСценария(Данные) Экспорт
  ```
- **Описание:**

  Конвертирует данные сценария, прочитанные из формата yaml в формат, подготовленный для записи сценария
  Параметры:
   Данные - Соответствие

  Возвращаемое значение:
   Структура

---

### <a name="добавитьинформациювсценарийовнешнемвызове"></a>ДобавитьИнформациюВСценарийОВнешнемВызове

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьИнформациюВСценарийОВнешнемВызове(UIDСценария, ДанныеВызывающегоСценария, ДанныеРанееИзмененныхСценариев) Экспорт
  ```
- **Описание:**

  Добавляет в сценарий информацию о том, что его вызывает другой сценарий
  Параметры:
   UIDСценария - Строка
   ДанныеВызывающегоСценария - Структура
   ДанныеРанееИзмененныхСценариев - Структура

---

### <a name="удалитьинформациюизсценарияовнешнемвызове"></a>УдалитьИнформациюИзСценарияОВнешнемВызове

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УдалитьИнформациюИзСценарияОВнешнемВызове(UIDСценария, ДанныеВызывающегоСценария, ДанныеРанееИзмененныхСценариев) Экспорт
  ```
- **Описание:**

  Удаляет из сценария информацию о том, что его вызывает другой сценарий
  Параметры:
   UIDСценария - Строка
   ДанныеВызывающегоСценария - Структура
   ДанныеРанееИзмененныхСценариев - Структура

---

### <a name="показатьокносравнениетекста"></a>ПоказатьОкноСравнениеТекста

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПоказатьОкноСравнениеТекста(Параметры) Экспорт
  ```
- **Описание:**

  Показывает окно сравнения двух тектов
  Параметры:
   Параметры - Структура

---

### <a name="каталогредактора"></a>КаталогРедактора

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогРедактора(КаталогДляДанныхТестирования = Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает каталог редактора

---

### <a name="естькешфайловредактора"></a>ЕстьКешФайловРедактора

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЕстьКешФайловРедактора(КаталогРедактора = "") Экспорт
  ```
- **Описание:**

  Возвращает есть или нет кеш файлов редактора

  Возвращаемое значение:
   Булево

---

### <a name="проверкаисправленияошибоквветке"></a>ПроверкаИсправленияОшибокВВетке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверкаИсправленияОшибокВВетке(Ветка, ФормаВладелец) Экспорт
  ```
- **Описание:**

  Выполняет проверку исправления ошибок в ветке
  Параметры:
   Ветка - Справочник.Ветки

---

### <a name="статустестоввветке"></a>СтатусТестовВВетке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СтатусТестовВВетке(Ветка) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="обработкапослепомещенияфайла"></a>ОбработкаПослеПомещенияФайла

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбработкаПослеПомещенияФайла(Результат,АдресХранилища,ВыбранноеИмяФайла,ДополнительныеПараметры) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="командаос"></a>КомандаОС

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КомандаОС(СтрокаКоманды, ЖдатьОкончания = -1) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="добавитьотбор"></a>ДобавитьОтбор

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДобавитьОтбор(ЭлементСтруктуры, Знач Поле, Значение = Неопределено, ВидСравнения = Неопределено, Использование = Истина, ВПользовательскиеНастройки = Ложь) Экспорт
  ```
- **Описание:**

  Добавляет отбор в коллекцию отборов компоновщика или группы отборов.

  Параметры:
  ЭлементСтруктуры - КомпоновщикНастроекКомпоновкиДанных, НастройкиКомпоновкиДанных, 
                      ОтборКомпоновкиДанных - Элемент структуры.
  Поле - Строка - имя поля, по которому добавляется отбор.
  Значение - Произвольный - Значение отбора.
  ВидСравнения - ВидСравненияКомпоновкиДанных - Вид сравнений компоновки данных (по умолчанию: Равно).
  Использование - Булево - Признак использования отбора (по умолчанию: Истина).
   ВПользовательскиеНастройки - Булево - Признак добавления отбора в пользовательские настройки.

  Возвращаемое значение:
  ЭлементОтбораКомпоновкиДанных - Добавленный элемент отбора.

---

