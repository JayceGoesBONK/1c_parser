# Модуль Универсальные

**Описание подсистемы:** Универсальные

Всего найдено экспортных методов: **15**

## Список методов

- [ПроверитьЗаполнениеРеквизитов](#проверитьзаполнениереквизитов) (Процедура)
- [НайтиСтрокуТаблицыЗначений](#найтистрокутаблицызначений) (Функция)
- [СообщитьОбОшибке](#сообщитьобошибке) (Процедура)
- [ЗначенияРеквизитовСправочника](#значенияреквизитовсправочника) (Функция)
- [УстановитьДоступностьЭлементовУправления](#установитьдоступностьэлементовуправления) (Процедура)
- [УстановитьНовоеЗначение](#установитьновоезначение) (Функция)
- [СтруктураОбъекта](#структураобъекта) (Функция)
- [СтруктураИзмененныхРеквизитовОбъекта](#структураизмененныхреквизитовобъекта) (Функция)
- [ДобавитьНовоеЗначениеКоллекцию](#добавитьновоезначениеколлекцию) (Процедура)
- [СписокРодителейЭлемента](#списокродителейэлемента) (Функция)
- [УстановкаПолногоКодаДочернихЭлементовПВХПриЗаписи](#установкаполногокодадочернихэлементовпвхпризаписи) (Процедура)
- [КорневойЭлементВИерархии](#корневойэлементвиерархии) (Функция)
- [ПолучитьСтруктуруЭлементаОтбора](#получитьструктуруэлементаотбора) (Функция)
- [ЗаполнитьЭлементОтбораИзСтруктуры](#заполнитьэлементотбораизструктуры) (Процедура)
- [Перенумеровать](#перенумеровать) (Процедура)

---

### <a name="проверитьзаполнениереквизитов"></a>ПроверитьЗаполнениеРеквизитов

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверитьЗаполнениеРеквизитов(Объект, СтруктураОбязательныхПолей, Отказ, Заголовок) Экспорт
  ```
- **Описание:**

  Проверяет правильность заполнения шапки документа.
  Если какой-то из реквизтов шапки, влияющий на проведение не заполнен или
  заполнен не корректно, то выставляется флаг отказа в проведении.
  Проверяется также правильность заполнения реквизитов ссылочных полей документа.
  Проверка выполняется по объекту и по выборке из результата запроса по шапке.

  Параметры: 
   ДокументОбъект             - объект проводимого документа, 
   СтруктураОбязательныхПолей - структура, содержащая имена полей, которые собственно и надо проверить.
   Отказ                      - флаг отказа в проведении.
   Заголовок                  - строка, заголовок сообщения об ошибке проведения.

---

### <a name="найтистрокутаблицызначений"></a>НайтиСтрокуТаблицыЗначений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НайтиСтрокуТаблицыЗначений(ТабличнаяЧасть, СтруктураОтбора) Экспорт
  ```
- **Описание:**

  Функция выполняет поиск первой, удовлетворяющей условию поика, строки табличной части.

  Параметры:
   ТабличнаяЧасть - табличная часть документа, в которой осуществляется поиск,
   СтруктураОтбора - структура - задает условие поиска.

  Возвращаемое значение: 
   Строка табличной части - найденная строка табличной части,
   Неопределено           - строка табличной части не найдена.

---

### <a name="сообщитьобошибке"></a>СообщитьОбОшибке

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СообщитьОбОшибке(Сообщение, ОписаниеСообщения = "") Экспорт
  ```
- **Описание:**

  Выводит сообщение об ошибке и выставляет параметр Отказ в "Истина". 
  В случае работы на клиенте или на сервере выводит в окно сообщений,
  в случае внешнего соединения вызывает исключение.

  Параметры:
   ТекстСообщения - строка, текст сообщения.
   Отказ          - булево, признак отказа (необязательный).

---

### <a name="значенияреквизитовсправочника"></a>ЗначенияРеквизитовСправочника

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЗначенияРеквизитовСправочника(Ссылка, ИменаРеквизитов) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="установитьдоступностьэлементовуправления"></a>УстановитьДоступностьЭлементовУправления

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьДоступностьЭлементовУправления(КоллекцияЭлементов, Доступность = Истина, СтрокаИсключений = "") Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="установитьновоезначение"></a>УстановитьНовоеЗначение

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УстановитьНовоеЗначение(Параметр, Значение) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="структураобъекта"></a>СтруктураОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СтруктураОбъекта(Знач Объект) Экспорт
  ```
- **Описание:**

  Возвращает структуру реквизитов справочника к которому относится объект

---

### <a name="структураизмененныхреквизитовобъекта"></a>СтруктураИзмененныхРеквизитовОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СтруктураИзмененныхРеквизитовОбъекта(Знач Объект) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="добавитьновоезначениеколлекцию"></a>ДобавитьНовоеЗначениеКоллекцию

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьНовоеЗначениеКоллекцию(Коллекция, Значение) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="списокродителейэлемента"></a>СписокРодителейЭлемента

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СписокРодителейЭлемента(Ссылка) Экспорт
  ```
- **Описание:**

  Возвращает список родителей элемента иерархического справочника включая сам элемент

---

### <a name="установкаполногокодадочернихэлементовпвхпризаписи"></a>УстановкаПолногоКодаДочернихЭлементовПВХПриЗаписи

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановкаПолногоКодаДочернихЭлементовПВХПриЗаписи(Источник, Отказ) Экспорт
  ```
- **Описание:**

  Устанавливает правильное значение реквизита ПолныйКод дочерним элементам записываемого ПВХ.

---

### <a name="корневойэлементвиерархии"></a>КорневойЭлементВИерархии

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КорневойЭлементВИерархии(Ссылка) Экспорт
  ```
- **Описание:**

  Определяет корневой для указанного элемент в спровчнике с иерархийе

---

### <a name="получитьструктуруэлементаотбора"></a>ПолучитьСтруктуруЭлементаОтбора

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолучитьСтруктуруЭлементаОтбора(Отбор, ИмяЭлемента) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="заполнитьэлементотбораизструктуры"></a>ЗаполнитьЭлементОтбораИзСтруктуры

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьЭлементОтбораИзСтруктуры(Отбор, ИмяЭлементаОтбора, Структура) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="перенумеровать"></a>Перенумеровать

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура Перенумеровать(Объект, ЕстьОшибки, Дочерние = Истина, Рекурсивно = Ложь, Владелец = Неопределено) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

