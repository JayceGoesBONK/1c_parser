# Модуль ОбновлениеКонфигурации

**Описание подсистемы:** Обновление конфигурации

Всего найдено экспортных методов: **27**

## Список методов

- [ИсправленияИзменены](#исправленияизменены) (Функция)
- [НастройкиОбновленияКонфигурации](#настройкиобновленияконфигурации) (Функция)
- [СохранитьНастройкиОбновленияКонфигурации](#сохранитьнастройкиобновленияконфигурации) (Процедура)
- [УстановленныеИсправления](#установленныеисправления) (Функция)
- [УстановкаИУдалениеИсправлений](#установкаиудалениеисправлений) (Функция)
- [ПараметрыУстановкиИсправлений](#параметрыустановкиисправлений) (Функция)
- [ПредупреждатьОНаличииРасширений](#предупреждатьоналичиирасширений) (Функция)
- [ДляТекущейВерсииНеобходимоУспешноеЗавершениеОбработчиков](#длятекущейверсиинеобходимоуспешноезавершениеобработчиков) (Функция)
- [ПроверитьНаличиеУстаревшихИсправлений](#проверитьналичиеустаревшихисправлений) (Процедура)
- [ОписанияНовыхПатчей](#описанияновыхпатчей) (Процедура)
- [ЗавершитьОбновление](#завершитьобновление) (Процедура)
- [КаталогСкрипта](#каталогскрипта) (Функция)
- [ИмяФормыУстановкиОбновления](#имяформыустановкиобновления) (Функция)
- [СвойстваИсправления](#свойстваисправления) (Функция)
- [ЭтоИсправление](#этоисправление) (Функция)
- [ОбновитьИсправленияИзСкрипта](#обновитьисправленияизскрипта) (Процедура)
- [ПослеОбновленияИнформационнойБазы](#послеобновленияинформационнойбазы) (Процедура)
- [ПриДобавленииПараметровРаботыКлиентаПриЗапуске](#придобавлениипараметровработыклиентапризапуске) (Процедура)
- [ПриДобавленииПараметровРаботыКлиента](#придобавлениипараметровработыклиента) (Процедура)
- [ПроверитьСтатусОбновления](#проверитьстатусобновления) (Процедура)
- [ОбновлениеКонфигурацииУспешно](#обновлениеконфигурацииуспешно) (Функция)
- [ЗаписатьСтатусОбновления](#записатьстатусобновления) (Процедура)
- [СброситьСтатусОбновленияКонфигурации](#сброситьстатусобновленияконфигурации) (Процедура)
- [ЕстьПраваНаУстановкуОбновления](#естьправанаустановкуобновления) (Функция)
- [СобытиеЖурналаРегистрации](#событиежурналарегистрации) (Функция)
- [ВыполнитьОтложенныеОбработчики](#выполнитьотложенныеобработчики) (Функция)
- [СведенияОбОбновлении](#сведенияобобновлении) (Функция)

---

### <a name="исправленияизменены"></a>ИсправленияИзменены

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИсправленияИзменены(ТолькоПроверка = Ложь) Экспорт
  ```
- **Описание:**

  Удаляет устаревшие исправления, устанавливает корректные
  свойства новым исправлениям. Требуется вызывать перед выполнением обновления
  конфигурации в пакетном режиме (см. ОбновлениеИнформационнойБазы.ВыполнитьОбновлениеИнформационнойБазы).
  Важно: Выполненные действия применятся после перезапуска сеанса.

  Параметры:
   ТолькоПроверка - Булево - если Истина, устаревшие патчи не будут удаляться.

  Возвращаемое значение:
   Структура:
    * ЕстьИзменения     - Булево - истина, если были внесены изменения в составе исправлений.
    * ОписаниеИзменений - Строка - информация о удаленных и измененных патчах.

---

### <a name="настройкиобновленияконфигурации"></a>НастройкиОбновленияКонфигурации

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НастройкиОбновленияКонфигурации() Экспорт
  ```
- **Описание:**

  Получает настройки обновления конфигурации.

  Возвращаемое значение:
    Структура:
      * РежимОбновления - Число - для файловой базы 0, для клиент-серверной 2.
      * ДатаВремяОбновления - Дата - дата запланированного обновления конфигурации.
      * ВыслатьОтчетНаПочту - Булево - признак необходимости отправки на электронную почту отчета об обновлении.
      * АдресЭлектроннойПочты - Строка - адрес электронной почты для отправки отчета обновлении.
      * КодЗадачиПланировщика - Число - код задачи планировщика Windows.
      * ИмяФайлаОбновления - Строка - имя файла, содержащего устанавливаемое обновление.
      * ФайлыИсправлений - Массив из Строка
      * СоздаватьРезервнуюКопию - Число - признак необходимости создания резервной копии.
      * ИмяКаталогаРезервнойКопииИБ - Строка - каталог для создания резервной копии.
      * ВосстанавливатьИнформационнуюБазу - Булево - признак необходимости восстанавливать информационную базу
                                                     в случае возникновения ошибок в процессе обновления.

---

### <a name="сохранитьнастройкиобновленияконфигурации"></a>СохранитьНастройкиОбновленияКонфигурации

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СохранитьНастройкиОбновленияКонфигурации(Настройки) Экспорт
  ```
- **Описание:**

  Сохраняет настройки обновления конфигурации.

  Параметры:
     Настройки - см. ОбновлениеКонфигурации.НастройкиОбновленияКонфигурации

---

### <a name="установленныеисправления"></a>УстановленныеИсправления

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УстановленныеИсправления() Экспорт
  ```
- **Описание:**

  Возвращает информацию об установленных патчах в конфигурации.

  Возвращаемое значение:
   Массив - структуры с ключами:
      * Идентификатор - Строка - уникальный идентификатор исправления (патча).
                      - Неопределено - если установка исправления выполнялась
                                 в текущем сеансе и еще не был выполнен перезапуск.
      * Наименование  - Строка - наименование исправления.

---

### <a name="установкаиудалениеисправлений"></a>УстановкаИУдалениеИсправлений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция УстановкаИУдалениеИсправлений(Исправления, Знач ПараметрыУстановкиИсправлений = Неопределено, УдалитьОбновитьПараметрыРаботыРасширений = Истина, УдалитьПроверятьПрименимостьПоМанифесту = Ложь) Экспорт
  ```
- **Описание:**

  Производит установку/удаление патчей.

  Параметры:
   Исправления - Структура:
      * Установить - Массив - файлы исправлений во временном хранилище, которые требуется установить.
      * Удалить    - Массив - уникальные идентификаторы (Строка) исправлений, которые требуется удалить.
   ПараметрыУстановкиИсправлений - см. ПараметрыУстановкиИсправлений
                                 - Булево - устарел
   УдалитьОбновитьПараметрыРаботыРасширений - Булево - по умолчанию Истина, при вызове из скрипта обновления конфигурации
                          необходимо устанавливать в значение Ложь.
   УдалитьПроверятьПрименимостьПоМанифесту - Булево - следует передавать Истина при ручной установке патчей.

  Возвращаемое значение:
   Структура:
      * Установленные - Массив - имена (Строка) установленных исправлений.
      * НеУстановлено - Число - количество неустановленных исправлений.
      * НеУдалено     - Число - количество неудаленных исправлений.
      * Ошибки        - Массив из Структура:
           * НомерПатча - Строка - полное имя патча.
           * Событие    - Строка - событие, при котором возникла ошибка. Установка или Удаление.
           * Причина    - Строка - подробное описание ошибки.

---

### <a name="параметрыустановкиисправлений"></a>ПараметрыУстановкиИсправлений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыУстановкиИсправлений() Экспорт
  ```
- **Описание:**

  Функция-конструктор подготовки параметров для УстановкаИУдалениеИсправлений

  Возвращаемое значение:
    Структура:
      * ВФоне - Булево - необходимо устанавливать в значение Истина, если вызов функции выполняется фоновом задании.
      * ОбновитьПараметрыРаботыРасширений - Булево - по умолчанию Истина, при вызове из скрипта обновления конфигурации
                                                     необходимо устанавливать в значение Ложь.
      * ПроверятьПрименимостьПоМанифесту - Булево - следует передавать Истина при ручной установке патчей.
      * ИспользуетсяВРаспределеннойИнформационнойБазе - Булево - по умолчанию Истина

---

### <a name="предупреждатьоналичиирасширений"></a>ПредупреждатьОНаличииРасширений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредупреждатьОНаличииРасширений() Экспорт
  ```
- **Описание:**

  Проверка, показывающая есть ли расширения, требующие отображение 
  предупреждения об имеющихся расширениях.
  Проверяет наличие расширений не являющихся исправлениями (патчами).

  Возвращаемое значение:
   Булево - результат проверки.

---

### <a name="длятекущейверсиинеобходимоуспешноезавершениеобработчиков"></a>ДляТекущейВерсииНеобходимоУспешноеЗавершениеОбработчиков

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДляТекущейВерсииНеобходимоУспешноеЗавершениеОбработчиков() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="проверитьналичиеустаревшихисправлений"></a>ПроверитьНаличиеУстаревшихИсправлений

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверитьНаличиеУстаревшихИсправлений() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="описанияновыхпатчей"></a>ОписанияНовыхПатчей

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОписанияНовыхПатчей(АдресХранилища, ТекущиеИсправления, ДляОбновления = Ложь) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="завершитьобновление"></a>ЗавершитьОбновление

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗавершитьОбновление(Знач РезультатОбновления, Знач ЭлектроннаяПочта, Знач ИмяАдминистратораОбновления, Знач КаталогСкрипта = Неопределено) Экспорт
  ```
- **Описание:**

  Вызывается при завершении обновления конфигурации через COM-соединение.

  Параметры:
   РезультатОбновления  - Булево - результат обновления.
   ЭлектроннаяПочта  - Строка - адрес электронной почты, на который должен быть отправлен отчет о результатах.
   ИмяАдминистратораОбновления  - Строка - имя пользователя, который выполнил обновление программы.
   КаталогСкрипта  - Строка - полный путь папки с логом скрипта обновления.

---

### <a name="каталогскрипта"></a>КаталогСкрипта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогСкрипта() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="имяформыустановкиобновления"></a>ИмяФормыУстановкиОбновления

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяФормыУстановкиОбновления() Экспорт
  ```
- **Описание:**

  Возвращает полное имя основной формы обработки УстановкаОбновлений.

  Возвращаемое значение:
   Строка

---

### <a name="свойстваисправления"></a>СвойстваИсправления

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СвойстваИсправления(ИмяИсправления) Экспорт
  ```
- **Описание:**

  Зачитывает свойства исправления из макета. Имя макет должно совпадать с именем исправления.
  Формат макеты XML. Соответствует XDTO пакету Patch.

  Возвращаемое значение:
   ОбъектXDTO
   Неопределено - исправление еще не применено.
   Строка       - "ОшибкаЧтения", некорректный формат макета с описанием исправления.

---

### <a name="этоисправление"></a>ЭтоИсправление

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоИсправление(Расширение) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="обновитьисправленияизскрипта"></a>ОбновитьИсправленияИзСкрипта

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбновитьИсправленияИзСкрипта(НовыеИсправления, УдаляемыеИсправления) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="послеобновленияинформационнойбазы"></a>ПослеОбновленияИнформационнойБазы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПослеОбновленияИнформационнойБазы() Экспорт
  ```
- **Описание:**

  См. ОбновлениеИнформационнойБазыБСП.ПослеОбновленияИнформационнойБазы.

---

### <a name="придобавлениипараметровработыклиентапризапуске"></a>ПриДобавленииПараметровРаботыКлиентаПриЗапуске

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриДобавленииПараметровРаботыКлиентаПриЗапуске(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиентаПриЗапуске.

---

### <a name="придобавлениипараметровработыклиента"></a>ПриДобавленииПараметровРаботыКлиента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриДобавленииПараметровРаботыКлиента(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиента.

---

### <a name="проверитьстатусобновления"></a>ПроверитьСтатусОбновления

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверитьСтатусОбновления(РезультатОбновления, КаталогСкрипта, УстановленныеИсправления) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="обновлениеконфигурацииуспешно"></a>ОбновлениеКонфигурацииУспешно

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОбновлениеКонфигурацииУспешно(КаталогСкрипта = "", УстановленныеИсправления = "") Экспорт
  ```
- **Описание:**

  Возвращает признак успешного обновления конфигурации на основе данных константы настроек.

  Параметры:
   КаталогСкрипта  - Строка - полный путь папки с логом скрипта обновления.
   УстановленныеИсправления  - Строка.

  Возвращаемое значение:
   Булево
   Неопределено

---

### <a name="записатьстатусобновления"></a>ЗаписатьСтатусОбновления

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьСтатусОбновления(СтатусОбновления, СообщенияДляЖурналаРегистрации = Неопределено) Экспорт
  ```
- **Описание:**

  Устанавливает новое значение в константу настроек обновления
  соответственно успешности последней попытки обновления конфигурации.

---

### <a name="сброситьстатусобновленияконфигурации"></a>СброситьСтатусОбновленияКонфигурации

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СброситьСтатусОбновленияКонфигурации() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="естьправанаустановкуобновления"></a>ЕстьПраваНаУстановкуОбновления

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЕстьПраваНаУстановкуОбновления() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="событиежурналарегистрации"></a>СобытиеЖурналаРегистрации

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СобытиеЖурналаРегистрации() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="выполнитьотложенныеобработчики"></a>ВыполнитьОтложенныеОбработчики

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВыполнитьОтложенныеОбработчики() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="сведенияобобновлении"></a>СведенияОбОбновлении

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СведенияОбОбновлении(Знач ИмяФайлаПоставкиОбновления) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

