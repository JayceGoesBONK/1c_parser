# Модуль ПолучениеФайловИзИнтернетаСлужебный

**Описание подсистемы:** Получение файлов из Интернета служебный

Всего найдено экспортных методов: **10**

## Список методов

- [ПриДобавленииПараметровРаботыКлиента](#придобавлениипараметровработыклиента) (Процедура)
- [ПриВключенииИспользованияПрофилейБезопасности](#привключениииспользованияпрофилейбезопасности) (Процедура)
- [ПриЗаполненииРазрешенийНаДоступКВнешнимРесурсам](#призаполненииразрешенийнадоступквнешнимресурсам) (Процедура)
- [СохранитьНастройкиПроксиНаСервере1СПредприятие](#сохранитьнастройкипроксинасервере1спредприятие) (Процедура)
- [СкачатьФайл](#скачатьфайл) (Функция)
- [НовыйИнтернетПрокси](#новыйинтернетпрокси) (Функция)
- [СостояниеНастроекПрокси](#состояниенастроекпрокси) (Функция)
- [ПредставлениеМестаДиагностики](#представлениеместадиагностики) (Функция)
- [ПроверитьДоступностьСервера](#проверитьдоступностьсервера) (Функция)
- [ЖурналТрассировкиМаршрутаСервера](#журналтрассировкимаршрутасервера) (Функция)

---

### <a name="придобавлениипараметровработыклиента"></a>ПриДобавленииПараметровРаботыКлиента

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриДобавленииПараметровРаботыКлиента(Параметры) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияПереопределяемый.ПриДобавленииПараметровРаботыКлиента.

---

### <a name="привключениииспользованияпрофилейбезопасности"></a>ПриВключенииИспользованияПрофилейБезопасности

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриВключенииИспользованияПрофилейБезопасности() Экспорт
  ```
- **Описание:**

  См. РаботаВБезопасномРежимеПереопределяемый.ПриВключенииИспользованияПрофилейБезопасности.

---

### <a name="призаполненииразрешенийнадоступквнешнимресурсам"></a>ПриЗаполненииРазрешенийНаДоступКВнешнимРесурсам

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПриЗаполненииРазрешенийНаДоступКВнешнимРесурсам(ЗапросыРазрешений) Экспорт
  ```
- **Описание:**

  См. РаботаВБезопасномРежимеПереопределяемый.ПриЗаполненииРазрешенийНаДоступКВнешнимРесурсам.

---

### <a name="сохранитьнастройкипроксинасервере1спредприятие"></a>СохранитьНастройкиПроксиНаСервере1СПредприятие

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СохранитьНастройкиПроксиНаСервере1СПредприятие(Знач Настройки) Экспорт
  ```
- **Описание:**

  Сохраняет параметры настройки прокси сервера на стороне сервера 1С:Предприятие.

---

### <a name="скачатьфайл"></a>СкачатьФайл

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СкачатьФайл(Знач URL, Знач ПараметрыПолучения, Знач НастройкаСохранения, Знач ЗаписыватьОшибку = Истина) Экспорт
  ```
- **Описание:**

  Функция для получения файла из сети Интернет.

  Параметры:
    URL           - Строка - url файла.
    ПараметрыПолучения   - Структура:
     * ПутьДляСохранения            - Строка - путь на сервере (включая имя файла), для сохранения скачанного файла.
     * Пользователь                 - Строка - пользователь от имени которого установлено соединение.
     * Пароль                       - Строка - пароль пользователя от которого установлено соединение.
     * Порт                         - Число  - порт сервера с которым установлено соединение.
     * Таймаут                      - Число  - таймаут на получение файла, в секундах.
     * ЗащищенноеСоединение         - Булево - для случая http загрузки флаг указывает,
                                              что соединение должно производиться через https.
     * ПассивноеСоединение          - Булево - для случая ftp загрузки флаг указывает,
                                              что соединение должно пассивным (или активным).
     * Заголовки                    - Соответствие - см. описание параметра Заголовки объекта HTTPЗапрос.
     * ИспользоватьАутентификациюОС - Булево - см. описание параметра ИспользоватьАутентификациюОС объекта HTTPСоединение.
     * ПроверятьДоставкуПакетовПриОшибке - см. ПолучениеФайловИзИнтернета.ДиагностикаСоединения.ПроверятьДоставкуПакетов.
     

    НастройкаСохранения - Соответствие - содержит параметры для сохранения скачанного файла. Ключи:
                  МестоХранения - Строка - может содержать
                         "Сервер" - сервер,
                         "ВременноеХранилище" - временное хранилище.
                  Путь - строка (необязательный параметр) -
                         путь к каталогу на клиенте либо на сервере либо адрес во временном хранилище
                         если не задано будет сгенерировано автоматически.
    ЗаписыватьОшибку - Булево                     

  Возвращаемое значение:
    Структура:
       * Статус - Булево
       * Путь   - Строка
       * СообщениеОбОшибке - Строка
       * Заголовки         - Соответствие
       * КодСостояния      - Число

---

### <a name="новыйинтернетпрокси"></a>НовыйИнтернетПрокси

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НовыйИнтернетПрокси(НастройкаПроксиСервера, URLИлиПротокол) Экспорт
  ```
- **Описание:**

  Возвращает прокси по настройкам НастройкаПроксиСервера для заданного протокола Протокол.

  Параметры:
    НастройкаПроксиСервера - Соответствие из КлючИЗначение:
     * Ключ - Строка - см. ниже список возможных ключей.
     * Значение - Произвольный
     ИспользоватьПрокси - Булево - использовать ли прокси-сервер.
     НеИспользоватьПроксиДляЛокальныхАдресов - использовать ли прокси-сервер для локальных адресов.
     НеИспользоватьПроксиДляАдресов - Массив из Строка
     ИспользоватьСистемныеНастройки - Булево - использовать ли системные настройки прокси-сервера.
     Сервер       - Строка - адрес прокси-сервера.
     Порт         - Строка - порт прокси-сервера.
     Пользователь - Строка - имя пользователя для авторизации на прокси-сервере.
     Пароль       - Строка - пароль пользователя.
     ИспользоватьАутентификациюОС - Булево - признак использования аутентификации средствами операционной системы.
    URLИлиПротокол - Строка - адрес ресурса или протокол, для которого устанавливаются параметры прокси сервера,
                              например "https://1c.ru", "http", "https", "ftp", "ftps".

  Возвращаемое значение:
    ИнтернетПрокси

---

### <a name="состояниенастроекпрокси"></a>СостояниеНастроекПрокси

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СостояниеНастроекПрокси(Знач Протокол = Неопределено) Экспорт
  ```
- **Описание:**

  Служебная информация для отображения текущих настроек и состояний прокси для выполнения диагностики.

  Параметры:
   Протокол - Строка - протокол, для которого необходимо получить настройки прокси.

  Возвращаемое значение:
   Структура:
      * СоединениеЧерезПрокси - Булево - признак того, что соединение должно выполняться через прокси.
      * Представление - Строка - представление текущего настроенного прокси.

---

### <a name="представлениеместадиагностики"></a>ПредставлениеМестаДиагностики

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредставлениеМестаДиагностики() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="проверитьдоступностьсервера"></a>ПроверитьДоступностьСервера

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПроверитьДоступностьСервера(АдресСервера) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="журналтрассировкимаршрутасервера"></a>ЖурналТрассировкиМаршрутаСервера

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЖурналТрассировкиМаршрутаСервера(АдресСервера) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

