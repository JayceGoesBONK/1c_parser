# Модуль СтандартныеПодсистемыВызовСервера

**Описание подсистемы:** Стандартные подсистемы вызов сервера

Всего найдено экспортных методов: **17**

## Список методов

- [СкрытьРабочийСтолПриНачалеРаботыСистемы](#скрытьрабочийстолприначалеработысистемы) (Процедура)
- [РасчетныеПоказателиЯчеек](#расчетныепоказателиячеек) (Функция)
- [ОбработатьОтветПроОтключениеOpenIDConnect](#обработатьответпроотключениеopenidconnect) (Процедура)
- [РолиТекущегоПользователяСокращены](#ролитекущегопользователясокращены) (Функция)
- [ПараметрыРаботыКлиентаПриЗапуске](#параметрыработыклиентапризапуске) (Функция)
- [ПараметрыРаботыКлиента](#параметрыработыклиента) (Функция)
- [ВойтиВОбластьДанных](#войтивобластьданных) (Процедура)
- [ПроверитьПравоОтключитьЛогикуНачалаРаботыСистемы](#проверитьправоотключитьлогикуначалаработысистемы) (Процедура)
- [ЗаписатьОшибкуВЖурналРегистрацииПриЗапускеИлиЗавершении](#записатьошибкувжурналрегистрациипризапускеилизавершении) (Процедура)
- [ПолноеИмяОбъектаМетаданных](#полноеимяобъектаметаданных) (Функция)
- [ИмяОбъектаМетаданных](#имяобъектаметаданных) (Функция)
- [ВерсияБиблиотеки](#версиябиблиотеки) (Функция)
- [ИменаКлиентскихМодулей](#именаклиентскихмодулей) (Функция)
- [СсылкиПоИменамПредопределенных](#ссылкипоименампредопределенных) (Функция)
- [ПредставлениеИдентификатораОбъектаМетаданных](#представлениеидентификатораобъектаметаданных) (Функция)
- [КомпонентаПоследнейВерсии](#компонентапоследнейверсии) (Функция)
- [ВремяПерезапускаПриложенияДляПримененияИсправлений](#времяперезапускаприложениядляпримененияисправлений) (Функция)

---

### <a name="скрытьрабочийстолприначалеработысистемы"></a>СкрытьРабочийСтолПриНачалеРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СкрытьРабочийСтолПриНачалеРаботыСистемы(Скрыть = Истина) Экспорт
  ```
- **Описание:**

  Устанавливает состояние отмены при создании форм рабочего стола.
  Требуется, если при запуске программы возникает необходимость
  взаимодействия с пользователем (интерактивная обработка).

  Используется из одноименной процедуры в модуле СтандартныеПодсистемыКлиент.
  Прямой вызов на сервере имеет смысл для сокращения серверных вызовов,
  если при подготовке параметров клиента через модуль ПовтИсп уже
  известно, что интерактивная обработка требуется.

  Если прямой вызов сделан из процедуры получения параметров клиент,
  то состояние на клиенте будет обновлено автоматически, в другом случае
  это нужно сделать самостоятельно на клиенте с помощью одноименной процедуры
  в модуле СтандартныеПодсистемыКлиент.

  Параметры:
   Скрыть - Булево - если установить Истина, состояние будет установлено,
            если установить Ложь, состояние будет снято (после этого
            можно выполнить метод ОбновитьИнтерфейс и формы рабочего
            стола будут пересозданы).

---

### <a name="расчетныепоказателиячеек"></a>РасчетныеПоказателиЯчеек

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РасчетныеПоказателиЯчеек(Знач ТабличныйДокумент, ВыделенныеОбласти, УникальныйИдентификатор = Неопределено) Экспорт
  ```
- **Описание:**

  См. ОбщегоНазначенияСлужебныйКлиентСервер.РасчетныеПоказателиЯчеек.

---

### <a name="обработатьответпроотключениеopenidconnect"></a>ОбработатьОтветПроОтключениеOpenIDConnect

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ОбработатьОтветПроОтключениеOpenIDConnect(Отключить) Экспорт
  ```
- **Описание:**

  См. РегистрыСведений.СведенияОПользователях.ОбработатьОтветПроОтключениеOpenIDConnect

---

### <a name="ролитекущегопользователясокращены"></a>РолиТекущегоПользователяСокращены

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РолиТекущегоПользователяСокращены() Экспорт
  ```
- **Описание:**

  См. ПользователиСлужебный.РолиТекущегоПользователяСокращены

---

### <a name="параметрыработыклиентапризапуске"></a>ПараметрыРаботыКлиентаПриЗапуске

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыРаботыКлиентаПриЗапуске(Параметры) Экспорт
  ```
- **Описание:**

  Возвращает структуру параметров, необходимых для работы клиентского кода конфигурации
  при запуске, т.е. в обработчиках событий ПередНачаломРаботыСистемы, ПриНачалеРаботыСистемы.

  Только для вызова из СтандартныеПодсистемыКлиентПовтИсп.ПараметрыРаботыКлиентаПриЗапуске.

---

### <a name="параметрыработыклиента"></a>ПараметрыРаботыКлиента

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыРаботыКлиента(СвойстваКлиента) Экспорт
  ```
- **Описание:**

  Возвращает структуру параметров, необходимых для работы клиентского кода конфигурации. 
  Только для вызова из СтандартныеПодсистемыКлиентПовтИсп.ПараметрыРаботыКлиента.

---

### <a name="войтивобластьданных"></a>ВойтиВОбластьДанных

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ВойтиВОбластьДанных(Знач ОбластьДанных) Экспорт
  ```
- **Описание:**

  См. РаботаВМоделиСервиса.ВойтиВОбластьДанных.

---

### <a name="проверитьправоотключитьлогикуначалаработысистемы"></a>ПроверитьПравоОтключитьЛогикуНачалаРаботыСистемы

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ПроверитьПравоОтключитьЛогикуНачалаРаботыСистемы(СвойстваКлиента) Экспорт
  ```
- **Описание:**

  Проверяет право на отключение логики работы системы и
  скрывает рабочий стол на сервере, если право есть,
  в противном случае вызывает исключение.

---

### <a name="записатьошибкувжурналрегистрациипризапускеилизавершении"></a>ЗаписатьОшибкуВЖурналРегистрацииПриЗапускеИлиЗавершении

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьОшибкуВЖурналРегистрацииПриЗапускеИлиЗавершении(ПрекратитьРаботу, Знач Событие, Знач ТекстОшибки) Экспорт
  ```
- **Описание:**

  Только для внутреннего использования.

---

### <a name="полноеимяобъектаметаданных"></a>ПолноеИмяОбъектаМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПолноеИмяОбъектаМетаданных(Тип) Экспорт
  ```
- **Описание:**

  Возвращает полное имя объекта метаданных по его типу.

---

### <a name="имяобъектаметаданных"></a>ИмяОбъектаМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяОбъектаМетаданных(Тип) Экспорт
  ```
- **Описание:**

  Возвращает имя объекта метаданных по типу.

  Параметры:
   Источник - Тип - объект.

  Возвращаемое значение:
    Строка

---

### <a name="версиябиблиотеки"></a>ВерсияБиблиотеки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВерсияБиблиотеки() Экспорт
  ```
- **Описание:**

  См. СтандартныеПодсистемыСервер.ВерсияБиблиотеки.

---

### <a name="именаклиентскихмодулей"></a>ИменаКлиентскихМодулей

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИменаКлиентскихМодулей() Экспорт
  ```
- **Описание:**

  Возвращаемое значение:
   Массив из Строка

---

### <a name="ссылкипоименампредопределенных"></a>СсылкиПоИменамПредопределенных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СсылкиПоИменамПредопределенных(ПолноеИмяОбъектаМетаданных) Экспорт
  ```
- **Описание:**

  См. СтандартныеПодсистемыПовтИсп.СсылкиПоИменамПредопределенных

---

### <a name="представлениеидентификатораобъектаметаданных"></a>ПредставлениеИдентификатораОбъектаМетаданных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПредставлениеИдентификатораОбъектаМетаданных(Ссылка) Экспорт
  ```
- **Описание:**

  См. Справочники.ИдентификаторыОбъектовМетаданных.ПредставлениеИдентификатора

---

### <a name="компонентапоследнейверсии"></a>КомпонентаПоследнейВерсии

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КомпонентаПоследнейВерсии(Идентификатор, Местоположение, ВнешняяКомпонента) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="времяперезапускаприложениядляпримененияисправлений"></a>ВремяПерезапускаПриложенияДляПримененияИсправлений

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВремяПерезапускаПриложенияДляПримененияИсправлений() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

