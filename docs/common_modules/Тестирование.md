# Модуль Тестирование

**Описание подсистемы:** Тестирование

Всего найдено экспортных методов: **58**

## Список методов

- [ПодготовитьПараметрыДляСозданияБазыИзФайлаВыгрузки](#подготовитьпараметрыдлясозданиябазыизфайлавыгрузки) (Функция)
- [КаталогЛокальнойБазы](#каталоглокальнойбазы) (Функция)
- [ФреймворкДляЗапускаТестов](#фреймворкдлязапускатестов) (Функция)
- [БиблиотекиТестов](#библиотекитестов) (Функция)
- [РепозиторийТестов](#репозиторийтестов) (Функция)
- [КомандаЗапускаВнешнегоТекстовогоРедактора](#командазапускавнешнеготекстовогоредактора) (Функция)
- [НастройкиБДДляЗапускаТестовИзСценария](#настройкибддлязапускатестовизсценария) (Функция)
- [НастройкиВыгрузкиТестов](#настройкивыгрузкитестов) (Функция)
- [ДанныеПользователейЭталоннойБазы](#данныепользователейэталоннойбазы) (Функция)
- [НастройкиПоказыватьОбычныйТекстВРедактореСценария](#настройкипоказыватьобычныйтекствредакторесценария) (Функция)
- [СохранитьНастройкиЗапускаБазыTestClient](#сохранитьнастройкизапускабазыtestclient) (Процедура)
- [СохранитьНастройкиБДДляЗапускаТестовИзСценария](#сохранитьнастройкибддлязапускатестовизсценария) (Процедура)
- [СохранитьНастройкиВыгрузкиТестов](#сохранитьнастройкивыгрузкитестов) (Процедура)
- [КаталогДляДанныхТестирования](#каталогдляданныхтестирования) (Функция)
- [ТекстСценария](#текстсценария) (Функция)
- [ТекстПроцесса](#текстпроцесса) (Функция)
- [ИмяПрофиляTestClient](#имяпрофиляtestclient) (Функция)
- [ВерсииСценариевБД](#версиисценариевбд) (Функция)
- [ДанныеДляПодключенияКБДTestClient](#данныедляподключениякбдtestclient) (Функция)
- [ЗаменитьСтрокуПодключенияПрофиляTestClient](#заменитьстрокуподключенияпрофиляtestclient) (Процедура)
- [ОбработкаСлужебныхСловНаДругихЯзыках](#обработкаслужебныхсловнадругихязыках) (Функция)
- [ДополнитьВложенныеСценарии](#дополнитьвложенныесценарии) (Процедура)
- [ДанныеФайлов](#данныефайлов) (Функция)
- [ПараметрыСценария](#параметрысценария) (Функция)
- [ПараметрыШаговПроцессаПоСценарию](#параметрышаговпроцессапосценарию) (Функция)
- [ПараметрыПроцесса](#параметрыпроцесса) (Функция)
- [СценарииПроцесса](#сценариипроцесса) (Функция)
- [ОтформатированныеТекстыСценариевПроцесса](#отформатированныетекстысценариевпроцесса) (Функция)
- [ТекстыСценариевПроцесса](#текстысценариевпроцесса) (Функция)
- [ПараметрыЗапускаНастройкиСценарияТестирования](#параметрызапусканастройкисценариятестирования) (Функция)
- [ДанныеИерархииПоДаннымGit](#данныеиерархииподаннымgit) (Функция)
- [ДанныеСценарияИзРепозитория](#данныесценарияизрепозитория) (Функция)
- [ДанныеНастройкиСценарияИзРепозитория](#данныенастройкисценарияизрепозитория) (Функция)
- [УстановитьТекстВФорматированныйДокумент](#установитьтекствформатированныйдокумент) (Процедура)
- [ПараметрыСценарияИзТекстовыхДанных](#параметрысценарияизтекстовыхданных) (Функция)
- [ТегиСценарияИзТекстовыхДанных](#тегисценарияизтекстовыхданных) (Функция)
- [ВызывающиеСценарииТекстовыхДанных](#вызывающиесценариитекстовыхданных) (Функция)
- [ВложенныеСценарииТекстовыхДанных](#вложенныесценариитекстовыхданных) (Функция)
- [ЗаполнитьПараметрыВходящиеИзТекстовыхДанных](#заполнитьпараметрывходящиеизтекстовыхданных) (Процедура)
- [ДанныеФайлаЭталонныхБаз](#данныефайлаэталонныхбаз) (Функция)
- [ФиксироватьИзменения](#фиксироватьизменения) (Функция)
- [ФайлыСценарияИзРепозитория](#файлысценарияизрепозитория) (Функция)
- [ДанныеНастроекСценарияИзРепозитория](#данныенастроексценарияизрепозитория) (Функция)
- [ДанныеПодключенияПроектаКGitСерверу](#данныеподключенияпроектакgitсерверу) (Функция)
- [ДвоичныеДанныеФайлаИзРепозитория](#двоичныеданныефайлаизрепозитория) (Функция)
- [ЗаписатьНовыйСценарийВКеш](#записатьновыйсценарийвкеш) (Процедура)
- [ДанныеДляВыгрузки](#данныедлявыгрузки) (Функция)
- [СлужебныеДанныеСценариев](#служебныеданныесценариев) (Функция)
- [НовыйЗапросНаСлияние](#новыйзапроснаслияние) (Функция)
- [ЗакрытьЗапросНаСлияние](#закрытьзапроснаслияние) (Функция)
- [СостояниеТестированияВетки](#состояниетестированияветки) (Функция)
- [ДобавитьСообщениеПоОбъекту](#добавитьсообщениепообъекту) (Процедура)
- [ДобавитьНеКонтекстноеСообщение](#добавитьнеконтекстноесообщение) (Процедура)
- [СобщенияПоОбъектуБезОтбораПоЗаголовку](#собщенияпообъектубезотборапозаголовку) (Функция)
- [GitLabСоединение](#gitlabсоединение) (Функция)
- [ГиперссылкаНаЗапросНаСлияние](#гиперссылканазапроснаслияние) (Функция)
- [ЭкранироватьСпецсимволыWeb](#экранироватьспецсимволыweb) (Функция)
- [НеКонтекстныеСобщенияБезОтбораПоЗаголовку](#неконтекстныесобщениябезотборапозаголовку) (Функция)

---

### <a name="подготовитьпараметрыдлясозданиябазыизфайлавыгрузки"></a>ПодготовитьПараметрыДляСозданияБазыИзФайлаВыгрузки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПодготовитьПараметрыДляСозданияБазыИзФайлаВыгрузки(ЭталоннаяБаза) Экспорт
  ```
- **Описание:**

  Получает параметры создания базы из настроек.

  Параметры:
   ЭталоннаяБаза - СправочникСсылка.ЭталонныеБазы - Ссылка на эталон из которого будут взяты настройки

  Возвращаемое значение:
   Структура

---

### <a name="каталоглокальнойбазы"></a>КаталогЛокальнойБазы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогЛокальнойБазы() Экспорт
  ```
- **Описание:**

  Получает каталог локальной базы из настроек

  Возвращаемое значение:
   Cтрока - Расположение локальной копии базы.

---

### <a name="фреймворкдлязапускатестов"></a>ФреймворкДляЗапускаТестов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ФреймворкДляЗапускаТестов() Экспорт
  ```
- **Описание:**

  Возвращает путь к фреймворку для запуска тестов

  Возвращаемое значение:
   Cтрока - Путь к обработке.

---

### <a name="библиотекитестов"></a>БиблиотекиТестов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция БиблиотекиТестов() Экспорт
  ```
- **Описание:**

  Возвращает пути к библиотекам тестов

  Возвращаемое значение:
   Массив - Путь к библиотекам тестов.

---

### <a name="репозиторийтестов"></a>РепозиторийТестов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция РепозиторийТестов() Экспорт
  ```
- **Описание:**

  Возвращает путь к репозиторию тестов

  Возвращаемое значение:
   Сстрока - Путь к репозиторию.

---

### <a name="командазапускавнешнеготекстовогоредактора"></a>КомандаЗапускаВнешнегоТекстовогоРедактора

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КомандаЗапускаВнешнегоТекстовогоРедактора() Экспорт
  ```
- **Описание:**

  Возвращает значение команды для запуска внешнего текстового редактора

  Возвращаемое значение:
   Строка - Значение команды.

---

### <a name="настройкибддлязапускатестовизсценария"></a>НастройкиБДДляЗапускаТестовИзСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НастройкиБДДляЗапускаТестовИзСценария() Экспорт
  ```
- **Описание:**

  Возвращает настройки БД для запуска тестов

  Возвращаемое значение:
   Структура - Набор настроек

---

### <a name="настройкивыгрузкитестов"></a>НастройкиВыгрузкиТестов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НастройкиВыгрузкиТестов() Экспорт
  ```
- **Описание:**

  Возвращает настройки для выгрузки тестов

  Возвращаемое значение:
   Структура - Набор настроек

---

### <a name="данныепользователейэталоннойбазы"></a>ДанныеПользователейЭталоннойБазы

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеПользователейЭталоннойБазы(ЭталоннаяБазаТестирования) Экспорт
  ```
- **Описание:**

  Возвращает соответствие по данным переданной базы


  Параметры:
   ЭталоннаяБазаТестирования - Справочник.ЭталонныеБазыТестирования

  Возвращаемое значение:
   Структура - Набор настроек

---

### <a name="настройкипоказыватьобычныйтекствредакторесценария"></a>НастройкиПоказыватьОбычныйТекстВРедактореСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НастройкиПоказыватьОбычныйТекстВРедактореСценария() Экспорт
  ```
- **Описание:**

  Возвращает значение настройки: показывать обычный текст или форматированный документ

  Возвращаемое значение:
   Булево

---

### <a name="сохранитьнастройкизапускабазыtestclient"></a>СохранитьНастройкиЗапускаБазыTestClient

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СохранитьНастройкиЗапускаБазыTestClient(Настройки) Экспорт
  ```
- **Описание:**

  Сохраняет настройки БД TestClient для запуска тестов

  Параметры:
   Структура - Настройки

---

### <a name="сохранитьнастройкибддлязапускатестовизсценария"></a>СохранитьНастройкиБДДляЗапускаТестовИзСценария

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СохранитьНастройкиБДДляЗапускаТестовИзСценария(Настройки) Экспорт
  ```
- **Описание:**

  Сохраняет настройки БД для запуска тестов

  Параметры:
   Структура - Настройки

---

### <a name="сохранитьнастройкивыгрузкитестов"></a>СохранитьНастройкиВыгрузкиТестов

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура СохранитьНастройкиВыгрузкиТестов(Настройки) Экспорт
  ```
- **Описание:**

  Сохраняет настройки для выгрузки тестов

  Параметры:
   Структура - Настройки

---

### <a name="каталогдляданныхтестирования"></a>КаталогДляДанныхТестирования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция КаталогДляДанныхТестирования() Экспорт
  ```
- **Описание:**

  Возвращает значение каталога данных, в который выгружаются данные для запуска сценария.

  Возвращаемое значение:
    Строка

---

### <a name="текстсценария"></a>ТекстСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТекстСценария(Параметры) Экспорт
  ```
- **Описание:**

  Возвращает тест сценария по переданным параметрам

  Параметры:
   Параметры - Структура

  Возвращаемое значение:
    Строка

---

### <a name="текстпроцесса"></a>ТекстПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТекстПроцесса(Параметры) Экспорт
  ```
- **Описание:**

  Возвращает тест процесса по переданным параметрам

  Параметры:
   Параметры - Структура

  Возвращаемое значение:
    Строка

---

### <a name="имяпрофиляtestclient"></a>ИмяПрофиляTestClient

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ИмяПрофиляTestClient(Роль,НастройкиБДДляЗапускаТестовИзСценария,ЭкранироватьСпецСимволы) Экспорт
  ```
- **Описание:**

  Возвращает имя профиля для подключения TestClient

  Параметры:
   Роль - СправочникСсылка.ПрофилиПользователей
   НастройкиБДДляЗапускаТестовИзСценария - Структура
   ЭкранироватьСпецСимволы - Булево

  Возвращаемое значение:
    Строка

---

### <a name="версиисценариевбд"></a>ВерсииСценариевБД

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВерсииСценариевБД(Сценарии) Экспорт
  ```
- **Описание:**

  Возвращает таблицу сценариев с их версиями

  Параметры:
   Сценарии - Массив - Содержит элементы справочника Сценарии.

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="данныедляподключениякбдtestclient"></a>ДанныеДляПодключенияКБДTestClient

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеДляПодключенияКБДTestClient(ПрофильПользователя,БазаДанных) Экспорт
  ```
- **Описание:**

  Возвращает данные для подключения TestClient

  Параметры:
   ПрофильПользователя - СправочникСсылка.ПрофилиПользователей
   БазаДанных - СправочникСсылка.ЭталонныеБазы

  Возвращаемое значение:
   - Структура
   - Неопределено

---

### <a name="заменитьстрокуподключенияпрофиляtestclient"></a>ЗаменитьСтрокуПодключенияПрофиляTestClient

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаменитьСтрокуПодключенияПрофиляTestClient(ТекстСценариев,НастройкиБДДляЗапускаТестовИзСценария,Роль) Экспорт
  ```
- **Описание:**

  Подставляет в текст сценария строку подключения TestClient

  Параметры:
   ТекстСценариев - Строка
   НастройкиБДДляЗапускаТестовИзСценария - Структура
   СправочникСсылка.ПрофилиПользователей - Роль

---

### <a name="обработкаслужебныхсловнадругихязыках"></a>ОбработкаСлужебныхСловНаДругихЯзыках

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОбработкаСлужебныхСловНаДругихЯзыках(Текст) Экспорт
  ```
- **Описание:**

  Заменяет служебные слова в тексте сценария на нужный язык

  Параметры:
   Текст - Строка

  Возвращаемое значение:
    Строка

---

### <a name="дополнитьвложенныесценарии"></a>ДополнитьВложенныеСценарии

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДополнитьВложенныеСценарии(Массив,Сценарий) Экспорт
  ```
- **Описание:**

  Добавляет файлы в переданный массив по указанному сценарию

  Параметры:
   Массив - Массив
   Справочник.СценарииРаботыПользователей - Сценарий

---

### <a name="данныефайлов"></a>ДанныеФайлов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеФайлов(МассивВладельцев) Экспорт
  ```
- **Описание:**

  Получает двоичные данные файлов 

  Параметры:
   МассивВладельцев - Массив

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="параметрысценария"></a>ПараметрыСценария

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыСценария(Сценарий) Экспорт
  ```
- **Описание:**

  Получает данные по параметрам сценария

  Параметры:
   Сценарий - Справочник.СценарииРаботыПользователей

  Возвращаемое значение:
   Массив

---

### <a name="параметрышаговпроцессапосценарию"></a>ПараметрыШаговПроцессаПоСценарию

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыШаговПроцессаПоСценарию(Процесс,ШагПроцесса) Экспорт
  ```
- **Описание:**

  Возвращает таблицу параметров, которые возможно выбрать по ссылке для данного шага процесса

  Параметры:
   Процесс - Справочник.Процессы
   ШагПроцесса - Справочник.ШагиПроцесса

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="параметрыпроцесса"></a>ПараметрыПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыПроцесса(Процесс) Экспорт
  ```
- **Описание:**

  Возвращает таблицу параметров, которые находятся на вкладке "Параметры" процесса

  Параметры:
   Процесс - Справочник.Процессы

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="сценариипроцесса"></a>СценарииПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СценарииПроцесса(Процесс) Экспорт
  ```
- **Описание:**

  Возвращает упорядоченную таблицу сценариев, которые используются в процессе

  Параметры:
   Процесс - Справочник.Процессы

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="отформатированныетекстысценариевпроцесса"></a>ОтформатированныеТекстыСценариевПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОтформатированныеТекстыСценариевПроцесса(ДополнительныеПараметры,МассивРолей,Процесс) Экспорт
  ```
- **Описание:**

  Возвращает отформатированный текст процесса

  Параметры:
   ДополнительныеПараметры - Структура
   МассивРолей - Массив
   Процесс - Справочники.Процессы

  Возвращаемое значение:
   Строка

---

### <a name="текстысценариевпроцесса"></a>ТекстыСценариевПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТекстыСценариевПроцесса(ПараметрыФормированияТекстаСценария,Процесс) Экспорт
  ```
- **Описание:**

  Возвращает отформатированный текст процесса

  Параметры:
   ПараметрыФормированияТекстаСценария - Структура
   Процесс - Справочники.Процессы

  Возвращаемое значение:
   Строка

---

### <a name="параметрызапусканастройкисценариятестирования"></a>ПараметрыЗапускаНастройкиСценарияТестирования

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыЗапускаНастройкиСценарияТестирования(ПараметрыНастройки, СценарийРаботыПользователя) Экспорт
  ```
- **Описание:**

  Возвращает набор параметров для запуска данной настройки сценария тестирования

  Параметры:
   ПараметрыНастройки - ТаблицаЗначений
   СценарийРаботыПользователя - Справочники.СценарииРаботыПользователей

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="данныеиерархииподаннымgit"></a>ДанныеИерархииПоДаннымGit

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеИерархииПоДаннымGit(Проект, ТокенПользователя, Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные иерархии сценариев по данным репозитория
  Параметры:
   Проект - Справочники.Проекты
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   ДвоичныеДанные

---

### <a name="данныесценарияизрепозитория"></a>ДанныеСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеСценарияИзРепозитория(Проект, ДанныеСценария, ТокенПользователя,  Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные сценария работы пользователя из репозитория
  Параметры:
   Проект - Справочники.Проекты
   ДанныеСценария - Структура
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   Структура

---

### <a name="данныенастройкисценарияизрепозитория"></a>ДанныеНастройкиСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастройкиСценарияИзРепозитория(Проект, ДанныеНастройки, ТокенПользователя,  Ветка, ИдентификаторКоммита) Экспорт
  ```
- **Описание:**

  Возвращает данные настройки сценария работы пользователя из репозитория
  Параметры:
   Проект - Справочники.Проекты
   ДанныеНастройки - Структура
   ТокенПользователя - Строка
   Ветка - Справочник.Ветки
   ИдентификаторКоммита - Строка

  Возвращаемое значение:
   Структура

---

### <a name="установитьтекствформатированныйдокумент"></a>УстановитьТекстВФорматированныйДокумент

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура УстановитьТекстВФорматированныйДокумент(Текст, ФорматированныйДокумент) Экспорт
  ```
- **Описание:**

  Устанавливает текст сценария пользователя в форматированный документ
  Параметры:
   Текст - Строка
   ФорматированныйДокумент - ФорматированныйДокумент

---

### <a name="параметрысценарияизтекстовыхданных"></a>ПараметрыСценарияИзТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыСценарияИзТекстовыхДанных(МассивДанных) Экспорт
  ```
- **Описание:**

  Возвращает таблицу параметров сценария по данным, прочитанным из yaml файла
  Параметры:
   МассивДанных - Массив

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="тегисценарияизтекстовыхданных"></a>ТегиСценарияИзТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ТегиСценарияИзТекстовыхДанных(МассивДанных) Экспорт
  ```
- **Описание:**

  Возвращает таблицу тегов сценария по данным, прочитанным из yaml файла
  Параметры:
   МассивДанных - Массив

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="вызывающиесценариитекстовыхданных"></a>ВызывающиеСценарииТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВызывающиеСценарииТекстовыхДанных(МассивДанных) Экспорт
  ```
- **Описание:**

  Возвращает таблицу вызывающих сценариев по данным, прочитанным из yaml файла
  Параметры:
   МассивДанных - Массив

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="вложенныесценариитекстовыхданных"></a>ВложенныеСценарииТекстовыхДанных

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВложенныеСценарииТекстовыхДанных(МассивДанных) Экспорт
  ```
- **Описание:**

  Возвращает таблицу вложенных сценариев по данным, прочитанным из yaml файла
  Параметры:
   МассивДанных - Массив

  Возвращаемое значение:
   ТаблицаЗначений

---

### <a name="заполнитьпараметрывходящиеизтекстовыхданных"></a>ЗаполнитьПараметрыВходящиеИзТекстовыхДанных

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьПараметрыВходящиеИзТекстовыхДанных(ПараметрыВходящие, ПереданныеПараметрыВходящие) Экспорт
  ```
- **Описание:**

  Заполняет таблицу параметров сценария по переданным данным, прочитанным из yaml файла
  Параметры:
   ПараметрыВходящие - ТаблицаЗначений
   ПереданныеПараметрыВходящие - Массив

---

### <a name="данныефайлаэталонныхбаз"></a>ДанныеФайлаЭталонныхБаз

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеФайлаЭталонныхБаз(ДанныеИзмененныхСценариев, ДанныеПодключения, ДанныеФиксацииИзменений) Экспорт
  ```
- **Описание:**

  Возвращает данные файла эталонных баз, дополняя его данными эталонных баз СППР
  Параметры:
   ДанныеИзмененныхСценариев - Массив
   ДанныеПодключения - Структура
   ДанныеФиксацииИзменений - Структура

  Возвращаемое значение:
   Структура

---

### <a name="фиксироватьизменения"></a>ФиксироватьИзменения

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ФиксироватьИзменения(ДанныеИзмененныхСценариев, ДанныеПодключения, ДанныеФиксацииИзменений) Экспорт
  ```
- **Описание:**

  Фиксирует изменения в системе контроля версий
  Параметры:
   ДанныеИзмененныхСценариев - Массив
   ДанныеПодключения - Структура
   ДанныеФиксацииИзменений - Структура

  Возвращаемое значение:
   Структура

---

### <a name="файлысценарияизрепозитория"></a>ФайлыСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ФайлыСценарияИзРепозитория(ДанныеДляПолученияФайлов) Экспорт
  ```
- **Описание:**

  Возвращает двоичные данные файлов сценария работы пользователя из репозитория
  Параметры:
   ДанныеДляПолученияФайлов - Структура

  Возвращаемое значение:
   Массив

---

### <a name="данныенастроексценарияизрепозитория"></a>ДанныеНастроекСценарияИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеНастроекСценарияИзРепозитория(ДанныеДляПолучения) Экспорт
  ```
- **Описание:**

  Возвращает данные настроек сценария работы пользователя из репозитория
  Параметры:
   ДанныеДляПолучения - Структура

  Возвращаемое значение:
   Массив

---

### <a name="данныеподключенияпроектакgitсерверу"></a>ДанныеПодключенияПроектаКGitСерверу

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеПодключенияПроектаКGitСерверу(Проект) Экспорт
  ```
- **Описание:**

  Возвращает данные проекта для работы с git сервером
  Параметры:
   Проект - Справочник.Проекты

  Возвращаемое значение:
   Структура

---

### <a name="двоичныеданныефайлаизрепозитория"></a>ДвоичныеДанныеФайлаИзРепозитория

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДвоичныеДанныеФайлаИзРепозитория(ДанныеДляПолученияФайла) Экспорт
  ```
- **Описание:**

  Возвращает двоичные данные файла из репозитория
  Параметры:
   ДанныеДляПолученияФайла - Структура

  Возвращаемое значение:
   ДвоичныеДанные

---

### <a name="записатьновыйсценарийвкеш"></a>ЗаписатьНовыйСценарийВКеш

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаписатьНовыйСценарийВКеш(ДанныеСценария, АдресСлужебныеДанныеСценариев) Экспорт
  ```
- **Описание:**

  Возвращает двоичные данные файла из репозитория
  Параметры:
   ДанныеСценария - Структура
   АдресСлужебныеДанныеСценариев - Строка

---

### <a name="данныедлявыгрузки"></a>ДанныеДляВыгрузки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеДляВыгрузки(МассивТестов) Экспорт
  ```
- **Описание:**

  Возвращает данные для выгрузки сценариев
  Параметры:
   МассивТестов - Массив

---

### <a name="служебныеданныесценариев"></a>СлужебныеДанныеСценариев

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СлужебныеДанныеСценариев(Параметры) Экспорт
  ```
- **Описание:**

  Возвращает считанные служебные данные сценариев из репозитория
  Параметры:
   Параметры - Структура

  Возвращаемое значение:
   Структура

---

### <a name="новыйзапроснаслияние"></a>НовыйЗапросНаСлияние

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НовыйЗапросНаСлияние(ДанныеДляСозданияЗапросаНаСлияние) Экспорт
  ```
- **Описание:**

  Создает новый запрос на слияние по указанным параметрам

  Параметры:
   ДанныеДляСозданияЗапросаНаСлияние - Структура

  Возвращаемое значение:
    Структура

---

### <a name="закрытьзапроснаслияние"></a>ЗакрытьЗапросНаСлияние

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЗакрытьЗапросНаСлияние(ДанныеДляСозданияЗапросаНаСлияние) Экспорт
  ```
- **Описание:**

  Закрывает запрос на слияние по указанным параметрам

  Параметры:
   ДанныеДляСозданияЗапросаНаСлияние - Структура

  Возвращаемое значение:
    Структура или Неопределено

---

### <a name="состояниетестированияветки"></a>СостояниеТестированияВетки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СостояниеТестированияВетки(Ветка) Экспорт
  ```
- **Описание:**

  Возвращает состояние тестирования ветки

  Параметры:
   Ветка - СправочникСсылка.Ветки

  Возвращаемое значение:
    Перечисление.СостоянияТестированияВетки

---

### <a name="добавитьсообщениепообъекту"></a>ДобавитьСообщениеПоОбъекту

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьСообщениеПоОбъекту(СсылкаНаОбъект, ПользовательПолучательСообщения, ПользовательАвторСообщения, ДатаНачала, 
  									ТекстСообщения, ШаблонСообщения = Неопределено) Экспорт
  ```
- **Описание:**

  Добавляет сообщение системы взаимодействия указанному объекту
  Параметры:
   СсылкаНаОбъект - Ссылка
   ПользовательПолучательСообщения - СправочникСсылка.Пользователи
   ПользовательАвторСообщения - СправочникСсылка.Пользователи
   ДатаНачала - Дата
   ТекстСообщения - Строка
   ШаблонСообщения - Строка

---

### <a name="добавитьнеконтекстноесообщение"></a>ДобавитьНеКонтекстноеСообщение

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьНеКонтекстноеСообщение(ПользовательПолучательСообщения, ПользовательАвторСообщения, ДатаНачала, 
  									ТекстСообщения, ШаблонСообщения = Неопределено) Экспорт
  ```
- **Описание:**

  Добавляет неконтекстное сообщение системы взаимодействия
  Параметры:
   ПользовательПолучательСообщения - СправочникСсылка.Пользователи
   ПользовательАвторСообщения - СправочникСсылка.Пользователи
   ДатаНачала - Дата
   ТекстСообщения - Строка
   ШаблонСообщения - Строка

---

### <a name="собщенияпообъектубезотборапозаголовку"></a>СобщенияПоОбъектуБезОтбораПоЗаголовку

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СобщенияПоОбъектуБезОтбораПоЗаголовку(СсылкаНаОбъект, ПользовательПолучательСообщения, ПользовательАвторСообщения, ДатаНачала, ТекстСообщения = Неопределено) Экспорт
  ```
- **Описание:**

  Возвращает сообщения системы взаимодействия по указанному объекту согласно переданным параметрам
  Параметры:
   СсылкаНаОбъект - Ссылка
   ПользовательПолучательСообщения - СправочникСсылка.Пользователи
   ПользовательАвторСообщения - СправочникСсылка.Пользователи
   ДатаНачала - Дата
   ТекстСообщения - Строка

---

### <a name="gitlabсоединение"></a>GitLabСоединение

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция GitLabСоединение(Сервер) Экспорт
  ```
- **Описание:**

  Возвращает соединение с git сервером
  Параметры:
   Сервер - Ссылка

---

### <a name="гиперссылканазапроснаслияние"></a>ГиперссылкаНаЗапросНаСлияние

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ГиперссылкаНаЗапросНаСлияние(Проект, ИдентификаторЗапросаНаСлияние) Экспорт
  ```
- **Описание:**

  Возвращает гиперссылку на запрос на слияние
  Параметры:
   Сервер - Ссылка

---

### <a name="экранироватьспецсимволыweb"></a>ЭкранироватьСпецсимволыWeb

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭкранироватьСпецсимволыWeb(Знач Стр) Экспорт
  ```
- **Описание:**

  Выполняет экранирование спецсимволов при формировании строке, содержащий обращение ресурсу по http или https
  Параметры:
   Стр - Строка

---

### <a name="неконтекстныесобщениябезотборапозаголовку"></a>НеКонтекстныеСобщенияБезОтбораПоЗаголовку

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция НеКонтекстныеСобщенияБезОтбораПоЗаголовку(ПользовательПолучательСообщения, ПользовательАвторСообщения, ДатаНачала, ТекстСообщения = Неопределено) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

