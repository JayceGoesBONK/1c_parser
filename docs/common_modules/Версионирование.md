# Модуль Версионирование

**Описание подсистемы:** Версионирование

Всего найдено экспортных методов: **11**

## Список методов

- [ЗаполнитьХранилищеВерсии](#заполнитьхранилищеверсии) (Процедура)
- [ДатаВремяПровереннойВерсии](#датавремяпровереннойверсии) (Функция)
- [ЗарегистрироватьИзмененияОбъекта](#зарегистрироватьизмененияобъекта) (Процедура)
- [ЗарегистрироватьИзмененияКоллекцииОбъектов](#зарегистрироватьизмененияколлекцииобъектов) (Процедура)
- [ДобавитьВКоллекциюИзмененияЭлементовСхем](#добавитьвколлекциюизмененияэлементовсхем) (Процедура)
- [ОписаниеСхемаВерсииОбъекта](#описаниесхемаверсииобъекта) (Функция)
- [СтруктураНастроекПользователяДляСравненияОбъектов](#структуранастроекпользователядлясравненияобъектов) (Функция)
- [ВидВерсионируемогоОбъекта](#видверсионируемогообъекта) (Функция)
- [ПодготовитьДанныеДляСравнения](#подготовитьданныедлясравнения) (Функция)
- [ДанныеДляСравненияВерсий](#данныедлясравненияверсий) (Функция)
- [ДатаПоследнегоИзмененияОбъекта](#датапоследнегоизмененияобъекта) (Функция)

---

### <a name="заполнитьхранилищеверсии"></a>ЗаполнитьХранилищеВерсии

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗаполнитьХранилищеВерсии(ЗаписьВерсииОбъекта) Экспорт
  ```
- **Описание:**

  Выполняет помещение в запись регистра печатных форм/схем версионируемого объекта

  Параметры:
   ЗаписьВерсииОбъекта - запись регистра

---

### <a name="датавремяпровереннойверсии"></a>ДатаВремяПровереннойВерсии

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДатаВремяПровереннойВерсии(СсылкаНаВерсионируемыйОбъект, ВидОбъекта = Неопределено) Экспорт
  ```
- **Описание:**

  Определение даты-времени последней проверенной версии
  Если записи не обнаружено - возвращается пустая дата

---

### <a name="зарегистрироватьизмененияобъекта"></a>ЗарегистрироватьИзмененияОбъекта

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗарегистрироватьИзмененияОбъекта(Объект) Экспорт
  ```
- **Описание:**

  Вызывает регистрацию изменений объекта в механизме версионирования

  Параметры:
   Объект - версионируемый объект

---

### <a name="зарегистрироватьизмененияколлекцииобъектов"></a>ЗарегистрироватьИзмененияКоллекцииОбъектов

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ЗарегистрироватьИзмененияКоллекцииОбъектов(КоллекцияИзмененныхОбъектов, ТекущийПользователь, Комментарии) Экспорт
  ```
- **Описание:**

  Регистрация изменений коллекции объектов как новой версии

---

### <a name="добавитьвколлекциюизмененияэлементовсхем"></a>ДобавитьВКоллекциюИзмененияЭлементовСхем

- **Тип:** Процедура
- **Сигнатура:**
  ```bsl
  Процедура ДобавитьВКоллекциюИзмененияЭлементовСхем(КоллекцияИзмененныхОбъектов, Объект, ИмяСправочника) Экспорт
  ```
- **Описание:**

  Вспомогательная процедура добавления в версию функции
  изменения элементов схемы

---

### <a name="описаниесхемаверсииобъекта"></a>ОписаниеСхемаВерсииОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ОписаниеСхемаВерсииОбъекта(Ссылка, ДатаВерсии = Неопределено, Документ = Неопределено, Схема = Неопределено, ВариантыСправки = Неопределено, ВидОбъекта = Неопределено) Экспорт
  ```
- **Описание:**

  Функция возвращает документ с описанием и схемой (если она есть) для версионируемого объекта

---

### <a name="структуранастроекпользователядлясравненияобъектов"></a>СтруктураНастроекПользователяДляСравненияОбъектов

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СтруктураНастроекПользователяДляСравненияОбъектов() Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="видверсионируемогообъекта"></a>ВидВерсионируемогоОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ВидВерсионируемогоОбъекта(Ссылка) Экспорт
  ```
- **Описание:**

  Определяет вид объекта

  Возвращаемое значение:
    ПеречислениеСсылка.ВидыОбъектов   - вид объекта

---

### <a name="подготовитьданныедлясравнения"></a>ПодготовитьДанныеДляСравнения

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПодготовитьДанныеДляСравнения(Ссылка, ДатаВерсии1, ДатаВерсии2, ВидОбъекта = Неопределено) Экспорт
  ```
- **Описание:**

  Подготавливает данные для сравнения версий объектов

---

### <a name="данныедлясравненияверсий"></a>ДанныеДляСравненияВерсий

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДанныеДляСравненияВерсий(Описание1, Описание2, ВариантыСправки1=Неопределено, ВариантыСправки2=Неопределено, ДатаВерсии1='00010101',ДатаВерсии2='00010101', ВидОбъекта=Неопределено) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="датапоследнегоизмененияобъекта"></a>ДатаПоследнегоИзмененияОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ДатаПоследнегоИзмененияОбъекта(Ссылка) Экспорт
  ```
- **Описание:**

  Определяет и возвращает дату последнего изменения указанного объекта.

  Параметры:
   Ссылка - Определяемый тип ВерсионируемыйОбъект - ссылка на объект, дату изменения которого нужно получить.

  Возвращаемое значение:
   ДатаИзменения - дата - дата последнего изменения указанного объекта.

---

