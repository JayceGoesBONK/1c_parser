# Модуль ОбменДаннымиПовтИспСППР

**Описание подсистемы:** Обмен данными повт исп СППР

Всего найдено экспортных методов: **6**

## Список методов

- [ЭтоПодчиненныйУзелРИБСОтбором](#этоподчиненныйузелрибсотбором) (Функция)
- [СформироватьТекстЗапросаПравилРегистрации](#сформироватьтекстзапросаправилрегистрации) (Функция)
- [СформироватьЗапросУзловДляОбъекта](#сформироватьзапросузловдляобъекта) (Функция)
- [МассивРазделовПроектаИзСсылки](#массивразделовпроектаизссылки) (Функция)
- [ПроверятьПринадлежностьДопсвойстваПроекту](#проверятьпринадлежностьдопсвойствапроекту) (Функция)
- [ПараметрыДляОбработкиШаблонаПроцесса](#параметрыдляобработкишаблонапроцесса) (Функция)

---

### <a name="этоподчиненныйузелрибсотбором"></a>ЭтоПодчиненныйУзелРИБСОтбором

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ЭтоПодчиненныйУзелРИБСОтбором() Экспорт
  ```
- **Описание:**

  Определяет является ли данная информационная база
  подчиненным узлом РИБ с фильтрами

  Возвращаемое значение:
   Булево - признак подчиненного узла РИБ с фильтрами

---

### <a name="сформироватьтекстзапросаправилрегистрации"></a>СформироватьТекстЗапросаПравилРегистрации

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СформироватьТекстЗапросаПравилРегистрации() Экспорт
  ```
- **Описание:**

  Возвращает текст запроса ПРО для использования его в обработчиках правил регистрации

  Возвращаемое значение:
   Строка - Текст запроса ПРО

---

### <a name="сформироватьзапросузловдляобъекта"></a>СформироватьЗапросУзловДляОбъекта

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция СформироватьЗапросУзловДляОбъекта(ТипЗначенияОбъекта = "") Экспорт
  ```
- **Описание:**

  Возвращает текст запроса ПРО для использования его в обработчиках правил регистрации

  Параметры:
  ТипЗначенияОбъекта - Строка - Строковое представление типа данных

  Возвращаемое значение:
  Строка - Текст запроса для указанного типа данных

---

### <a name="массивразделовпроектаизссылки"></a>МассивРазделовПроектаИзСсылки

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция МассивРазделовПроектаИзСсылки(ПроверяемаяСсылка) Экспорт
  ```
- **Описание:**

  Возвращает массив с разделами проекта, которые указаны в шапке и в табличной части ссылки.
  Параметры:
   ПроверяемаяСсылка - Произвольный - Ссылка на объект, из которого необходимо получить разделы проекта
  Возвращаемое значение:
   Массив, содержит элементы типа СправочникСсылка.РазделыПроекта.

---

### <a name="проверятьпринадлежностьдопсвойствапроекту"></a>ПроверятьПринадлежностьДопсвойстваПроекту

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПроверятьПринадлежностьДопсвойстваПроекту(НаборСвойствСсылка) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

### <a name="параметрыдляобработкишаблонапроцесса"></a>ПараметрыДляОбработкиШаблонаПроцесса

- **Тип:** Функция
- **Сигнатура:**
  ```bsl
  Функция ПараметрыДляОбработкиШаблонаПроцесса(ШаблонПроцесса) Экспорт
  ```
- **Описание:**

  Описание отсутствует

---

